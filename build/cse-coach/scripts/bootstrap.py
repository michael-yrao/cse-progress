"""cse-coach first-run setup.

STUB — implemented in build step 6. See .claude/skills/cse-init/SKILL.md for the
contract. Will: run the intake, write cse.config.yml, project the chosen
curriculum tiers onto real dates to generate study_guide + week-1 schedule, reset
trackers/logs, and install the git hook (git config core.hooksPath .githooks).
"""

if __name__ == "__main__":
    raise SystemExit("bootstrap.py is not implemented yet (build step 6).")
