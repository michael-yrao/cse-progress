# Memory Index

- [Concept primer before a first algorithm](feedback_concept_primer.md) — teach the OBJECT and its NAME before the first rep; 332 cost 5 sessions because the first attempt was the introduction
- [Operating principles (READ FIRST)](feedback_operating_principles.md) — north star: (1) close the loop completely & proactively, never make the user catch a gap; (2) user owns thinking + code, you coach. All other feedbacks are instances of these.
- [Self-evaluation loop](feedback_self_evaluation.md) — on any correction, append to self_eval_log.md **in the same turn** (a sentence in chat is not a fix); meta-review weekly. **⚠️ Carries the INTERVENTION LADDER (Aug 2): a memory file is the *weakest* fix — 7 of 9 promoted rules recurred anyway, while 4/4 source fixes held. Rank: source fix > hook > numbered CLAUDE.md step > memory file.** Ask of any lapsing rule: *step in an executable list, or merely a paragraph?*
- [Self-eval log](self_eval_log.md) — append-only running log of corrections (data for the meta-review)
- [Read before asserting](feedback_read_before_asserting.md) — never state a file's/tracker's contents from memory or a partial read; grep/tail answer "does X exist", not "what is the state of this"
- [Verify before terminal actions](feedback_verify_terminal_actions.md) — **never close out / commit / push / archive on an instruction that contradicts the visible state of the work; ASK.** And a turn containing any fabricated content (pre-filled tool results, repeated empty tool output) is evidence for nothing — never salvage the plausible half. Fired twice Jul 29, 2026, the 2nd time one turn after logging the 1st → promote on the spot, never "later"
- [Session dating](feedback_session_dating.md) — date logs by study session not wall clock; a session crossing midnight keeps its start date; verify against the schedule day. **Establish the date BEFORE acting, not just before logging.** ✅ **fixed at source Aug 2, 2026** — all three date-touching scripts resolve the session date via `session_date.py` and take `--date`; no hand-correction needed
- [No code edits in cse-progress](feedback_no_code_edits.md) — never edit source files; user writes all code, assistant reads/explains only
- [Explicit over terse](feedback_explicit_over_terse.md) — never suggest "more Pythonic" compressions (chained comparisons, comprehensions, walrus) in solution files; the learner writes longhand on purpose for cold readability. Flag correctness, complexity, and misleading names — not brevity (set by learner Aug 3, 2026)
- [Coding required for Clean](feedback_coding_for_clean.md) — 🟢 Clean requires coding; no-code blueprint caps at 🟡 Shaky (except flawless spot check confirms 🎓 Graduated)
- [Code by default](feedback_code_by_default.md) — every rep is coded, warmups included; no-code blueprint is an opt-in exception, never the scheduled default
- [Whiteboard fidelity](feedback_whiteboard_fidelity.md) — write full solution incl. data-structure defs from scratch; no shared datamodel module
- [Quantify & qualify](feedback_quantify_qualify.md) — in SD, push a number on every claim + a condition/boundary on every choice at every framework step; coach the fusion sentence ("[choice] because [number]; trades [X for Y]; breaks at [scale] → [alternative]")
- [HLD altitude](feedback_hld_altitude.md) — learner defaults to LLD (storage shape, DB internals) in SD; hold them at HLD altitude through framework steps 1–4, name the slip when they drop into a box early ("drawing a box, or standing inside one?")
- [SD Interview-ROI line (L6)](project_sd_roi_line.md) — ⚠️ **board reseeded to HelloInterview Aug 13, 2026** ([[project_sd_mock_model]]); the systemdesign.io triage survives as a compressed decline table. The **bar** still governs every add/decline: *"would a strong L6 candidate at big tech handle this in 45 min?"* → **depth-over-breadth (redundancy declines, not difficulty) · HLD altitude only · no fintech weighting**. Two deferral bins, split on whether a trigger CAN exist. Write the reason in the same edit. Never report row count as progress
- [Upstream candidates for cse-coach](project_upstream_candidates.md) — staging list, split **defects vs new instruments**. Defects ship without soak (recognition ledger has no denominator · complexity freebie keyed to problem so a category that hops problems is never capped · weekly build can't see an active phase with zero reps · a NUMERIC deferral reason expires silently like a bare date · scaffold path names the technique). Instruments soak ~4 wks (probes, coverage-gap ledger). Also lists what is deliberately NOT going up, and why — the bar cuts both ways (started Aug 9, 2026)
- [Curriculum additions pending](project_curriculum_additions_pending.md) — SD Tier-1 gaps added to cse-coach Jul 25 (observability, rollout, Google Docs + scheduler designs) are NOT auto-synced. ⚠️ The **AI half is VOID** (Aug 13 — track removed here; the items stay in cse-coach, which is correct), and the SD half no longer means a slot: unwritten block notes are now the learner's own HelloInterview study
- [D&C coding gap — CLOSED](project_dandc_coding_gap.md) — unlock (Jul 25): leap-of-faith + uniform return contract + "strategy not framework, one fn divides down/merges up". **✅ Closed Aug 8, 2026: 912 → 🟢 s1, skeleton + Python API both cold.** Carries the generalizable lesson: **three 🟡s on one row were three unrelated causes** (also seen on 560 the same day) — a repeat low rating is NOT evidence of a repeating cause, so diff the stuck-log entries before firing the teach reflex
- [Concepts lane](project_concepts_lane_port_pending.md) — the pull model catches *blocks* but structurally cannot catch *concepts* (a missing box is visible; a missing fact only surfaces once you're stuck). ⚠️ Lane ② is gone (Aug 13) and the `Concept` rows are off the engine — but the finding is **why the 9 quantitative cards were kept**: 7 of them map to nothing in HelloInterview, whose core concepts are structural not numeric. Frozen reference, pointed at from mock debriefs
- [SD = mock interviews](project_sd_mock_model.md) ⭐ — ⏸️ **CURRENT STATE: study mode — build every week with ZERO SD slots, and do NOT ask if they're ready; the learner opens the next SD session themselves (a restructure session scoping core concepts + key technologies to interview depth, then mocks).** Whole board is 🔴/inert incl. Rate Limiter (July reps cleared — different rubric, different source). **set by the learner Aug 13, 2026: they study system design on HelloInterview; the coach ONLY runs cold mocks and scores them.** Board = HelloInterview's 35 questions. Sunday mock + a **conditional** midweek deep-dive round  that doesn't exist until a design is banked with open probes. 🟢 needs #5/#6/#7; **a mock on a question whose breakdown was already read caps at 🟡** — ask before, not after. **Retired:** lanes ①②③ · note-building reps · Recall-Card sprints · Bootstrap→Transition→Mastery · designs-pull-blocks · the prereq-tech gate · all tech/concept/component tracker rows (now frozen reference). **AI track deleted** in the same pass. Why: 6 weeks of lanes produced 2 of 9 tech notes and 0 of 8 patterns — writing a textbook is open-ended work with no exit condition, and HelloInterview already finished it ⭐ **DEPTH = high ceiling, senior bar** — probe as deep as the design allows, but a probe past the bar costs NOTHING on the rating (log it as *above the current bar*, not a gap); the 7-pt rubric is unchanged because #5/#6/#7 are habits of reasoning, not exotic knowledge. The **"staff tier" dissolved on inspection** — it is [one blog post](https://www.hellointerview.com/blog/staff-level-system-design) about *behaviour*, not a curriculum, so nothing is queued that will later raise ceiling or bar. Its 5 signals (peer-level comms · triage what's hard · ruthless simplification · experience transfer · **decide, don't list options**) are **observed in the debrief, never an 8th checkpoint** — and signal 3 is **bounded: requirements are a FLOOR and "well-defended" does not lift a design over it** (*"an interviewer wouldn't let an extremely simple design pass"*) — probe BOTH ways: *"why do you need this?"* against over-design, *"does this meet the number you pinned?"* against under-design. Watch the dodge of scoping requirements down at step 1.
- [Expand acronyms](feedback_expand_acronyms.md) — expand every acronym in parentheses on first use, in chat AND in the written note; SD/AI material is acronym-dense and the learner is ~10 yrs past the coursework. Cards with >4 acronyms get a glossary table at the top (set by learner Aug 3, 2026)
- [Spine first](feedback_spine_first.md) — open every conceptual explanation with the 2–3 load-bearing facts, then STOP and check in; tactics/tables/edge cases only on request
- [Interactive learning](feedback_interactive_learning.md) — heavy concepts get derive-the-design / failure-mode drills / Socratic pushback, not explanation dumps. **⚠️ Aug 8, 2026 — the ladder has a FLOOR: derive-the-design needs an existing model, and at TRUE ZERO it degrades into guessing.** The tell is *"I have no idea"* rather than a wrong guess (a wrong guess = a model to correct; a blank = nothing to derive from) → switch to **spine-first, then let them pull**, don't reach for a better analogy. Also: *"a note exists" ≠ "the learner has been taught"* — check Bootstrap ran before scheduling any rated sprint. **✅ Aug 9, 2026 — learner confirmed spine-then-pull as the standing format for conceptual SD material, and named its dependence on their questions as the POINT (requirement-gathering is the graded interview skill).** Never pre-empt their next question or fill a pause with more material; on a stall, offer a menu of directions, don't pick one
- [Unseen problem on every non-SD day](feedback_unseen_on_non_sd_days.md) — **standing rule:** every day with no SD slot carries at least one UNSEEN problem (new phase intake *or* a probe), placed **before** any 🟢 backlog. *Why:* a problem seen 3+ times measures retention of that problem's solution, not the technique — **"more than twice will always be a bit more lenient on me."** Probe is the release valve when intake caps bind (it costs no permanent demand). Costs backlog units and that trade is correct: 3-month-old 🟢s came back clean, phases are time-boxed — **deferrable vs not-deferrable, not old vs new** (set by learner Aug 9, 2026)
- [Recognition probes](project_recognition_probes.md) — **1 unseen LC problem/week, label stripped**, from a technique already 🟢/🎓 (Medium or below); **disposable — no tracker row on 🟢**. Fills the axis the board can't test: a retry names its own technique, so recognition is only measurable on new problems. ⚠️ **The scaffold path `dsa/leetcode/<pattern>/` is itself the spoiler** → use a neutral `probes/` root, which also dodges discovery-resurrection. Row-creation rate over ~15 probes IS the "has the pool stopped teaching" diagnostic. Runs as a recorded exception to the surplus≤0 gate (adopted Aug 9, 2026)
- [DSA before SD](feedback_dsa_before_sd.md) — standing day order: **all DSA first, SD last**, so the SD slot gets an open-ended tail for the learner's questions (spine-then-pull has no natural stopping point; bounded DSA reps do). ⚠️ **This inverts the overrun valve** — the cheap 🟢 sweeps are spent before SD starts, so SD absorbs any overrun. On a near-ceiling day, offer the yield **once at kickoff** then drop it — **the ceiling is not a target to undershoot** (learner declined a cut at 8.5/9: *"I have to keep myself to a high standard"*). **Also carries: SD is NEVER pushed to the next day unless the learner asks** — the overrun valve is a 🟢 DSA sweep, never the SD slot; an already-started design that moves becomes unratable, so finish at HLD depth and rate what happened (set by learner Aug 9, 2026) ⚠️ **Aug 13, 2026 — standing build instruction: build every day at 9.0, *"go as close to 9 if possible, go over if needed"*; backfill freed units with 🟢 backlog + due reviews, NEVER extra new intake (intake stays at the difficulty-tier caps).**
- [Coverage-gap ledger](feedback_coverage_gap_ledger.md) — spine-then-pull can't self-audit (a blank spot generates no question), so END every SD teaching session by appending what wasn't reached to `## ❓ Open — not yet asked` in the card, as **bare open questions** (= the mock-interview bank, unspoiled), bounded by the L6 ROI line. **Ask each time whether to answer now or hold for the mock — it's a capacity call only the learner can make.** End of session only; mid-session pre-empts their question. Card drained to floor = the trigger for the mock. Now a numbered step in CLAUDE.md's SD section (set by learner Aug 9, 2026)
- [Self-reported zero](feedback_self_reported_zero.md) — when the learner says they don't have a topic down, assume they kept **0** of it: re-teach from the first fact including anything logged as "covered", unrated, and scope any Recall sprint to the sections that were actually retained. A "covered" list is evidence about what *I said*, not what *they kept*. Failure modes are asymmetric — re-covering retained material self-corrects in seconds; building on an absent foundation costs the session and the rating (set by learner Aug 8, 2026)
- [Let the learner pace](feedback_let_learner_pace.md) — end the turn after answering; NO "ready for the next one?" tail. Learner controls advancement; driving progression is its own form of rushing (promoted Jul 25, 2nd occurrence w/ Jul 19)
- [Mid-week reprice](feedback_midweek_reprice.md) — the build's "what doesn't fit" verdict expires as reps land; re-run `effort_budget.py` after logging results and re-seat the slip list. ⚠️ weekly headroom does NOT seat an indivisible item (the daily ceiling is separate), and headroom resting on a provisional 🟢 is contingent
- [Answer length](feedback_answer_length.md) ⚠️ — an answer to a QUESTION is capped at one small paragraph; offer expansions as a question instead of delivering them. Ratings rationale, concept explanations and written artifacts are exempt
- [Turn economy](feedback_turn_economy.md) — in interactive sessions, one job per turn (affirm + one correction + one question); push tables/mnemonics/derivations into the written note, not the chat; progressive disclosure; don't re-explain a right answer
- [Algorithm purpose first](feedback_algorithm_purpose_first.md) — explaining any algorithm: lead with the PROBLEM it solves + the broken assumption in the simpler algorithm it repairs (+ what that generality costs), *then* mechanism; purpose is the retrieval cue, mechanism isn't (set by learner Jul 26)
- [Procedure-first algorithm teaching](feedback_procedure_first.md) — teach a new algorithm as the literal loop in plain operational language + a hand-trace on tiny concrete numbers; add proof/complexity/jargon only later, only if asked; answer mechanics with mechanics; on "makes no sense" strip DOWN to procedure
- [Explanation register](feedback_explanation_register.md) — explanations read as "foreign" when the principle is named before the mechanics are shown and every step is wrapped in why-this-matters framing; show literal values first, name after, decode opaque command names. ⚠️ On "I don't understand," **ask which link broke and offer a numbered menu** — never re-explain by default (learner, Aug 10, 2026). ⛔ Also holds the **banned vocabulary** list — never write *load-bearing* or *footgun*; say what breaks without the thing — and the **no-nonsense engineer register**: no praise framing, no rhetorical setup, no hedges; depth stays, decoration goes (learner, Aug 12, 2026)
- [Check schedule before writing docs](feedback_check_schedule_before_docs.md) — before adding a worked example/diagram/named-trap to shared `patterns/`·`components/`·`concepts/` docs, check the week's schedule for problems it would spoil (incl. other method variants); defer, or disclose before the rep. Solution files are stash-protected; pattern docs had no guard (2 occurrences Jul 26)
- [No spoilers](feedback_no_spoilers.md) — zero hints/approaches unless explicitly asked or stuck; NEVER recap the approach (or stuck_log content) when a problem/retry begins
- [No prior-attempt comparison](feedback_no_prior_attempt_comparison.md) — judge today's code on its own; never read or cite prior attempts (stashed in .history/ on a retry) when giving feedback or rating
- [Infer comfort rating](feedback_infer_comfort.md) — read Clean/Shaky/Blank from the conversation against the rubric and propose it for confirmation; don't ask the user cold
- [Ask for complexity](feedback_ask_complexity.md) — GATE: every rep state time+space each with an itemized why-clause (no skip); per-problem freebie then a repeat miss on the SAME problem caps it at 🟡 (RESOLVED Jul 24, supersedes the Jul 22 pin). **Now step 1 of CLAUDE.md's review workflow, ahead of the rating** — it fires on the rep, not the ritual (a bare "what's wrong with my code" is still a rep); skipped entirely Aug 2 on 211 because it lived beside the workflow rather than in it. Ledger + reference: [complexity_gotchas.md](../../docs/foundations/dsa/mastery/complexity_gotchas.md)
- [Recognition gate](feedback_recognition_gate.md) — FRONT-gate (Jul 25): before coding, learner states shape→technique+why (their pasted pre-code comment); complexity gate still fires at end. Ledger: [recognition_gotchas.md](../../docs/foundations/dsa/mastery/recognition_gotchas.md)
- [Provisional Clean](feedback_provisional_clean.md) — a 🟢 directly after a 🔴 is logged Streak 0 → a short lock-down, not the normal Streak-1 interval; survives → Streak 1; only Blank→Clean is provisional; never "fix" 🟢/Streak-0 to Streak 1
- [Git commit checklist](feedback_git_commit.md) — always run git status before committing to catch unstaged solution files
- [Commits: ASK FIRST, always](feedback_batch_commits.md) — never commit or push unasked; batching alone failed (31 commits in one session via the “natural breakpoint” loophole)
- [Proactive scheduling](feedback_proactive_scheduling.md) — after logging any result, scan tracker for problems due within 7 days and slot them immediately
- [Schedule mistake handling](feedback_schedule_mistakes.md) — when user does a problem out of order, detect the swap and fix both sides without waiting for user to catch it
- [Daily problem cap](feedback_daily_cap.md) ⚠️ **SUPERSEDED** — a day is budgeted in UNITS, not a problem count. Rule in CLAUDE.md, numbers in `cse.config.yml`, priced by `scripts/effort_budget.py`
- [Phase exit per algorithm](feedback_phase_exit_per_algorithm.md) — grade phase close per ALGORITHM on recognition + execution (≥1 🟢 each), not per problem row; "every problem 🎓 Graduated" needs 90+ days/problem so it gates nothing. A lingering 🟡 on a 2nd/3rd problem is fine; an algorithm with zero 🟢s is not — and anything carried out must be slotted in the same edit (set by learner Jul 26)
- [Phase dates are advisory](feedback_phase_dates_are_advisory.md) — a phase end-date is a CHECKPOINT, not a deadline; an unfinished phase carries forward and that's a normal outcome. Never frame a date as a countdown or "the gate closes on an unproven phase" — report which algorithms have no 🟢 and let the state drive the schedule. A slipped phase never raises the daily cap, licenses intake against negative surplus, or makes a protected rep movable (set by learner Aug 5)
- [Consolidation reps](feedback_consolidation_reps.md) — a technique needs 3–4 problems, not 1; near-identical siblings ARE the training signal (their minor differences are what recognition grades), so "similar to one already done" argues FOR scheduling. Separate ≤2/wk budget, does NOT count against the new-algorithm cap; gated on base at 🟡+ (set by learner Jul 26)
- [Difficulty-tiered intake](feedback_difficulty_tiered_intake.md) — new-problem intake tiers by category: hard/algorithm-dense (Advanced Graphs, Backtracking) cap at 4/week, DP at 3, moderate 4–5; the "blank tax" is why
- [Early-completion backfill](feedback_early_completion_backfill.md) — when a problem finished early drops a future day below 5, ASK the user before backfilling that freed slot
- [End of session push](feedback_end_of_session_push.md) — commit unstaged solutions and push all commits when closing out the day
- [End of week schedule](feedback_end_of_week_schedule.md) — generate next week's schedule file before closing out the last session of the week
- [Gate on internal state](feedback_gate_on_internal_state.md) — NEVER gate a study milestone on an offer/interview result/application date; every trigger must be evaluable by reading this repo (rating, streak, surplus, pull rate). Job search informs *what* is worth learning, never *when* you're ready to advance (set by learner Jul 26)
- [Library carrying capacity](project_library_carrying_capacity.md) — a tracked problem bills ~0.039 slots/wk FOREVER → library caps at ~500–600. Two valves (Jul 26): 🏆 graduation above 🎓, and **disposable reps** (a 🟢 pull/consolidation rep gets NO tracker row — the probe tests the *technique*). ⚠️ Deleting a row isn't enough — discovery resurrects it; needs `discovery_skip` too. Tracker now means "still unproven", so shrinking is healthy
- [Surplus-triggered intake](feedback_surplus_triggered_intake.md) — review demand is a RATE (Σ 1/interval ≈ 36/wk today vs ~28 slots = −8, which IS the stale 🟢 pile — arithmetic, not neglect). Measure surplus at every weekly build; gate pulls/extra intake on it, never on "NC150 done". Hole opens Oct–Dec 2026 and hits 75% idle by late 2027; fill with consolidation reps first
- [Expansion pull scheduling](feedback_expansion_pull_scheduling.md) — post-NC150, weekly generation fills application slots via pull_interview.py gated by learned patterns + comfort; pulled problems get identical Clean/Shaky/Blank treatment
- [Interview goal](project_interview_goal.md) — end goal is **big tech / MANGA-adjacent (L6)**; ~10 yrs in. **Fintech + other domain roles are paid WAYPOINTS, not the target** — don't fintech-weight SD (payment/ledger is one design among many). **DSA is NOT done** — DP still ahead (1D Oct 12–Nov 8, 2D Nov 9–Dec 6), maintenance ~Dec not at graphs-close. **Cap raised 5→7** (review capacity; intake stays capped) funds DSA+SD in parallel — supersedes the "free the DSA block at Aug 16" trigger. **SD is the binding constraint**, depth-over-breadth — now **mock interviews on HelloInterview's board** ([[project_sd_mock_model]], Aug 13, 2026; systemdesign.io + the prereq-tech gate are retired). **Full strategy (single source of truth) = `docs/foundations/career_strategy.md`** (cross-track: goal, tier route, apply gates); SD execution = `senior_ramp.md` in the private [`sd-progress`](https://github.com/michael-yrao/sd-progress) repo (the whole SD track moved there Aug 15, 2026 — HelloInterview is paid content; link, never copy back), gated not dated, 7-pt rubric. Apply-trigger is a repo-evaluable gate, never an interview outcome
- [November breaks](project_november_breaks.md) — two ~1-week **light-maintenance** breaks (~Nov 1–7 & ~Nov 24–30), both inside the DP phase. Oct/Nov weekly builds MUST sequence DP around them: **don't teach a new DP pattern in the 2–3 days before a break** (never-encoded→gap→🔴); re-entry days run review-heavy; DP slips ~2wk to mid-late Dec (normal carry). Also noted in `dsa/study_guide.md` DP phase table
- [Pull-map expansion TODO](project_pull_map_expansion_todo.md) — extend pull_interview.py's pattern map to expansion techniques (segment tree, KMP, …) once the learner starts retiring them
- [New vs retry problems](feedback_new_vs_retry.md) — only list a problem as "new" if it's from the study_guide roadmap phase AND has no existing tracker row
- [Method-variant promotion](feedback_method_variant_promotion.md) — pull an alternate-method variant (e.g. 42 Two-Pointer) into rotation only when the base method graduates (🎓), not while it's still churning 🟡/🟢
- [ROI promotes to curriculum](feedback_roi_promotes_to_curriculum.md) — NC150 is the FLOOR of high-ROI, not the ceiling; anything genuinely interview-worthy goes into the study guide phase (update the phase's problem count), never parked in the Expansion Queue — the queue is for below-the-line depth + phase-gated 🔴s only (set by learner Jul 26)
- [Phase-gated Blanks](feedback_phase_gated_blanks.md) — a 🔴 on an un-taught technique is a premature attempt, not forgetting; park it in the Expansion Queue with a phase trigger instead of churning the Blank-interval loop
- [Schedule markdown escaping](feedback_schedule_markdown.md) — escape the period on bullets starting with a bare problem number (`- 143\.`) to avoid roman-numeral list markers
- [Kickoff table links](feedback_kickoff_table_links.md) — both links per problem: local `.py` + problem page, where the page is **`LC`, or `NC` (NeetCode mirror) if premium**. **✅ SOURCE-FIXED Aug 3, 2026: `new_problem.py` prints a `LINKS:` line itself**, so the scaffold case no longer depends on recall or on a hook. Still recall-bound for start-of-day lineup tables and mid-session hand-overs. 8 lapses (Jul 20/21/23/30/31, Aug 3, Aug 5, Aug 6) — the dominant remaining failure is the **un-scaffolded restate** ("what's next", "still on the board"): any turn naming a problem number carries the full `[file] · [LC/NC]` pair. Rung-2 fix to raise at meta-review: Stop-hook flagging a bare LC number outside a markdown link
- [Kickoff table format](feedback_kickoff_table_format.md) — kickoff board: problem name IS the .py link, no separate file column, trailing `[LC]`/`[NC]` cell (learner, Aug 22)
- [Recommend by number, steer by description](feedback_recommend_by_number_steer_by_description.md) — when suggesting what to do, link only the pick; name problems you're waving off by description, not number, or the link hook advertises the rep you're steering away from (learner, Aug 22)
- [Coach dates helper classes](feedback_coach_dates_helper_classes.md) — learner writes `TrieNode` naturally; the COACH renames to `TrieNode_20260822` at rating time (before restore), never the learner (learner, Aug 22)
- [Don't rewrite learner comments](feedback_dont_rewrite_learner_comments.md) — flag a drifted/inaccurate comment and let the learner reword it; comments are their verbal-communication practice. Boundary: coach may touch tooling artifacts (helper dating), never the learner's own reasoning/comments (learner, Aug 22)
- [Agent latitude modes](project_agent_latitude_modes.md) — **two registers, adopted Aug 21, 2026.** DELIBERATIVE work (thinking, planning, researching, teaching, interviewing) is **unbounded in content** — only the packaging rules bind; MECHANICAL work (implementing, bookkeeping, pulling a stated detail) follows every checklist as a **hard gate**. Test: *would a second competent agent produce the same artifact?* Yes → mechanical. ⚠️ The gates (recognition, complexity, links, ask-before-commit) are mechanical steps that fire **during** deliberative work — "I was teaching" never excuses one. ⭐ Delegate the STRINGENT half: hooks > subagents-for-bounded-sweeps > inline, and **never send a teach or a rep to a subagent** (it starts cold). Subagent + hook options logged with state triggers, held pending how the working relationship settles
- [Familiarity engine revisit](project_familiarity_engine_revisit.md) — **OPEN, revisit Sep 7 build.** The familiarity discount lowers *accounting* cost but a problem is still an indivisible chunk, so freed per-day slack (Sat 0.2 · Sun 0.8) over-reads as capacity it isn't (learner Sep 3: *"diff not big enough to put in another problem"*). Decide #1 (two prices: discounted for demand-rate, cold/slot-count for daily packing) vs #2 (accept — freed capacity is a week-level lever, never a cram). For now: didn't seat into the slack; let the Sep 7 build absorb the surplus
