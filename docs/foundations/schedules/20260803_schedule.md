# Week of August 3 – 9, 2026

**Goal:** Advance the Advanced Graphs phase. It is judged **per algorithm** — recognition + execution,
≥1 🟢 each — and right now **five of its algorithms have zero 🟢s**: Hierholzer, Floyd-Warshall, Dijkstra,
Bellman-Ford, Prim's. Four of the five have their rep this week (332, 1334, 743, 787); Prim's (1584) and
the second Dijkstra (778) land Aug 11–12. Everything else on the board yields to those four.

> **📌 Policy, set by the learner Aug 5 — read this before writing any date-driven urgency into a build.**
> **Aug 16 is a checkpoint, not a deadline.** If the phase isn't closed by then it **carries forward**, and
> that is a normal outcome, not a failure to recover from. *"It is why we have this system in place… we keep
> carrying on until we finish it. Add new problems slowly and keep trucking along the blank/shaky/cleans."*
> The comfort→interval engine already decides when something is learned; a calendar date cannot, so grading
> against it double-counts. **Report which algorithms have no 🟢 and let that drive the schedule — never
> frame the date as a countdown.** A slipped phase does not raise the daily cap, does not license intake
> against a negative surplus, and does not make a protected rep movable.
> See [[feedback_phase_dates_are_advisory]].

Second theme, forced by the arithmetic below: the 🟢 backlog is now **30 overdue** and growing. That is
not neglect — it's the surplus deficit compounding, and it is stated openly rather than quietly reshuffled.

---

## ⚖️ Capacity check

```
110 rows · 2 🔴 · 18 🟡 · 66 🟢s1 · 22 🟢s2 · 2 🎓
weekly demand  37.6     capacity  28     SURPLUS  −9.6   (last week: −7.3)
```

**Deficit widened by 2.3.** Per the fill table a ≤0 surplus means **no consolidation reps and no
application pulls** — second week running. 1631 and 1514 stay deferred out of the Aug 16 window; the
phase is still judged on 9 problems, not 11 (per-algorithm exit, so the consolidation reps deepen the
bar rather than reach it).

**Scheduled 31 DSA reps against 56 eligible** (26 due Aug 3–9 + 30 already overdue). All 14 🔴/🟡 are
scheduled. The 25 that slip are **all 🟢 s1** — 🟢 carries the most slack, and clearing one only
advances streak 1→2.

**⚠️ The overdue 🟢 pile is the thing to watch, not this week's board.** It has grown 22 → 30 in a week.
At −9.6/wk it keeps growing until either the library shrinks (🏆 retirement, disposable reps) or
capacity rises. **Raise it at the next weekly build as a standing agenda item, not as a surprise.**

**Per-day load** (⚠️ **cap raised 5 → 7 mid-week, Aug 6** — `cse.config.yml`; the +2/day is 🟢-backlog
sweep capacity only, intake unchanged):

| | Mon | Tue | Wed | Thu | Fri | Sat | Sun |
|---|---|---|---|---|---|---|---|
| DSA problems | 4 | **5** | 4 | **7** | **7** | **7** | **6** |
| Other | SD ② | — | SD ① | SD ② | — | — | SD ③ |

*Days carrying a 🔴 teaching-measurement (Tue, Wed) are held to 4–5 deliberately — a Blank costs its own
slot plus 2–3 follow-up warmups, and both are protected reps that must not be rushed.*

**Cap-7 rework (Aug 6), Thu–Sun raised to hold 7:** Thu did 7 (4 board + 3 overflow, all logged). Fri/Sat/
Sun each get **+2 backlog 🟢** pulled in overflow/oldest-first order — 122, 11 (Fri) · 88, 100 (Sat) · 133,
1011 (Sun) — all existing rows converting s1→s2, so weekly demand *drops*, it doesn't rise. **Two judgment
flags:** (1) **Sat is the heaviest day** — already 4 🟡 back-to-back, so its two additions are deliberately
5-min easies (88, 100), not more mediums; if it overruns, drop 20 🟢 first (unchanged rule). (2) **Sun holds
6, not 7** — it carries the full SD ③ design *plus* the 3 HTTP clusters, and 7 DSA on top of a 45–60-min
design manufactures a bad rep; its two 🟢 sweeps (133, 1011) **yield to next week if the design runs long**
(cheapest to move, no 🔴/🟡 displaced).

---

## Daily Schedule

| Day | Slot 1 | Slot 2 | Active block |
|-----|---|---|---|
| **Mon Aug 3** | ~~**SD ② — Networking fundamentals**~~ ✅ *card written; HTTP half → Thu* | ~~127 Word Ladder 🟡 *(from Jul 31)*~~ ✅ **🟡** ⚠️ *teach trigger*<br>~~229 Majority Element II 🟡~~ ✅ **🟢 s1** | ~~219 Contains Duplicate II 🟢~~ ✅ **🟢 s2**<br>~~994 Rotting Oranges 🟢~~ ✅ **🟢 s2** |
| **Tue Aug 4** | ~~743 Network Delay Time 🟡~~ ✅ **🟡** ⚠️ *Dijkstra still 0×🟢*<br>~~143 Reorder List 🟡~~ ✅ **🟢 s1** | ~~138 Copy List w/ Random Pointer 🟢~~ ✅ **🟡** *(→ Aug 14)*<br>~~2 Add Two Numbers 🟢~~ ✅ **🟢 s2** *(→ Oct 3)* | ~~**332 Reconstruct Itinerary 🔴**~~ ✅ **🟡** *(→ Aug 14)*<br>⚠️ *teaching took — Hierholzer recalled cold* |
| **Wed Aug 5** | ~~**SD ① — Redis re-sprint (RATED)**~~ ✅ **🟡** *(→ Aug 15)*<br>⚠️ *3rd flat 🟡; all 3 drill targets missed again — teach trigger* | ~~787 Cheapest Flights 🟡~~ ✅ **🟡** *(→ Aug 15; Bellman-Ford still 0×🟢)*<br>~~323 Connected Components (BFS) 🟢~~ ✅ **🟢 s2** *(→ Oct 4; fast-track declined — see agenda item)* | ~~**1334 Find the City 🔴**~~ ✅ **🟡** *(→ Aug 15)*<br>⚠️ *teaching took on recognition, not execution; Floyd-Warshall still 0×🟢* |
| **Thu Aug 6** | ~~**SD ② — LB close-out**~~ ✅ *rate-limit table derived · SPOF taught; metaphor + backend failure modes owed. HTTP ×3 → Sun* | ~~540 Single Element 🟡~~ ✅ **🟢 s1** *(→ Sep 5; parity supplied self — teach trigger cleared)*<br>~~261 Graph Valid Tree (DFS) 🟢~~ ✅ **🟡** *(→ Aug 16; undirected-DFS parent-skip + return-propagation bugs, flagged)* | ~~496 Next Greater Element I 🟢~~ ✅ **🟡** *(→ Aug 16; stack self-derived, needed the value-keyed bridge nums1→answer)*<br>~~27 Remove Element 🟢~~ ✅ **🟢 s2** *(→ Oct 5)* |
| **Fri Aug 7** | ~~19 Remove Nth Node (Postorder) 🟡~~ ✅ **🟡** *(→ Aug 17; traversal root, not the counter — teach declined)*<br>~~110 Balanced Binary Tree 🟢~~ ✅ **🎓 fast-tracked** *(→ Feb 3, 2027; over-learned fast-track accepted)*<br>~~122 Best Time to Buy/Sell II 🟢~~ ✅ **🟢 s2** *(→ Oct 6; 3-month-overdue block, item 8 of 8)* | ~~130 Surrounded Regions (UF) 🟢~~ ✅ **🟢 s2** *(→ Oct 6)*<br>~~973 K Closest Points 🟢~~ ✅ **🟢 s2** *(→ Oct 6; complexity freebie spent — loose bound `n log n` for `n log k`, carded)*<br>~~11 Container With Most Water 🟢~~ ✅ **🟢 s2** *(→ Oct 6; 3-month-overdue block CLEARED — 8 of 8)* | ~~42 Trapping Rain Water (Array) 🟢~~ ✅ **🟢 s2** *(→ Oct 6)* |
| **Sat Aug 8** | 269 Alien Dictionary 🟡<br>560 Subarray Sum = K 🟡<br>88 Merge Sorted Array 🟢 *(cap-7 sweep — cheap on purpose)* | 912 Sort an Array (Merge Sort) 🟡<br>235 LCA of BST 🟡<br>100 Same Tree 🟢 *(cap-7 sweep — cheap on purpose)* | 20 Valid Parentheses 🟢 |
| **Sun Aug 9** | **SD ③ — DESIGN: URL shortener** *(+ HTTP ×3, moved from Thu)*<br>*(resumes at step 4 — see gate below)* | 721 Accounts Merge 🟡<br>105 Construct Tree from Pre+In 🟢<br>133 Clone Graph 🟢 *(cap-7 sweep — yields if SD runs long)* | 141 Linked List Cycle 🟢<br>621 Task Scheduler 🟢<br>1011 Capacity to Ship 🟢 *(cap-7 sweep — yields if SD runs long)* |

---

## ⚠️ Protected reps — do not move, do not rush

**332 (Tue) and 1334 (Wed) are the measurement halves of two teaching sessions.** Both were taught in
full after a 🔴; these reps are what tell you whether the teaching took. **Moving them invalidates the
measurement** — that's the whole reason they're hard-slotted rather than floating.

- **332 Reconstruct Itinerary 🔴** — 2nd consecutive Blank, then taught. Hierholzer has **zero 🟢** and
  one problem. If this comes back 🔴 a third time, the technique is not being forgotten, it was never
  encoded — stop re-repping and treat it as a phase-gated Blank (park it with a trigger, per
  `feedback_phase_gated_blanks`).
- **1334 Find the City 🔴** — first exposure Jul 31, taught in full (Floyd-Warshall). A 🟡 here is a
  **good** outcome; per `feedback_infer_comfort`, a rep where the algorithm was handed over caps low,
  and that's expected on a measurement.

**Neither is eligible for a 🟢 lock-down shortcut.** A 🟢 directly after a 🔴 is **provisional (Streak 0,
+10)**, not Streak 1 — and this week is the second consecutive week a provisional lock-down has failed
(787 on Jul 26, 211 on Aug 2). Log them Streak 0 and expect to re-check.

---

## Warmup Notes

- **Everything is coded from a blank page**, warmups included. A no-code blueprint is an explicit opt-in
  that hard-caps at 🟡.
- **743 Network Delay Time (Tue)** is the purest Dijkstra rep on the board and it gates the
  `dijkstra.md` technique note (staggered write plan, set Jul 31). Dijkstra has **two** problems and
  **no 🟢** — this is the conversion rep.
- **540 Single Element (Thu)** — ⚠️ 5th attempt, still 🟡. **If pair-start parity has to be supplied
  again, stop re-repping and teach it** (§2a). Three 🟡s on the same sticking point is a teaching signal,
  not a repetition signal.
- **19 Remove Nth Node (Fri)** — ⚠️ 2nd 🟡 on the Postorder variant; both failures were the counter's
  **origin** (it counts from the tail), not the recursion. A third → teach it.
- **269 Alien Dictionary (Sat)** — remaining gap is **edge-set bookkeeping** (dedupe before incrementing
  indegree), not modeling. Modeling was the Jul 27 failure and it's fixed; don't re-diagnose it.
- **912 Merge Sort (Sat)** — 3rd consecutive 🟡, and **not** a teach-again trigger. D&C is solid; the
  failure moved to Python API (`append` vs `extend`). Read the stuck-log entry, not the rating.
- **Saturday is four 🟡s in a row.** That's the heaviest cognitive day of the week by some margin. If it
  overruns, drop **20 Valid Parentheses 🟢** first — it's the only 🟢 on the day and the cheapest to move.

---

## Backlog Status ⚠️

**25 🟢 s1 reviews slip out of this week.** Listed so none are lost — they are not rescheduled to a date,
because there is no date with room. They re-enter at the next build in overdue order.

*Oldest first (the ones most at risk of decaying below 🟢):* 1929 Concatenation of Array (Apr 24) ·
217 Contains Duplicate (Apr 24) · 26 Remove Duplicates (May 2) · 344 Reverse String (May 4) ·
125 Valid Palindrome (May 5) · 1768 Merge Strings Alternately (May 10) · 11 Container With Most Water
(May 14) · 122 Best Time to Buy/Sell II (May 17) · 88 Merge Sorted Array (Jul 4) · 100 Same Tree (Jul 5) ·
133 Clone Graph (Jul 7) · 1011 Capacity to Ship (Jul 12) · 21 Merge Two Sorted Lists (Jul 12) ·
572 Subtree of Another Tree (Jul 12) · 680 Valid Palindrome II (Jul 12) · 14 Longest Common Prefix
(Jul 15) · 695 Max Area of Island (Jul 17) · 2300 Successful Pairs (Jul 19) · 53 Maximum Subarray
(Jul 27) · 323 Connected Components (UF) (Jul 29) · 66 Plus One (Jul 31) · 684 Redundant Connection
(Jul 31) · 703 Kth Largest in a Stream (Aug 1) · 19 Remove Nth Node (Iterative) (Aug 8) ·
36 Valid Sudoku (Aug 8).

⚠️ **Eight of these are 3+ months overdue** (the Apr–May block). A 🟢 s1 that hasn't been touched since
April is not meaningfully still 🟢 — the rating is a claim about *recall*, and recall decays whether or
not the tracker says so. **Two honest options, both better than leaving them:** schedule a batch of them
as a single low-cost sweep day, or accept that some belong in the 🏆 retirement / disposable-rep lane and
shrink the library deliberately. Decide at the next build.

---

## 🔁 Overflow block — pull from here, in this order, when a day finishes early

**Set by the learner Aug 3.** Last week ran busy: the 🔴s went uncleared and a few problems were skipped,
so this week is expected to absorb some of that. This list is the **pre-approved** answer to "I have time,
what next?" — it exists so a spare 30 minutes turns into a rep instead of a decision.

**This list supersedes the ask-before-backfilling rule** ([[feedback_early_completion_backfill]]) *for
these items only*: the order is already the learner's call, so pull the next one and go. Anything **not**
on this list still gets asked about first.

| # | Problem | Due | Diff | Why this rank |
|---|---|---|---|---|
| 1 | ~~1929 Concatenation of Array~~ ✅ **🟢 s2** *(Aug 5; → Oct 4)* | Apr 24 | Easy | oldest; ~5 min |
| 2 | ~~217 Contains Duplicate~~ ✅ **🟢 s2** *(Aug 5; → Oct 4)* | Apr 24 | Easy | oldest; ~5 min |
| 3 | ~~26 Remove Duplicates from Sorted Array~~ ✅ **🟢 s2** *(Aug 5; → Oct 1)* | May 2 | Easy | in-place two-pointer, worth keeping warm |
| 4 | ~~344 Reverse String~~ ✅ **🟢 s2** *(Aug 6; → Oct 5)* | May 4 | Easy | ~5 min |
| 5 | ~~125 Valid Palindrome~~ ✅ **🟢 s2** *(Aug 6; → Oct 5; O(1) two-pointer noted for next time)* | May 5 | Easy | pairs naturally with 344 and 680 |
| 6 | ~~1768 Merge Strings Alternately~~ ✅ **🟢 s2** *(Aug 6; → Oct 5)* | May 10 | Easy | ~5 min |
| 7 | ~~11 Container With Most Water~~ ✅ **🟢 s2** *(Aug 7; → Oct 6)* | May 14 | Med | last of the 3-month block; the only non-trivial one |
| 8 | ~~122 Best Time to Buy and Sell Stock II~~ ✅ **🟢 s2** *(Aug 7; → Oct 6)* | May 17 | Med | last of the 3-month block |
| 9+ | then the Jul backlog, oldest first | — | — | ~~88~~ · ~~100~~ · ~~133~~ · ~~1011~~ *(→ Sat/Sun, cap-7)* · 21 · 572 · 680 · 14 · 695 · 2300 … |

### Rules for pulling from it

- **🟢 backlog only — never new intake.** Surplus is −9.6; a pull that adds a *new tracker row* raises
  weekly demand forever and is still blocked. These rows already exist, so clearing them costs nothing
  future-facing. **The two are not the same thing and the cap doesn't treat them the same.**
- **Never at the expense of a scheduled 🔴/🟡.** If the day's own board isn't finished, the board wins.
  In particular **332 (Tue) and 1334 (Wed) are protected** — an overflow pull must never crowd a
  teaching measurement.
- **Respect the daily cap of 5.** Overflow reps count. A day at 5 is finished, even if it felt easy.
- **Expect some to come back 🟡, and treat that as the point.** A 🟢 untouched since April is an
  *unverified* 🟢. If several slip, that's the staleness hypothesis confirmed — it's a measurement, not a
  setback, and it's why this block exists rather than blanket-retiring the rows.

### Why this is the highest-value spare-time work available

Clearing a 🟢 s1 moves it to **s2**, which changes its interval from **30 days → 60**, halving what that
row bills every week thereafter. Ten conversions ≈ **−1.2 reps/wk of demand, permanently.**

**This is the only lever that improves surplus without shrinking the library** — retirement is the other
one, and it costs coverage. So an easy sweep isn't filler; it's the structural fix, and it happens to be
made of five-minute problems.

---

## System Design (3×/week)

| Slot | Lane | Item |
|---|---|---|
| **Mon Aug 3** | ② blocks & probes | ✅ **Networking fundamentals** — `concepts/networking_basics.md` written (see split below) |
| **Wed Aug 5** | ① tech fluency | ✅ **Redis re-sprint (RATED) → 🟡** (7/12; → Aug 15). ⚠️ **Teach trigger fired** — cards 8/9/10/12 have now missed three sprints running. Next Redis slot is a **teach, not a sprint.** Card redesign underway (see below). |
| **Thu Aug 6** | ② blocks & probes | ✅ **LB close-out** — rate-limit interaction table **learner-derived** (incl. the two-threshold mirror + the LB/rate-limiter coupling punchline); LB-as-SPOF **taught** (floating IP + heartbeat + gratuitous ARP failover — owed a blind sprint); metaphor **learner-derived** (restaurant host). **Still open:** 2 backend failure modes (dead/slow → health checks) — owed. **HTTP clusters ×3 → moved to Sun Aug 9** ahead of the URL shortener (pre-approved split). |
| **Sun Aug 9** | ③ designs | **HTTP clusters ×3 first** *(304+`ETag`/`Cache-Control` · method idempotency · 429-vs-503-vs-500 — carried from Thu; cache/error-code semantics, directly useful here)*, **then URL shortener** — resumes at step 4 (HLD) |

### Networking fundamentals — scope (carried from the Jul 27 build, + HTTP added Aug 2)

**Prerequisite, not a leaf.** Requested Aug 1 mid-session (*"I think I am missing a lot of basic
networking details"*), then ambushed a second session on Aug 2 (301-vs-302 in the URL shortener).
A prerequisite beats a blocker, because it silently taxes every session downstream instead of stalling one.

Scope: IP vs TCP vs HTTP nesting · ports · what a "connection" is and what TCP guarantees · TCP vs UDP ·
TLS and what "termination" means · DNS resolution path · OSI layers 3/4/7 · latency vs bandwidth vs RTT.
**Plus the four HTTP clusters** (301-vs-302 · 304 + `ETag`/`Cache-Control` · method idempotency ·
429-vs-503-vs-500) — admitted under the rule *"only if picking it differently changes an architecture."*

⚠️ **Scope is CLOSED.** It grew once, deliberately. A third addition means this is a course and should
split into `concepts/http_semantics.md` as its own card. **What yields:** the PostgreSQL writes/sharding
half, still slipped — a known cost, not drift.

#### ✅ Aug 3 outcome — card written, HTTP half carried to Thu Aug 6

**Done Mon:** the layer-nesting spine · what a middlebox can see (plaintext vs TLS, SNI leak) ·
**TLS termination** + the three LB→backend options · the cold-start RTT ladder (DNS→TCP→TLS, 3 RTTs)
with a distance table · latency vs bandwidth vs RTT · the DNS resolution path · **TTL** with a worked
migration trace · ports and the 4-tuple · TCP's guarantees *and* its non-guarantees · head-of-line
blocking · TCP vs UDP with the "is a late packet still worth anything?" rule · stream framing
(`Content-Length` as length-prefix) · **301-vs-302**. A 15-question Recall Card covers all of it.

**Carried to Thu Aug 6 — the remaining 3 HTTP clusters:** `304` + `ETag`/`Cache-Control` · method
idempotency · 429-vs-503-vs-500. **301-vs-302 was taken first on purpose** — it's the one that
ambushed the Aug 2 URL-shortener session and it gates Sunday's step 4; the other three do not.

⚠️ **Thursday now carries two SD items** (LB close-out ×3 *and* HTTP ×3). That is a genuine overload,
flagged rather than hidden. If it won't fit, the honest split is HTTP clusters → **Sun Aug 9 ahead of
the URL shortener** (they're cache/error-code semantics, directly useful there), *not* dropping them.

**Not yet drilled.** The card was *taught*, not measured — its `design_progress.md` row is 🔴 with no
attempt dates, per the teach/measure split. The blind sprint is a separate rep and needs its own slot.

### URL shortener (Sun) — ⚠️ two gates before step 4

1. **Reconcile the scale numbers.** Jul 20 banked 1k/100k QPS; Aug 2 re-derived 100/10k. Same 100:1
   ratio, **one order of magnitude apart.** Pick one — every downstream infrastructure claim inherits it.
2. **Short-code generation.** ~15B codes needed; the length calculation and the *uniqueness* mechanism
   (counter+base62 vs. hash-and-truncate vs. pre-generated key pool) are both untouched. **Do this before
   drawing boxes** — the write path grows a box, or doesn't, depending on the answer.

Banked Aug 2: scale arithmetic ✅, step 3 API ✅, the 301/302 fork ✅, and the `ClickEvent` entity (the
analytics log is ~40× the product data and only exists *because* 302 was chosen). Steps 4–5 remain —
realistically two sessions, so this likely runs into Aug 16.

---

## End-of-Week Targets

- [x] **All four Advanced Graphs conversion reps attempted** — 332 ✅ · 743 ✅ · 787 ✅ · 1334 ✅.
      **Done by Wed.** All four came back 🟡.
- [ ] ❌ **≥2 of the five no-🟢 Advanced Graphs algorithms reach 🟢** (Hierholzer · Floyd-Warshall ·
      Dijkstra · Bellman-Ford · Prim's). **Currently 0 of 5, and all four of this week's chances are spent.**
      The next reps: 1584 (Aug 11) · 778 (Aug 12) · 743 (Aug 14) · 787 + 1334 (Aug 15). **This target
      cannot be met this week** — it carries, stated here rather than quietly dropped.
      **Carrying is the expected outcome, not a miss** (see the policy note at the top). The phase stays
      open until the algorithms have their 🟢s; Aug 16 just marks where we check.
- [ ] Networking card written, with a "You'll want this when…" trigger line per section.
- [ ] Redis rated sprint done — it has slipped twice.
- [ ] URL shortener past step 4.
- [x] **The 8 three-month-overdue 🟢s: swept, or a deliberate decision to retire them.** No third week of
      drift. The overflow block above is the sweep — items 1–8 are exactly this list, so working the
      overflow *is* discharging this target.
      ✅ **Met Aug 7 — all 8 swept, all 8 came back 🟢 s1→s2** (1929 · 217 · 26 · 344 · 125 · 1768 on Aug 5–6;
      122 · 11 on Aug 7). **The staleness hypothesis was NOT confirmed** — the prediction was that several
      would slip to 🟡, and none did, on rows untouched for 3–4 months. Worth carrying into the Aug 10
      build: **it is evidence for the fast-track**, since a 🟢 that survives 3 months cold is behaving like
      an over-learned row, not a decaying one. Demand drops ~0.13 reps/week permanently from the interval
      doubling alone.
- [ ] **Recovery target, set by the learner:** last week left the 🔴s uncleared and skipped a few problems.
      Making that up runs through the board, not around it — **332 and 1334 are the uncleared Blanks**, and
      they're already Tue/Wed. Clear those two and the slippage is genuinely recovered; the overflow block
      is the bonus, not the repair.
      ✅ **Met — both cleared.** 332 🔴→🟡 (Aug 4) and 1334 🔴→🟡 (Aug 5). **The board now carries zero 🔴.**

---

## Week of Aug 10 Preview

**⚠️ Aug 12 carries four 🟡 in one day** — 778 Swim in Rising Water · 271 Encode/Decode Strings ·
875 Koko Eating Bananas · 211 Add and Search Words. All four were rated Aug 2 and all took +10.
**Split them at build time**; note that 875 and 271 are both on the boundary/endpoint watch while 211
and 778 are not, so those are the natural pairs to separate.

**⚠️⚠️ Dijkstra now has THREE problems and still ZERO 🟢 (updated Aug 4).** 743 was *the* conversion rep
and came back 🟡 — the algorithm was recognized cold and written correctly from a blank page, but the
problem-specific aggregation (last arrival vs. running sum) had to be supplied. **The remaining chances
before the Aug 16 gate are 778 (Aug 12) and 743 again (Aug 14) — two reps, both inside the final week.**
If both slip, Dijkstra simply stays unproven and carries past Aug 16 with its next reps rescheduled — not a
failure, just not done (see the policy note at the top).
**Protect both dates at the Aug 10 build.**

**⚠️ The Advanced Graphs phase closes Aug 16, and two of its conversion reps fall inside that week:**
1584 Min Cost Connect Points 🟡 → **Aug 11** (Prim's — still zero 🟢, and this is the last rep before the
gate; **protect the date**) and 778 → Aug 12 (second Dijkstra).

**⏳ WAITING ROOM — 42 Two-Pointer, trigger `graduates:42`, evaluated Aug 7: NOT fired, one rep away.**
42 Array went 🟢 s1 → **s2** on Aug 7 (→ Oct 6); the trigger needs **s3 / 🎓**, so the Oct 6 rep fires it.
⚠️ **The trigger's wording was wrong and is now fixed in `dsa_progress.md`** — it read *"when 42 Array
retires (🏆, streak 3)"*, conflating the two tiers across the Jul 26 label swap. 🏆 Retired sits **well
past** streak 3, so read literally it would have deferred the variant by roughly a year beyond its intent.
Re-check at the build after Oct 6, not before.

**⚠️ 19 Remove Nth Node (Postorder) 🟡 → Aug 17** — 3rd consecutive 🟡, but the **first not caused by the
counter's origin** (this one was the traversal root: `dfs` started at `head`, so the dummy could never be
the surgeon and `n == sz` was unreachable). **Teach offered at the Aug 7 rep and declined** — *"re-rep is
fine, I caught the issue instantly."* Do not re-raise it as a new finding at the Aug 10 build; it is a
decided question. **The original trigger still stands** — if the next rep misses on the counter's origin
again, that is the third counter failure and the teach fires.

**⚠️ 127 Word Ladder 🟡 → Aug 13** — 3rd attempt, **2nd consecutive identical off-by-one** on the BFS
level counter, and the layer loop itself regressed from Jul 21. Per the 540/19 rule this is a **teach
trigger, not another re-rep**. ✅ **Decided Aug 3 by the learner: slot the teach.**

**→ Mon Aug 10 · ~15 min · TEACH: counter semantics in layered BFS.** Hard-slotted at the Aug 10
build. **Measured by 127 on Thu Aug 13** — a 3-day gap on purpose, so the rep tests retention rather
than fresh recall (same teach/measure split as 332 and 1334).

The lesson is *not* BFS — 994 was flawless the same day, so the graph modeling is intact. It is the
**counter**, and it has exactly two decisions:

1. **Increment per layer drained, never per node popped.** (127's first bug, Aug 3.)
2. **Start at 0 or 1 depending on the unit the answer asks for** — count the *nodes* (start 1) or the
   *steps between* them (start 0). (127's bug on both Jul 21 and Aug 3.)

| | 994 Rotting Oranges | 127 Word Ladder |
|---|---|---|
| Asks for | minutes elapsed | number of words in the chain |
| Unit | transitions (edges) | layers (nodes) |
| Layer 1 is | minute **0** | word **1** |
| Counter starts at | **0** | **1** |

Covers the whole family — unweighted shortest path, minimum-moves puzzles, word/knight/maze chains.
Write it into `recognition_gotchas.md` **at the teach**, not before.

**🏆 AGENDA ITEM — traversal fast-track sweep (raised by the learner Aug 5).** Asked *"why do I need to do
3 versions of the same problem"* about 323, and the arithmetic says the instinct is right and generalises
well past 323. **Graph/grid BFS+DFS carry 12 rows; tree BFS+DFS carry 10. Of those 22, exactly one is
non-🟢** — 127 Word Ladder, whose failure is *counter semantics*, not traversal (994 was flawless the same
day, and the teach is already slotted for Aug 10). That is **~0.6 slots/week of standing demand for the
learner's strongest area**, against a −9.6 surplus.

The over-learned fast-track (🟢 → 🎓 +180, ~6× load cut) is built for exactly this, and its coverage gate is
easily satisfied here — every one of these techniques has a harder representative still on the normal ladder.
**Do the surplus arithmetic at the build, then sweep, keeping one representative per technique.** Specific
to 323: fast-track **DFS**, keep **Union-Find** — it's the coldest of the three (last touched Jun 29,
overdue since Jul 29) and the only one attached to a live 🟡 (721 Accounts Merge, due Aug 9).

**✅ First fast-track ACCEPTED — 110 Balanced Binary Tree, Aug 7** (🟢 s1 → 🎓 s3, → Feb 3, 2027). Coverage
gate satisfied by 543 Diameter and 105 Construct-from-Pre+In, both harder tree-DFS and both still on the
normal ladder. Demand drops 0.033 → 0.006 slots/day for that row. **This is the precedent for the sweep** —
and read alongside the 323 decline it confirms the framing: the answer varies per row, so bring it as a
menu. Rows where the learner still gets something from writing it out stay; motor-skill rows go.

**⚠️ 323-BFS was offered the fast-track on Aug 5 and the learner DECLINED it** — rated 🟢 s2 (→ Oct 4)
rather than 🎓. Reason given: *"it was easy but was still useful to write out."* **Do not re-offer it as
a new finding at the build** — it is a decided question, not an unnoticed one. This matters for how the
sweep is framed: the fast-track's condition 2 is a *self-report*, so a decline is a valid outcome and
the sweep should be presented as a menu, not a backlog to clear. Expect the same answer on any row the
learner still gets value from writing out.

⚠️ **The 721 caveat, so this isn't mis-read as "UF is fine":** 323-UF is the *easy* UF problem, so keeping it
does **not** address 721. UF *mechanics* are clean (130, 261, 684 all 🟢); the 721 gap is **modeling** — deciding
what the nodes are before anything can be unioned. If that's the real gap, the fix is a consolidation rep on
the modeling side, not another 323 rep.

**⚠️⚠️ Bellman-Ford is now in the same position as Dijkstra — 🟡 again on Aug 5, still ZERO 🟢.** 787 was
*the* Bellman-Ford conversion rep and came back 🟡: the algorithm was derived cold and the frozen-snapshot
relaxation was written correctly from a blank page, but `range(k)` vs `range(k+1)` — **`k` counts stops, not
edges** — needed three nudges to find. **787 is Bellman-Ford's only problem, so its next rep on Aug 15 is the
only rep before Aug 16.** If it slips, Bellman-Ford carries — schedule the next one and move on.
**Protect Aug 15 at the Aug 10 build**, and note that Aug 15 already carries 146 LRU Cache.

**⚠️⚠️ Floyd-Warshall joins them — 🟡 on Aug 5, still ZERO 🟢.** 1334 was the measurement half of the Jul 31
teach and it **half-took**: the algorithm was named cold from "all pairs + small n" before any code, and the
midpoint-outermost loop order was right unaided — but the matrix was supplied (the learner reached for an
adjacency map), the init cases were prompted, and both final-scan bugs were caught by the coach. Recognition
proven, execution not. **1334 is Floyd-Warshall's only problem, so its Aug 15 rep is the single remaining
rep before Aug 16** — same position as Bellman-Ford.

**⚠️ Aug 15 is overloaded, and that's a scheduling problem regardless of the checkpoint.** It carries 787,
1334, *and* 146 LRU Cache — two conversion reps that both need a clear head, on one day. **At the Aug 10
build: move 146 off it and consider pulling one of 787/1334 to Aug 13–14.** Not because Aug 16 is at stake
(it isn't — both carry if they slip), but because **three loaded reps on one day is how you manufacture a
🟡 that the schedule caused rather than the recall.** That reason survives the deadline going away.

**⚠️ Four for four — every Advanced Graphs conversion rep this week came back 🟡** (332 Aug 4, 743 Aug 4,
787 Aug 5, 1334 Aug 5). All four were *recognized* correctly and cold; all four lost the rating on
problem-specific execution details, and no two failures shared a cause. **Read that as a pattern before the
Aug 10 build:** the teaching is landing on recognition and not converting to execution, so more *teaching* is
likely the wrong lever — the reps that follow a teach need to be graded on writing it from blank, which is
already what they are. This is the honest input to the Aug 16 gate decision, not a reason to reshuffle dates.

**🛑 SD ① — Redis is a TEACH next week, not a sprint. Two slots needed, and they are not interchangeable.**
Aug 5's rated sprint came back 🟡 for the third time running, with cards 8/9/10/12 missing on all three
sprints — the 540/19 teach trigger, fired. A fourth cold pass measures the same four gaps again and teaches
nothing.

- **→ Mon Aug 10 or Tue Aug 11 · SD ① · TEACH (unrated):** the four stuck clauses — SPOF naming before
  mitigations · *the score is the mechanism* (ZSet: points vs timestamp, and `ZREMRANGEBYSCORE`) · scale
  **out** ≠ scale **up**, and that the cap is on **cores, not RAM** · the `volatile-*` immortal-key footgun.
- **→ Fri Aug 15 · SD ① · MEASURE (rated):** re-sprint on the redesigned card. A 4-day gap on purpose, so it
  tests retention rather than fresh recall — same teach/measure split as 332 and 1334.
- ⚠️ **The card was rewritten Aug 5** ([`technologies/redis.md`](../system_design/technologies/redis.md)) —
  now **one twelve-node thread**, symptom-first, after the learner challenged the format twice mid-session and
  both critiques were upheld: (1) the old stems named the answer's category ("which **data type**…", "**TTL vs
  LRU**…", "name **three**"), making it recognition rather than recall; (2) the questions had **no connectivity**
  — a quiz rather than a line of inquiry. Each node is now caused by the previous one, matching the shape of the
  Jul 15 derive-the-design session, still the best Redis rep on record. **Accept once, deliberately: Aug 15's rating is not comparable to the four
  sprints before it.** The instrument changed; the interval math still works, the trend line restarts.
- **A second teach agenda now exists and is NOT yet slotted** — the note's coverage is
  Redis-through-the-rate-limiter only (missing: cache-aside/write-through · stampede · invalidation · Cluster
  hash slots · Redlock · Pub/Sub & Streams · data types beyond ZSet). Listed in the note under *Coverage gaps*.
  **Needs ≥2 slots; find them at the Aug 10 build** — recorded here so it isn't lost, per the integrity rule.

**⚠️ Two technique notes are unwritten and both are gate-relevant:** `bellman_ford.md` does not exist, and
the `dijkstra.md` staggered write plan set Jul 31 never ran (743 was supposed to gate it). Of the five
no-🟢 Advanced Graphs algorithms, only Floyd-Warshall and Prim's have `patterns/techniques/` notes.
**Slot both writes at the Aug 10 build** — a phase gate judged per algorithm shouldn't close with two of
them undocumented.

Also due: 787 Cheapest Flights 🟡 → **Aug 15** *(🟡 again Aug 5 — see gate warning above)* ·
743 Network Delay Time 🟡 → **Aug 14** *(🟡 again Aug 4 — see gate warning below)* ·
1334 Find the City 🟡 → **Aug 15** *(🔴→🟡 Aug 5; Floyd-Warshall still 0×🟢 — see gate warning above)* ·
503 Next Greater Element II 🟡 → Aug 10 · 80 Remove Duplicates II 🟢 → Aug 10 ·
98 Validate BST 🟢 → Aug 10 · 323 Connected Components (DFS) 🟢 → Aug 11 · 121 Best Time to Buy/Sell 🟢
→ Aug 13 · 901 Online Stock Span 🟢 → Aug 13 · 146 LRU Cache 🟢 → Aug 15 · 208 Implement Trie 🟢 →
Aug 16 · 75 Sort Colors 🟢 → Aug 16 · 261 Graph Valid Tree (DFS) 🟡 → Aug 16 *(🟢→🟡 Aug 6)* ·
496 Next Greater Element I 🟡 → Aug 16 *(🟢→🟡 Aug 6)*.

**Further out:** 27 Remove Element 🟢 **s2** → **Oct 5** *(s1→s2 Aug 6)* · 344 Reverse String 🟢 **s2** → **Oct 5** *(s1→s2 Aug 6)* · 125 Valid Palindrome 🟢 **s2** → **Oct 5** *(s1→s2 Aug 6; redo in-place O(1) two-pointer)* · 1768 Merge Strings Alternately 🟢 **s2** → **Oct 5** *(s1→s2 Aug 6)* · 540 Single Element 🟢 s1 → **Sep 5** *(🟡→🟢 Aug 6)* · 143 Reorder List 🟢 s1 → **Sep 3** *(🟡→🟢 Aug 4)* · 543 Diameter of Binary Tree 🟢 s1 → Aug 30 · 417 Pacific Atlantic 🟢 s1 → **Aug 30**
*(two on Aug 30 — split at build time)* · 261 Graph Valid Tree (Union-Find) 🟢 s1 → Aug 31 ·
424 Longest Repeating Char 🟢 s1 → Aug 26 · 18 Four Sum 🟢 s1 → Aug 26 ·
229 Majority Element II 🟢 s1 → **Sep 2** *(🟡→🟢 Aug 3)* ·
219 Contains Duplicate II 🟢 **s2** → **Oct 2** · 994 Rotting Oranges 🟢 **s2** → **Oct 2**
*(both streak 1→2 on Aug 3 — the first +60 intervals on the board; split them at build time)* ·
323 Connected Components (BFS) 🟢 **s2** → **Oct 4** *(🟢 s1→s2 Aug 5)*.
