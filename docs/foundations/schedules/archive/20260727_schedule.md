# Week of July 27 – August 2, 2026

**Goal:** **Convert, don't accumulate.** Advanced Graphs has *zero* of its own problems at 🟢 — by the
new per-algorithm exit standard the phase has not locked down a single algorithm. So this week is
built around turning 🟡s into 🟢s, with new intake held exactly at the cap. **332 Hierholzer is the
critical path**: it's the only 🔴 on the board, and a second Blank means the technique was never
encoded rather than forgotten (teach, don't re-rep). Advanced Graphs finishes new intake this week —
**269 Alien Dictionary** (last NC150 problem in the phase), **1334 Floyd-Warshall**, **721 Accounts
Merge**. SD gives its first slot to the **infrastructure review** at the learner's request.

---

## ⚖️ Capacity check (first build under the surplus rule)

```
107 rows · 1 🔴 · 19 🟡 · 67 🟢s1 · 19 🟢s2 · 1 🟢s0
weekly demand  35.3     capacity  28     SURPLUS  −7.3
```

**Over-subscribed, so: reviews + new intake at the cap, and nothing else.** Per the fill table, a
≤0 surplus means **no consolidation reps and no application pulls** this week.

**Consequence, stated plainly — this changes the Advanced Graphs plan.** 1631 and 1514 were promoted
into the phase Jul 26 as consolidation reps. **They cannot be scheduled while surplus is ≤0**, and the
projection doesn't open surplus until Oct–Dec. They are therefore **deferred out of the Aug 16 phase
window** and become the first thing scheduled when surplus turns positive. The phase exit is unaffected:
exit is judged **per algorithm** (≥1 🟢 + recognition), and Dijkstra already has 743 + 778 — the
consolidation reps deepen it beyond the bar rather than reaching it. **Advanced Graphs is judged on 9,
not 11.**

**8 🟢 reviews slip to the Aug 3 week** — 🟢 carries the most slack and cleaning one only advances
streak 1→2. Listed below so none are lost.

**Per-day load after the Wed/Sun top-up** (cap is 5 DSA problems/day):

| | Mon | Tue | Wed | Thu | Fri | Sat | Sun |
|---|---|---|---|---|---|---|---|
| DSA problems | 5 | 3 | **6** ⚠️ | ~~2~~ **1** | 4 | ~~5~~ **2** | **4** |
| Other | — | infra | SD ② | ~~SD ②~~ ❌ | ~~SD ①~~ ~~SD ②~~ ❌ | **SD ②** | SD ③ |

*(Updated Fri Jul 31: Thu ran short — neither the LB close-out nor 1334 happened, so Thu finished at
**1**. 1334 took Friday's active block and **127 Word Ladder 🟡 slipped to Mon Aug 3**, so Friday's count
is unchanged at 4.)*

*(Updated again end of Fri Jul 31 — **the LB close-out did not happen a third time**, so Friday's SD slot
went unused and the close-out moved to **Sat Aug 1**. Saturday was deliberately **lightened 5 → 2 DSA**
at the learner's request: a heavy work week plus a late arrival home. Nothing was deferred to buy the
room — **271, 875 and 211 were all scheduled a day EARLY**, due Aug 2, and simply moved to Sunday, their
real due date. Saturday keeps only the two genuinely **overdue** problems, both of which are also the
higher-value reps: 261 (Jul 28) and 1584 Prim's (Jul 30, and Prim's has no 🟢 with the phase closing
Aug 16). Sunday holds at 4. The three 🟢 displaced from Sunday slip to the Aug 3 week.)*

*(Updated late Jul 29: 235 was pulled from Thu into the Jul 29 session, putting Wed at **6 against a
cap of 5** — a deliberate override, taken knowingly for a ~10-min 🟡 warmup retry.*

*Then revised again the same night: **the Thu LB quiz became the LB close-out** (part 1 of two), which
takes Thu's morning warmup doubled to ~30 min. That's a **5th SD-shaped slot** in a week already at
−7.3, and per the lane rule it comes out of warmups — so Thu's two DSA warmups are displaced: **417 → Fri**
(due 🟡, protected) and **323 → back to the Aug 3 slip list** (🟢, most slack). Thu now carries 2 DSA
problems, but both are **NEW** (721 Union-Find modeling + 1334 Floyd-Warshall), so the day is not light.
Fri rises to 4.)*

**Wed finished *over* the cap at 6**; Mon and Sat sit at it. *(Wed rose to 5 on Jul 27 when 269 logged
🔴 and landed there — the learner kept 733 rather than slip it, on the grounds that a basic BFS is a
~5-minute rep. Noted because a day at cap has no absorption left if Tue's 332 also comes back 🔴 and
needs a +2 slot on Thu.)* **The surplus figure measures the *week*, not the
*day*** — a −7.3 week can still contain slack days, and this build had two. Check the per-day row
before accepting a slip list.

---

## Daily Schedule

> Results only — one line per problem. **Why** a result landed lives in
> [Results — detail](#results--detail) below; a table can't hold prose.

| Day | Morning Warmup (15 min) | Evening Warmup (15 min) | Active Block (45 min) |
|-----|---|---|---|
| **Mon Jul 27** | ~~424 Longest Repeating Char 🟡~~ ✅ 🟢 s1 → Aug 26<br>~~540 Single Element 🟡~~ ✅ 🟡 → Aug 6 | ~~18 Four Sum 🟡~~ ✅ 🟢 s1 → Aug 26<br>~~104 Max Depth 🟢~~ ✅ 🟢 s2 → Sep 25 | ~~269 Alien Dictionary — NEW~~ ✅ 🔴 → Jul 29 |
| **Tue Jul 28** | ~~SD — infra review~~ ✅ **PostgreSQL**, teaching, unrated | ~~19 Remove Nth Node 🟡~~ ✅ 🟡 → Aug 7<br>~~169 Majority Element 🟢~~ ✅ 🟢 s2 → Sep 26 | ~~332 Reconstruct Itinerary 🔴~~ ✅ 🔴 *(2nd)* → **Aug 4** ⚠️ override |
| **Wed Jul 29** | ~~SD ② — load balancer~~ ⏳ **3 of 4 rungs**, teaching, unrated | ~~704 Binary Search 🟢~~ ✅ 🎓 *(early, Jul 26)*<br>~~269 Alien Dictionary 🔴~~ ✅ 🟡 → Aug 8<br>~~560 Subarray Sum = K 🟢~~ ✅ 🟡 → Aug 8<br>~~128 Longest Consecutive 🟢~~ ✅ 🟢 s2 → Sep 27<br>~~733 Flood Fill 🟢~~ ✅ 🎓 → Jan 25 2027<br>~~235 LCA of BST 🟡~~ ✅ 🟡 → Aug 8 *(pulled from Thu — ⚠️ 6th problem, over-cap override)* | ~~912 Merge Sort 🟡~~ ✅ 🟡 → Aug 8 |
| **Thu Jul 30** | ~~SD ② — LB close-out, part 1~~ ❌ **not done — late start; → Aug 3 lane ②**<br>~~235 LCA of BST 🟡~~ ✅ done Jul 29 (🟡) | ~~**721 Accounts Merge — NEW**~~ ✅ 🟡 → Aug 9 | ~~**1334 Find the City — NEW**~~ ❌ **not done — → Fri Jul 31** |
| **Fri Jul 31** | **SD ② — LB close-out, part 1**<br>*(3 of 6 sections — see scope note)* | ~~503 Next Greater Element II 🟡~~ ✅ 🟡 → Aug 10<br>~~543 Diameter of Binary Tree 🟡~~ ✅ 🟢 s1 → Aug 30<br>~~417 Pacific Atlantic 🟡 *(from Thu)*~~ ✅ 🟢 s1 → Aug 30 | ~~**1334 Find the City — NEW**~~ ✅ 🔴 → **Aug 5** ⚠️ override<br>*(Floyd-Warshall — taught in full)* |
| **Sat Aug 1** ⬇️ *lightened* | **SD ② — LB close-out, part 1**<br>*(virtual nodes · sticky sessions · L4-vs-L7)* | ~~261 Graph Valid Tree (UF) 🟡~~ ✅ 🟢 s1 → Aug 31 | ~~**1584 Min Cost Connect Points 🟡**~~ ✅ 🟡 → Aug 11 |
| **Sun Aug 2** | ~~778 Swim in Rising Water 🟡~~ ✅ 🟡 → Aug 12<br>~~271 Encode/Decode Strings 🟡 *(from Sat)*~~ ✅ 🟡 → Aug 12 | ~~875 Koko Eating Bananas 🟡 *(from Sat)*~~ ✅ 🟡 → Aug 12<br>~~211 Add and Search Words 🟢 **prov.** *(from Sat)*~~ ✅ 🟡 → Aug 12 ⚠️ **lock-down failed** | ~~**SD ③ — DESIGN: URL shortener**~~ ✅ **scale gate cleared + step 3 (API) done**<br>*(⚠️ steps 4–5 untouched — ~2 more sessions. Next: reconcile the 10× QPS discrepancy, then short-code generation)* |

---

## Results — detail

**Mon Jul 27**
- **269 Alien Dictionary (new) 🔴** — Kahn's recalled cold and correct; all four failures were graph
  **modeling**, not the algorithm. See `stuck_log.md`.

**Tue Jul 28**
- **SD infra review → PostgreSQL** *(topic chosen by coach: 3× deferred, zero coverage, and Sunday's
  URL shortener needs a datastore)*. Teaching session, unrated —
  [`technologies/postgresql.md`](https://github.com/michael-yrao/sd-progress/blob/main/technologies/postgresql.md) written.
  ⚠️ **Arc half-open: writes/sharding not covered.**
- **19 Remove Nth Node 🟡** — the counter counts from the **end**, compared against a front-indexed `n−1`.
- **169 Majority Element 🟢 s2** — over-learned fast-track offered and **declined**; learner chose the
  extra rep.
- **332 Reconstruct Itinerary 🔴 (2nd consecutive)** — became a teaching session mid-rep, per the
  pre-written rule. **+2 deliberately overridden → rated re-rep Tue Aug 4.** See Warmup Notes.

**Wed Jul 29**
- **SD ② load balancer — 3 of 4 rungs, teaching session, unrated.** Round robin → least connections
  derived from constraints. Note **restructured mid-session from a 5-column table into a ladder** (the
  algorithms are successive *repairs*, and a grid can't hold the "which is why" link). **IP hash
  completed, learner-written**, all four cells — same-server guarantee, load-blindness, key skew, and a
  self-corrected causality inversion (*assumption → skew* vs *skew when the assumption breaks*).
  **Consistent hashing reached the ring**: "hash the servers into the same space" derived cold, then
  "closest"; next-upward-wrapping supplied as convention, and the rest of the section written up by the
  coach when the learner called time. Outstanding: sticky sessions · rate-limit table · L4-vs-L7 ·
  failure modes · metaphor · virtual nodes.
- **704 Binary Search 🎓** — done early Jul 26, over-learned fast-track; **first graduation in the
  tracker**. Next look Jan 22 2027.
- **269 Alien Dictionary 🟡** — 🔴→🟡, real movement. Recognition, full Kahn's, cycle check *and* the
  **prefix-invalid rule** (one of Monday's four modeling failures) all cold. Two bugs supplied:
  `words[i]` vs `words[i-1]`, and duplicate edges double-counting indegree. Complexity **repeat miss →
  capped at 🟡**.
- **560 Subarray Sum = K 🟡** — map *contents*, not technique: pre-counted the whole array, then missed
  the `diffMap[0] = 1` empty-prefix seed.
- **128 Longest Consecutive 🟢 s2** — fast-track **refused**: no harder problem in Hash Set Membership,
  so 128 *is* the coverage.
- **733 Flood Fill 🎓** — over-learned fast-track, **2nd graduation**; BFS coverage gate held by
  127 🟡 / 417 🟡 / 200 / 994.
- **235 LCA of BST 🟡** *(pulled from Thu, late in the session)* — recognition cold and correct, and on the
  mechanism-inventory prompt the learner **deleted their own redundant equality check**, seeing the `else`
  already covers `root == p`. Rated 🟡 on one supplied bug: the recursive calls discarded their return
  value, so anything not answered at the root returned `None`. ⚠️ **Same bug as Jul 19 — 2-for-2, both
  times supplied.** The BST logic is not the gap; recursive return-plumbing is. Complexity freebie spent
  (BST read as balanced). See `stuck_log.md` for the 3rd-occurrence rule.
- **912 Merge Sort 🟡** — **the D&C teaching STUCK**: skeleton, split/recurse-both, uniform return
  contract all cold and correct. Sole defect `append` vs `extend` on the leftover slice → gap moved
  **conceptual → Python API**, so no re-teach.

**Thu Jul 30**
- **721 Accounts Merge (new) 🟡** — recognition, the union-account-indices modeling, the email→indices map
  and the entire DSU all cold and correct on a first exposure; also self-caught an `accounts`/`accts`
  shadowing bug. Rated 🟡 on **five supplied bugs, every one list-mutate-vs-return Python API**
  (`.sort()`→`None`, `+=` vs `append`, name-vs-index ×2, `rankMap`/`parentMap`). Result-assembly was hinted.
  Complexity passed-and-taught (new-problem freebie, 1 of 2), then both bounds derived correctly — including
  challenging the sort bound unprompted with the multiply-vs-add trap. ⚠️ **This is not a Union-Find gap.**
  See `stuck_log.md`.

**Fri Jul 31**
- **543 Diameter of Binary Tree 🟢 s1** — **attempt 8, and the first Clean.** Recognition and both
  complexity bounds volunteered cold with real why-clauses; the hard part (a helper that *returns* height
  while *tracking* diameter via `nonlocal`) came out correct with nothing supplied, including the
  leaf-returns-1 convention that makes `left + right` count **edges** — the usual off-by-one here.
  Front-gate question asked which node the two sides are summed at; "every node, running max" was the
  learner's. ✅ **Counter-evidence on the 235 watch item:** that was 🟡 twice for recursive calls discarding
  their return value, and this rep is entirely return-plumbing, done right.

- **503 Next Greater Element II 🟡** — everything structural cold (monotonic stack, double-pass modular
  circularity, indices-not-values, and the `-inf` sentinel + overwrite guard, which is the subtle piece).
  Two supplied bugs: `i % 2` for `i % len(nums)` — a **slip**, since the pre-code comment stated the right
  intent — and the sentinel never converted back to `-1`, the substantive one. Complexity passed; the
  **amortized** framing (each index pushed ≤2×, popped ≤2×, so pops are bounded by pushes) was taught
  after, since the stated why was per-op cost rather than the aggregate argument. See `stuck_log.md`.

- **417 Pacific Atlantic 🟢 s1** — clean sweep, nothing supplied: reverse BFS from both borders with the
  flipped `>=` comparison, correct Pacific/Atlantic seeding, visited-marked-on-push, set intersection, and
  both complexity bounds on request. ✅ **Coverage consequence:** 733's Jul 29 graduation rested on a BFS
  gate held by *127 🟡 / 417 🟡 / 200 / 994* — with 417 now 🟢, **127 is the last 🟡 holding BFS**, and it's
  on Mon Aug 3's active block. Noted for the phase-exit read.

- **1334 Find the City (new) 🔴** — **became a full teaching session at the learner's request** ("I don't
  know this algorithm at all, teach it all the way"). Brought cold: the every-node-as-source framing, DFS-per-node
  as the naive move, and — given the counterexample — that pruning over-budget paths is legitimate but doesn't
  fix the visited-set problem. Supplied: the entire algorithm, plus 5 bugs. Notably **3 of the 5 bugs sat in the
  final counting loop, the part that wasn't taught**, while the taught triple loop was translated correctly first
  try. Note written live: [`patterns/techniques/floyd_warshall.md`](../dsa/patterns/techniques/floyd_warshall.md).
  ⚠️ **+2 overridden → Aug 5** (see Warmup Notes). See `stuck_log.md`.

**Sat Aug 1**
- **261 Graph Valid Tree (Union-Find) 🟢 s1** — 🟡→🟢, nothing supplied. The front-gate answer named both
  conditions (acyclic **and** exactly `n−1` edges) and why neither alone suffices; the early exit on
  `len(edges) != n−1` before building any structure, path compression, union-by-rank with the correct
  tie-break, and cycle detection via equal roots were all cold. Both complexity bounds volunteered with
  real why-clauses and **both correct**. Two sharpenings offered, neither a miss and **no freebie spent**:
  `α(N)` named explicitly as the DSU constant (learner's own call that it's not worth carrying — correct),
  and the recursive `find` call stack as a second `O(N)` space contributor that happens to be dominated here.
  *Coverage note: Union-Find already held 🟢 at 130 and 684, so this deepens the technique rather than
  unblocking it — **721's modeling is still the 🟢-less item** for the phase-exit read.*

- **1584 Min Cost to Connect All Points (Prim's) 🟡** — 4th attempt, still 🟡, but the residual is narrow.
  Cold: recognition, the greedy loop in words, the skeleton, and both helpers. **The dist-array design was
  the learner's** — asked what the graph's edge count actually is, they got to complete-graph → n², and
  derived the O(V²) array version themselves rather than the heap they'd opened with. Supplied: (a) the
  `while len(visited) < size` reframing, after `for i in range(size)` read as noise — correctly, since `i`
  is a pure round counter and never a node; (b) the relaxation bug, via failing case `[[0,0],[1,0],[0,1]]`
  → 3 vs 2. `updateDistance` **assigned** rather than `min`'d, so `distance[i]` silently meant "distance to
  the last node added" instead of "to the component." Learner localized and fixed it once pointed at the
  array, then rewrote both pre-code comments to state the invariant *and* what it forbids.
  ⚠️ **Prim's still has no 🟢 and the phase closes Aug 16** — this is now the tightest per-algorithm gap.
  Complexity: space O(V) clean; time O(V²) right but derived as a **product** (`(V-1)*(V-2)*…` = V!) where
  it's a **sum** — first miss here, **freebie, no rating hit**, carded. Note written:
  [`patterns/techniques/prims_mst.md`](../dsa/patterns/techniques/prims_mst.md).

**Sun Aug 2**
- **778 Swim in Rising Water 🟡 → Aug 12** — 2nd attempt. Opened with a greedy *step-to-the-cheapest-neighbour +
  DFS + wait for the water* plan; the one hole (**the next cell need not touch the one you're standing on**) was
  surfaced by a counterexample grid, and everything after it was the learner's: the min-heap, `cost = max on
  path` rather than sum, `(elevation, r, c)` as the key, and *"the heap is the traversal order, so DFS-vs-BFS is
  the wrong question — it's best-first."* The **Dijkstra-vs-Prim's discrimination is the keeper** and was
  volunteered into the pre-code comment: no source/target in Prim's, and Prim's key is one edge's weight while
  this key carries the whole path. Code was structurally right first pass — **500 randomized grids vs a
  binary-search+BFS reference, zero mismatches**. Rated 🟡 on **three supplied mechanical bugs** (dead `visited`
  test comparing an int to a tuple set · source pushed with a hardcoded `0` elevation · empty-heap `IndexError`
  on `[[0]]`) **and independently on a complexity repeat miss**. Post-rating, took the mechanism-inventory note
  and **collapsed `maxNodeTraversed` away** — pops arrive in non-decreasing elevation, so the popped value *is*
  the running max.
  - ⚠️ **Complexity repeat miss (freebie spent Jul 23) → capped at 🟡 on its own.** Time `O(N log N)` and space
    `O(N)` were both right with real why-clauses; the miss was the *derivation* of the `time` loop — called
    "constant" because `n ≤ 50 ⟹ grid[i][j] < 2500`, and held on re-ask at n = 1000. `2500` is `n²`, so the
    ceiling moves. **Mirror image of 269's miss** (where `26` really is constant), and the reason both are now
    one entry in `complexity_gotchas.md` under a single test: *does the ceiling depend on the input?* Plus the
    one-liner — **the argument proves too much**: if `n ≤ 50` made it constant, the whole function is `O(1)`.
  - **Interval:** learner asked for it sooner than +10 *unless another Dijkstra lands first*. **743 Network
    Delay Time is due Wed Aug 4**, so the condition is met and the +10 stands.

- **271 Encode and Decode Strings 🟡 → Aug 12** — 5th attempt. Technique named instantly and correctly
  (length-prefix framing), and the **two-pointer parse was right on the first structural pass**: 3000 random
  round-trips including all 256 ASCII characters as payload, `#` and digits inside the data, empty strings and
  the empty list — zero failures. Rated 🟡 on two things. **(1) The discriminator was supplied.** Asked what
  rules out a `#` delimiter, the answer given was *cost* ("having to read char until the next `#`"); the real
  answer is **correctness** — every one of the 256 characters is legal payload, so no rarer delimiter fixes it.
  That distinction *is* the recognition point, and it took a failing case (`["a#b","c"]`) from the coach.
  **(2) One supplied bug**, not self-caught: `while j != '#'` compared the *index* to the character.
  - ⚠️ **Same bug family as 778 the same morning** — `grid[nr][nc] not in visited` vs `(nr,nc) not in visited`.
    Both are *comparing the wrong kind of thing*; neither is an algorithm error. **Two in one session is the
    session's real signal**, and it is a pre-run check, not a knowledge gap: *what type is each side?*
  - **Complexity: freebie spent, no cap** (misses inside one rep spend the single freebie together). Three,
    all carded: the **unit** counted as number-of-strings rather than total characters — *the same correction
    taught on 721 four days ago*; **string immutability**, i.e. `result +=` in a loop is O(N²) not O(1), which
    the learner then **rewrote to `"".join`** unprompted; and **multiply-vs-add** on `decode`'s slices
    (`O(N·n)` for costs that sum to `N`).
  - ⚠️ **Multiply-vs-add is now 3 occurrences in 4 days** (721 Jul 30 caught *unprompted* · 1584 Aug 1 missed ·
    271 Aug 2 missed). The concept is present; the **direction** is what's unstable. Both this and the
    unit-of-work pattern now have standing cue entries in `complexity_gotchas.md`.

- **875 Koko Eating Bananas 🟡 → Aug 12** — 5th attempt, 4th consecutive 🟡, and the residual has narrowed to a
  single line. Cold and correct: the technique named instantly, `max(piles)` derived as the upper bound, the
  **min-boundary loop shape** right first pass, `canFinish` right first pass, and **complexity volunteered
  unprompted with a why-clause** (`O(n log k)` / `O(1)`) — which is itself the fix to Jul 23's logged miss.
  Rated 🟡 on **line 45, wrong three times**: `0, len(piles) - 1` (index space, not answer space) → `0,
  max(piles) - 1` → `1, max(piles) - 1` → correct. All supplied. Also stated the monotone direction inverted at
  the front gate, self-corrected on a concrete case.
  - ⚠️ **`l = 0` is a VERBATIM repeat of Jul 23**, whose entry names the same failing case (`[1], h=1`).
  - ⚠️ **So is 271's `while j != '#'` — a verbatim repeat of Jul 3.** Two of today's three bugs are the *same
    specific slip* recurring across a month, on problems whose algorithms are plainly owned.
  - **Explicitly NOT a §2a teach trigger** *(contrast 540, which is)*. Nothing here was unrecalled — the hard
    parts came out cold, and `canFinish`, which failed on both Jul 3 and Jul 13, was clean. This is a missing
    **pre-run check**, not a missing concept, so re-repping is the wrong instrument and would just re-log 🟡.

**⚠️ SESSION PATTERN (Aug 2) — three problems, three bugs, one root cause: *the variable does not hold what the
code assumes*.** 778: `grid[nr][nc] not in visited` (value vs coordinate-tuple). 271: `while j != '#'` (index
vs character). 875: `l, r = 0, len(piles) - 1` (index vs speed). None is an algorithm error; all three were
supplied; **two are verbatim repeats of entries already in `stuck_log.md`.** The reps themselves were strong —
every technique was named cold, and the structurally hard pieces (best-first + minimax key, the two-pointer
chunk parse, the min-boundary loop) were all first-pass correct.
**Intervention proposed for the Aug 3 build — a pre-run check, not more reps:** before running anything, read
each comparison and each loop-init aloud as *"what does this variable hold?"*, and diff the endpoint sentence
against the initializing line. Costs seconds, targets the actual failure, and is the only thing here that
re-repping demonstrably has **not** fixed in a month.

**Sat Aug 1 — if there's time left over, pull in this order** *(deliberately listed so extending the day
is a choice, not an improvisation — and so that stopping at the planned 2 is a complete day, not a shortfall)*:
1. **211 Add and Search Words 🟢 prov.** — the +10 lock-down after its 🔴→🟢. 787's equivalent **failed**
   its lock-down, so this outranks a normal 🟢 despite the colour. Currently parked on Sunday.
2. **271 Encode/Decode 🟡** or **875 Koko 🟡** — both due Aug 2, both on the boundary-arithmetic watch.
3. Anything from the Aug 3 slip list. Don't — unless 1–2 are done.

**Still to come this week**
- ~~**Thu — LB close-out, part 1**~~ ❌ Jul 30 late start → recovered to Fri Jul 31 (taking Redis's slot;
  Redis → Wed Aug 5) → ❌ **missed again Jul 31, out of time. NOW SAT AUG 1**, with Saturday lightened to
  2 DSA specifically to protect it. ⚠️ **Third consecutive miss.** The pattern is not scope any more —
  scope was already cut to 3 sections — it is that this lane keeps getting scheduled into a day's leftover
  capacity. Saturday is the first time it has been given the *protected* slot rather than the residual one.
  ⚠️ **SCOPE CUT, decided at the swap rather than discovered mid-session — this is contention option (b).**
  Friday's morning warmup is **one 15-min slot**, half of Thursday's two, and Wednesday's post-mortem already
  ruled six sections a two-session block. So Friday ships the **three self-contained sections only —
  virtual nodes · sticky sessions · L4-vs-L7** — and the **rate-limit table · failure modes · metaphor**
  are *formally parked* to Aug 3 lane ②. Parked, not dropped: they're in the Aug 3 preview. Ordering below
  is cheapest-coupling first and is why the cut falls where it does:
  - **TODAY (Fri):** **virtual nodes** — it closes the consistent-hashing rung that was left open. Then
    **sticky sessions** and **L4-vs-L7**; all three are self-contained.
  - **PARKED → Aug 3 lane ②:** **rate-limit table** (the 5×3 grid, and it depends on all four algorithms
    being settled), **failure modes**, **metaphor**.
  - ⚠️ Still **do not re-teach** what's banked: round robin · least connections (incl. stale-counter starvation) ·
    IP hash (all four cells) · the 75% arithmetic · `% N` as the culprit · the ring and its ~1/N property.
  - ⚠️ Consistent hashing remains **TAUGHT, NOT TESTED** (coach-written Jul 29). Virtual nodes extends it, so
    writing it does not measure it either — the **rated sprint** is where it gets tested.
- ~~**Thu — 1334 Floyd-Warshall**~~ ❌ **NOT DONE Jul 30 → moved to Fri Jul 31's active block**, displacing
  **127 Word Ladder 🟡 → Mon Aug 3 active block**. New intake beats a due 🟡 here: 1334 is the *last*
  Advanced Graphs new problem and the only thing standing between the phase and conversion-only, whereas
  127 is a 🟡 re-rep whose slip costs 3 days of interval. Advanced Graphs new intake **still finishes this
  week** if Friday lands.
  ⚠️ Its scaffold was **deleted from this machine** rather than committed, because an unattempted problem file
  is auto-added by discovery as 🔴 Blank / +2 (see CLAUDE.md). It needs re-scaffolding before Friday's block.
- **Thu — 721** counts against the cap: the modeling step (the node is an *email*, not an account) is
  genuinely new even though Union-Find mechanics are 🟢 across 323/684/261.
- ~~**Fri — Redis**~~ → **Wed Aug 5, lane ①.** Card list travels with it: drill cards 9 scale-out, 8 ZSet
  "how", 11 footgun, **+ new card 3b** (`EVAL`/Lua, per-command vs across-commands atomicity).
- **Sat — 211** is a provisional lock-down; see Warmup Notes.
- **Sun — 110 + 973** were pulled back off the slip list, and were due Aug 2 anyway.
- **Sun — URL shortener** resumes at step 3 (steps 1–2 banked Jul 20). ⚠️ **Gate step 3 on the scale
  arithmetic being spoken aloud first** — the Jul 26 mock skipped it entirely.

---

## Warmup Notes

- ~~**⚠️ 211 is a provisional lock-down** (🟢 Streak 0, due Aug 2) — the +10 check after its 🔴→🟢. Same
  shape as 787 last week, which **failed** its lock-down. Treat it as higher priority than a normal 🟢.~~
  ✅ **Ran Aug 2 — and it failed the same way 787 did: 🟢 prov. → 🟡, streak back to 0, → Aug 12.**
  That is **two provisional lock-downs in a row that did not hold**, which is the signal the provisional
  rule was added to catch, and it is working — both would have been booked as +30 under the old math.
  Worth noting the failure was *not* a recall failure: the trie and the wildcard DFS came out whole from
  a blank page. It was a one-line variable slip (root vs. current node). See `stuck_log.md`.
- **Everything is coded from a blank page** — warmups included. No-code blueprints are an explicit
  opt-in that hard-caps at 🟡.
- **332's two outcomes are not symmetric.** 🟡 or better → the phase's hardest algorithm is alive.
  A second 🔴 → **stop re-repping and teach it** (§2a: nothing to forget means the +2 loop just
  re-blanks). Do not schedule a third sprint on the reflex.
  - **RESOLVED Jul 28: the second 🔴 landed.** Jul 28 converted to a teaching session (derive-the-design
    for the post-order append; direct explanation for the exhaust-all-edges loop). What the learner
    brought cold: graph modeling, min-heap for lexical order, and the *edges-not-nodes* discriminator.
    What was supplied: the Eulerian/Hierholzer name, the post-order insight, the `while` loop, the
    append-`node`-not-`child` fix, and the empty-heap guard removal.
  - ⚠️ **The +2 is overridden to Aug 4, and the tracker CANNOT express that.**
    `update_review_dates.py` recomputes `Next Review Date` from comfort+streak+latest on every run
    (there is no override field), so the tracker will read **2026-07-30** and 332 will show as overdue
    Jul 30 → Aug 4. **That is intentional, not a missed problem.** The schedule is the source of truth
    for this slot. Rationale: a Thursday re-rep would measure recall of Tuesday's teaching conversation,
    not retention — an inflated rating there corrupts every interval computed from it (§2a).
  - **Third-🔴 rule, pre-written so it isn't decided in the moment:** if Aug 4 blanks again, stop
    re-repping Hierholzer entirely and change format — the failure is not the algorithm, it is that
    explanation-then-attempt has now failed twice.
- ⚠️ **1334's +2 is overridden to Wed Aug 5, and the tracker CANNOT express that** — same mechanism as 332
  below. `update_review_dates.py` recomputes `Next Review Date` from comfort+streak+latest with no override
  field, so it reads **2026-08-02** and 1334 will show as overdue Aug 2 → Aug 5. **That is intentional.**
  The schedule is source of truth. Rationale: Jul 31 was a full teaching session, so an Aug 2 rep measures
  recall of that conversation rather than retention, and an inflated rating there corrupts every interval
  computed from it (§2a) — the identical correction made on 332 on Jul 28.
  - **On the re-rep, watch the seeding**: 2 of the 5 supplied bugs were table-seeding (edges never written
    in; undirected direction missing), and the taught triple loop was translated correctly first try. The
    counting-loop bugs are ordinary and are *not* the signal.
- **Boundary arithmetic still on watch** — 424, 540, 875, 271 all land this week and all four are
  window/pointer boundary problems. State the invariant aloud before running: *inclusive `[l,r]` →
  length `r−l+1`*.
- **721 is new-technique, not a consolidation rep** — the modeling step (the node is an *email*, not
  an account) is genuinely new even though Union-Find mechanics are 🟢. It counts against the cap.

---

## Backlog Status ⚠️

**New intake: 3 — exactly at the Hardest-tier cap** (269, 1334, 721). No 4th, and no consolidation
reps or application pulls, per the −7.3 surplus.

**🟢 slipping to the Aug 3 week (6)** — none lost, all re-slotted at build time:

| Problem | Was due |
|---|---|
| 323 Connected Components (UF) | Jul 29 — briefly backfilled to Thu Jul 30, then displaced by the LB close-out; **still slipping** |
| 66 Plus One · 684 Redundant Connection (UF) | Jul 31 |
| 703 Kth Largest in Stream | Aug 1 |
| **110 Balanced Binary Tree · 973 K Closest Points** | Aug 2 — **displaced off Sunday Jul 31** to make room for the three problems moved off Saturday. Both 🟢 s1, the most slack on the board |
| **53 Maximum Subarray 🟢** | ⚠️ **variant unresolved — settle this at the Aug 3 build.** The tracker holds three 53 rows: **Prefix Sum** 🟢 s1 due **Jul 27** (overdue), **Kadane** 🟢 s2 due **Sep 24** (not due at all), and a D&C consolidation rep that is still condition-gated. The schedule cell only ever said "53 Maximum Subarray 🟢", so it is not knowable which one was meant. Schedule the **Prefix Sum** row and leave Kadane alone unless you decide otherwise |
| 130 Surrounded Regions (UF) | Aug 2 |
| 261 Graph Valid Tree (**DFS**) | Jul 23 — carried; its **Union-Find twin is Sat Aug 1**, so don't stack them |

**Six pulled back off the slip list**, in two passes:
- *First pass (load re-check):* Wed was carrying 1 DSA problem and Sun 2, both far under the cap.
  **704 + 733 → Wed; 110 + 973 → Sun evening** (the Sun pair were due Aug 2 anyway, so not pulled
  forward — just not slipping).
- *Second pass (704 finished early Jul 26):* that vacated Wed's slot, dropping it back to 2.
  **560 + 128 → Wed**, both due Jul 28/29, so again not pulled forward — they land on their own due
  dates. Wed is now at 4.

**Note the pattern:** an early finish *vacates* a future slot, and the vacancy is invisible unless the
per-day row is re-checked. Backfilling is the default when the day is under cap and something on the
slip list is due that day anyway.

**↩️ REVERSED — Thu Jul 30 backfill (same night).** 235 finishing early dropped Thu to 3, and **323** was
backfilled into the vacancy (approved; overdue since Jul 29, so not pulled forward). **Then Thu's morning
warmup was reassigned to the LB close-out**, which consumed that slot and 417's alongside it. So:

| Problem | Outcome |
|---|---|
| **417 Pacific Atlantic 🟡** | → **Fri Jul 31** — due 🟡, protected over the 🟢 |
| **323 Connected Components 🟢** | → **back onto the Aug 3 slip list** — 🟢 carries the most slack, and cleaning it only advances streak 1→2 |

**The lesson, recorded because it repeated inside one night:** a vacancy is only worth backfilling if it's
still a vacancy when the day is finished being edited. The early-finish check fired **three times** this
week (704, then 560/128, then 235) and this fourth pass **un-fired** — the per-day row has to be re-read
*after* the last edit, not after the first.

**Stale 🟢 pile (~18):** still accepted as stale per the Jul 19 re-baseline policy — each gets a fresh
+30 the next time it's touched. **Not overdue, don't re-list.** And per the capacity math, this pile is
**arithmetic, not neglect**: at −7.3 it cannot drain until demand falls below capacity.

**Phase-gated (not overdue):** 1216 Valid Palindrome III ×2 → Backtracking (Sep 14) / 1D DP (Oct 12).

---

## System Design (4×/week + 1 one-off)

| Lane | Slot | This week | Status |
|---|---|---|---|
| **— (one-off)** | Tue, 1 warmup | PostgreSQL | ✅ teaching, unrated · ⚠️ arc half-open |
| **② Blocks & probes** | Wed, 2 warmups | `components/load_balancer.md` | ⏳ 3 of 4 rungs · ⏱️ overran |
| **② Blocks & probes** | ~~Thu, 2 warmups~~ | ~~LB close-out, part 1~~ | ❌ **not done** — late start Jul 30 |
| ~~**② Blocks & probes**~~ | ~~Fri, 1 warmup~~ | ~~LB close-out, part 1~~ | ❌ **not done Jul 31 — 3rd consecutive miss** (Jul 29 overrun · Jul 30 late start · Jul 31 out of time) |
| **② Blocks & probes** | **Sat, warmup** | ~~**LB close-out, part 1**~~ ✅ **DONE Aug 1** — all 3 sections | ✅ virtual nodes · sticky sessions · L4-vs-L7 all written. **Fourth attempt, first landing** — protecting the slot (Saturday lightened to 2 DSA) is what did it. Teaching session, **unrated**. Sticky sessions largely **learner-derived**; virtual nodes and L4/L7 **taught, not tested** |
| ~~**① Tech fluency**~~ | ~~Fri, 1 warmup~~ | ~~Redis re-sprint (**RATED**)~~ | ❌ **displaced → Wed Aug 5, lane ①**. Due Jul 31, so it carries in **overdue** |
| **③ Designs** | Sun, 45–60 min | URL shortener, from step 3 | queued |

⚠️ **Lane ② now owns two slots this week, which breaks the one-lane-one-slot rule on purpose.** The rule
exists so lanes can't bid against each other for a single slot; here the *same* lane is deliberately given
a second slot because its Wednesday session overran and its output (the LB note) is the thing gating a rated
sprint. Noting it explicitly so it reads as a decision rather than drift — and because it pushes SD to
**5 SD-shaped slots** in a −7.3 week, all of it paid for out of 🟢 warmup capacity.

**↩️ Jul 31 — lane ② took lane ①'s slot outright, so the week is back to 4 SD slots, not 5.** Thursday's
lane-② slot never happened, and rather than let the close-out ride to Aug 3 (where it was already the
5-items-3-slots problem), it took **Friday's lane ① slot** and **Redis moved to Wed Aug 5**. The trade,
stated so it's auditable: the LB note is what *gates* the LB rated sprint, whereas Redis is a rated sprint
that gates nothing downstream — a blocker beats a leaf. Cost: Redis's re-sprint goes **overdue from Jul 31**
and its forgetting gap grows by 5 days, which for a due 🟡 is a real (accepted) loss of measurement fidelity.

**Tue — PostgreSQL, what got covered:** the spine (query-by-non-key · transactions · one write machine),
ACID with Read Committed as the default, and the read-scaling path — cache → primary+replicas, with the
replication-lag tradeoff and the read-your-own-writes fix. Two follow-ups owed (below).

~~**Fri — why Redis outranks PostgreSQL for lane ①:** a due 🟡 carrying fresh material beats new intake.
PostgreSQL yields again.~~ **Moot as of Jul 31 — lane ① has no slot this week.** Redis still outranks
PostgreSQL, and both now bid for the Aug 3 week; the ranking carries forward intact.

**⚠️ The infrastructure slot has a real cost.** It's a 4th SD-shaped slot in a week already at −7.3, so
it comes out of warmup capacity and deepens the 🟢 slip. Accepted deliberately — noting it so it isn't
discovered later.

**Owed from the Jul 28 PostgreSQL teaching session:**
- **Finish the note's writes/sharding half** → next lane ① or ② slot in the Aug 3 week. The arc is
  half-open (reads covered, writes not), and the finish-the-arc guardrail says close it before starting
  a new technology. Includes the **MVCC/vacuum thread**, deliberately deferred from Tuesday — it
  explains both "readers never block writers" and the bloat problem, and bloat is a write-path concern.
- **First RATED blind sprint on PostgreSQL** → Aug 3 week, *after* the writes half exists and with a
  real forgetting gap. Do **not** sprint it this week: it would measure recall of Tuesday's
  conversation, the same §2a error being corrected on 332.

**Owed from the Jul 29 load-balancer session** *(what happened is in Results — detail; this is only what's left)*:
- **Finish the note** → **split across Fri Jul 31 (part 1) + Aug 3 lane ② (part 2)**, per the two-session
  sizing in the lane-② post-mortem below. *(Was Thu + Aug 3; Thu didn't happen.)* Outstanding: sticky
  sessions · rate-limit table · L4-vs-L7 · failure modes · metaphor · virtual nodes — see the Thu bullet in
  *Still to come this week* for which 3 ship today and which 3 are parked.
- 🆕 **Cookies & stateless session tokens — learner-requested Aug 1, parked with a trigger.** Two things,
  one slot: (a) how an LB-issued routing cookie actually works end-to-end (`Set-Cookie`, scope, expiry,
  what happens when it's absent or stale), and (b) the **signed-cookie / JWT** option — session data carried
  *in* the cookie so there's no shared-store lookup at all, and what that costs (revocation is the hard
  part). Came up deriving sticky sessions, where the stateless-servers answer landed on Redis.
  **Trigger: `solved:sticky-sessions + rated:load_balancer`** — i.e. after the LB blind sprint, so this
  doesn't get taught into the middle of the thing it would spoil. Not a date, per the anti-void rule.
- ⚠️ **Consistent hashing is in the note as TAUGHT, NOT TESTED** — coach-written when the learner called
  time. The section carries a banner. Do not read those cells as evidence of retention.
- ~~**Jul 30 quiz is UNRATED**~~ — **quiz dropped Jul 29**, slot reassigned to LB close-out part 1. The §2a
  reasoning is unchanged and still binds: Thursday is a *writing/teaching* session, so **nothing on Thu is
  rated**, and the rated sprint stays in the Aug 3 week with a real forgetting gap (same error being
  corrected on 332 and 912).
- **RATED blind sprint → Aug 3 week**, after the note is finished and with a real forgetting gap.
- **Do not re-teach** what's banked: round robin · least connections (incl. stale-counter starvation) ·
  IP hash (all four cells) · the 75% arithmetic · `% N` as the culprit · the ring and its ~1/N property.

⏱️ **Lane ② overran badly — a planning signal, not a one-off.** A 30-minute slot was asked to carry a
four-rung ladder plus a mid-session restructure, and the last rung had to be handed over rather than
derived — the exact outcome derive-the-design exists to avoid. **At the Aug 3 build:** a `components/`
note comparing 4+ alternatives is a **two-session block**. Don't schedule the remaining LB sections as
one slot either — the rate-limit table alone is a 5×3 grid depending on all four algorithms.

**Owed from the Jul 26 Rate Limiter mock:**
- **Rate Limiter Mastery re-rep → Aug 3 week.** §2a forgetting gap on `EVAL`/Lua + LB routing.
- **7-lens fact-check vs Hello Interview's rate-limiter deep-dive** — deferred; run it *after* the
  re-rep, or against a design that isn't imminent.

### ⚠️ SD contention for Aug 3 — 5 items, 3 slots

~~⚠️ **Updated Jul 30 — Thursday's slot did not happen (late start), so the whole LB close-out lands here.**~~

**↩️ Updated Jul 31 — partly resolved in advance, by taking option (b).** The close-out was recovered into
Friday's slot at cut scope, so Aug 3 inherits **half a close-out instead of a whole one** — but it also
inherits **Redis**, which was pushed here to pay for that. Net: still 5 items, 3 slots.

| Owed item | Wants | Proposed |
|---|---|---|
| 🆕 **Networking fundamentals — `concepts/networking_basics.md`** *(learner-requested Aug 1)* | lane ② | **slot it** — see the note below; **PostgreSQL yields to it** |
| **Redis re-sprint (RATED)** — ⚠️ **overdue since Jul 31** | lane ① | **Wed Aug 5, lane ①** — hard-slotted at the swap, per the Schedule Integrity Rule |
| **LB close-out — remaining 3** *(rate-limit table · failure modes · metaphor)* | lane ② | **lane ②** — now plausibly one slot, which is the point of the cut |
| **LB rated sprint** | any | **viable again** if Friday lands — the note's three gating sections ship today |
| PostgreSQL writes/sharding half | lane ② | **slip** — yields to Redis and the LB remainder |
| Rate Limiter Mastery re-rep · PostgreSQL rated sprint | any | Sunday, or slip |

#### 🆕 Networking fundamentals — why it takes a slot rather than joining the queue

**Requested by the learner Aug 1, mid-session**, on the L4-vs-L7 section: *"I think I am missing a lot of
basic networking details to understand this."* The immediate gap was closed inline (the IP/TCP/HTTP nesting,
ports as apartment numbers, "L4 vs L7 = how many envelopes it opens") — but the request is for the
foundation, not the patch.

**It is a `concepts/` card, not a `components/` note.** Not a box on a diagram; a set of facts needed
*mid-sentence* to justify a claim. That means the **pull model structurally cannot catch it** — a missing box
is visible on the whiteboard, a missing fact only surfaces once you're already stuck, which is exactly what
happened tonight. This is the ambush-log failure the concepts lane exists to prevent.

**Why it outranks the other five owed items:** it is a **prerequisite**, not a leaf or even a blocker of one
thing. It gated comprehension tonight and will gate TLS termination, CDNs, WebSocket-vs-SSE-vs-polling, and
a good part of the URL shortener. A blocker beats a leaf (the Jul 31 rule); a *prerequisite* beats a blocker,
because it silently taxes every session downstream instead of stalling one.

**What yields: the PostgreSQL writes/sharding half**, already marked *slip* in the table above. Noted so the
half-open arc is a **known** cost, not drift — the finish-the-arc guardrail is being overridden deliberately.

**Candidate scope** (a card, not a course — resist expanding it): IP vs TCP vs HTTP nesting · ports · what a
"connection" is and what TCP guarantees · TCP vs UDP · TLS and what "termination" means · DNS resolution
path · the OSI layers that actually come up (3, 4, 7) · latency vs bandwidth vs RTT. Each with a
**"You'll want this when…"** trigger line, per the concepts-lane format.

**➕ HTTP semantics — added Aug 2, learner-requested during SD ③.** Second ambush from the same root: the
301-vs-302 choice in the URL shortener turned out to *be* the design decision (it creates or destroys the
entire analytics firehose — 100 GB/day), and it stalled the derivation until HTTP was explained from
scratch. Same signature as the Aug 1 L4-vs-L7 request, which is the argument for folding it in here rather
than opening a second card.

**Admission rule, to honour "a card, not a course": include a code or method ONLY if picking it differently
changes an architecture.** Everything else is lookup, not recall. That admits four clusters and no more:

| Cluster | The design decision it carries | Trigger line |
|---|---|---|
| **301 vs 302** | permanent redirects are cached by browsers/proxies → you lose the click *and* the ability to ever repoint or expire the link. Load vs. control+analytics | *"…when a redirect is on the hot path"* |
| **304 + `ETag` / `Cache-Control`** | how a cache asks "still valid?" without refetching the body — the mechanism behind every CDN/browser-cache claim | *"…when you claim something is cached"* |
| **Method idempotency** (GET/PUT/DELETE safe to repeat, POST not) | decides whether a client may retry at all — connects straight to [`retry_storms_and_stampedes.md`](https://github.com/michael-yrao/sd-progress/blob/main/concepts/retry_storms_and_stampedes.md) and to idempotency keys on writes | *"…when you add retries or a queue"* |
| **429 vs 503 vs 500** | retryable-with-backoff vs retryable vs never — the client's behaviour is driven by which you return. Ties to [`rate_limiter.md`](https://github.com/michael-yrao/sd-progress/blob/main/components/rate_limiter.md) | *"…when you rate-limit or shed load"* |

**Explicitly OUT** (lookup, not recall): the full status-code table, header catalogues, HTTP/2 vs /3
multiplexing, content negotiation, cookie semantics. If one of these ever gates a design mid-sentence, it
earns its way in the same way these did — by ambushing a session, not by seeming important.

Scope grew once, deliberately, and is now **closed**. A third addition means this is a course and should be
split into `concepts/http_semantics.md` as its own card rather than inflating this one.

**Still decide this at build time.** The pressure moved, it did not disappear: Wednesday's post-mortem
rule stands — **a `components/` note comparing 4+ alternatives is a two-session block** — and the
rate-limit table alone is a 5×3 grid depending on all four algorithms. What is *not* an option is
scheduling six sections into one slot and calling it planned; that already failed on Jul 29 and Jul 30,
and taking option (b) on Jul 31 is the correction.

---

## End-of-Week Targets

- [ ] **332 Hierholzer clears to 🟡+** — or is correctly diagnosed as never-encoded and *taught*
- [x] **Advanced Graphs new intake complete** (269 🟡 · 721 🟡 · 1334 🔴) — **done Jul 31; the phase is
      conversion-only from here.** ⚠️ All three landed 🟡 or worse, so intake finishing is *not* the same as
      the phase being close to exit — none of the three has a 🟢, and exit is judged per algorithm
- [ ] All 15 due 🟡 done
- [x] 912 rated re-rep done (measures the Jul 25 D&C teaching) — **🟡, but the D&C structure held; residual is Python API**
- [→] ~~Redis cleared to 🟡+ **including card 3b**~~ — **moved out of the week Jul 31; → Wed Aug 5, lane ①**
- [~] `components/load_balancer.md` written — **3 of 4 rungs** (round robin · least connections · IP hash), note restructured table → ladder; **close-out part 1 LANDED Sat Aug 1** on its 4th attempt (virtual nodes · sticky sessions · L4-vs-L7, plus the algorithm-family vocabulary table). **Remaining 3 → Aug 3 lane ②**: rate-limit table · failure modes · metaphor
- [ ] URL shortener reaches step 5, **with the scale arithmetic spoken aloud at step 2**
- [ ] Infrastructure review done

---

## 📝 Advanced Graphs technique notes — staggered write plan (set Jul 31)

The learner asked for a note per Advanced Graphs algorithm plus a "when to use each" decision table.
**All four missing notes collided with a rep inside 5 days**, and the decision table is pure *recognition*
content — which is exactly what the front-gate tests. Rather than defer indefinitely or spoil five reps,
each note is written **immediately after that algorithm's next rep**, which is also when it comes out best
(written from a fresh attempt, with the actual sticking points in hand).

| Note | Write it after | Rep date |
|---|---|---|
| ~~`prims_mst.md`~~ ✅ | 1584 Min Cost Connect Points | **Sat Aug 1 — written** |
| `dijkstra.md` | ~~778 Swim in Rising Water~~ → **743 Network Delay Time, covering both** | **Aug 4** *(deferred Aug 2 — see below)* |
| `hierholzer.md` | 332 Reconstruct Itinerary — ⚠️ **the measurement rep, protect it** | **Aug 4** |
| `bellman_ford.md` | 787 Cheapest Flights | **Aug 5** |
| **`graph_algorithm_selection.md`** — the decision table | **last**, once the four above exist | ~Aug 5 |

**↩️ Aug 2 — `dijkstra.md` deferred to Aug 4, taking the option the table already offered.** 778's rep is done,
so the trigger fired — but **743 Network Delay Time is due Wed Aug 4 and is the purest Dijkstra rep on the
board.** A `dijkstra.md` written today is read before that rep, which is the §4 spoiler this plan exists to
prevent (the note would be sitting in the repo for two days with nothing marking it off-limits). Deferring
costs nothing and **produces a better note**: 743 is textbook Dijkstra (`+` accumulation, cost from source) and
778 is the **minimax variant** (`max` in place of `+`), so writing after both lets the note lead with the
variation rather than bolt it on. What 778 banks for it, recorded here so it isn't lost: the *anchors* tell
(Prim's has no source/target) · the *key-memory* tell (one edge's weight vs the whole path's history) · and
*"the heap is the traversal order, so it's neither DFS nor BFS"*.

⚠️ **Do not write any of these early.** 332 is the load-bearing case: it has blanked twice, Jul 28 became
a teaching session, and Aug 4 exists *solely* to measure whether that teaching stuck. A Hierholzer note
read beforehand doesn't reduce that rep's value — it removes its purpose. Same reasoning that overrode
1334's +2 to Aug 5.

✅ Already written: [`floyd_warshall.md`](../dsa/patterns/techniques/floyd_warshall.md) (Jul 31 — safe,
because its rep *was* the teaching session), plus pre-existing `union_find.md`, `topological_sort.md`,
`tree_bfs.md`.

---

## Week of Aug 3 Preview

**Advanced Graphs is conversion-only from here** — no new intake. The phase closes **Aug 16** and is
judged **per algorithm** (recognition + execution, ≥1 🟢 each), on **9 problems**, not 11.

**Per-algorithm status is now GENERATED — read [`technique_coverage.md`](../dsa/mastery/technique_coverage.md), don't re-derive it by hand.** Its `no-green` list *is* the phase-exit blocker list (7 techniques as of Jul 28, four of them this phase), and `thin` names what to pull. It also caught two the hand-maintained line below missed: **912's D&C** and **271 length-prefix encoding**, both 🟡 with no 🟢. Kept for reference:

**Per-algorithm status to re-check when building:** Dijkstra (743, 778) · Bellman-Ford (787) ·
Prim (1584) · Hierholzer (332) · BFS-transform (127) · Topo (269) · Floyd-Warshall (1334) ·
Union-Find modeling (721). **As of Jul 26 not one has a 🟢.**

**Carrying in:**

| Problem | Due | Watch for |
|---|---|---|
| **127 Word Ladder 🟡** | **Jul 31 — overdue, hard-slotted to Mon Aug 3's active block** | Displaced by 1334 (see Jul 30 bullet). Hard + 🟡, so it wants the active block, not a warmup. It is also the **only 🟢-less problem holding BFS-transform**, and 733's graduation leaned on it |
| **332 Reconstruct Itinerary 🔴** | **Aug 4** — hard-slotted | Measurement half of Jul 28's teaching. ⚠️ Tracker shows Jul 30; **ignore that date** |
| **1334 Find the City 🔴** | **Aug 5** — hard-slotted | Measurement half of Jul 31's teaching. ⚠️ Tracker shows Aug 2; **ignore that date**. Watch the **table seeding** specifically |
| 743 Network Delay Time | Aug 4 | — |
| 787 Cheapest Flights 🟡 | Aug 5 | Failed its lock-down. Watch whether the *queue-free* shape returns — recognition was never the problem |
| 540 Single Element 🟡 | Aug 6 | ⚠️ 5th attempt still 🟡. If pair-start parity is supplied **again**, stop re-repping and teach it (§2a) |
| 19 Remove Nth Node 🟡 | Aug 7 | ⚠️ 2nd 🟡 on this variant; both failures were the counter's **origin** (counts from the tail), not the recursion. A third → teach it (§2a) |
| 560 Subarray Sum = K 🟡 | Aug 8 | Broke a 🟢 streak; check the empty-prefix seed cold |
| 912 Merge Sort 🟡 | Aug 8 | 3rd consecutive 🟡 — **not** a §2a teach-again trigger; D&C is solid, failure moved to Python API |
| 269 Alien Dictionary 🟡 | Aug 8 | Remaining gap is **edge-set bookkeeping** (dedupe before incrementing indegree), not modeling |

**Further out (logged here so the date isn't lost before that week's file exists):** 503 Next Greater
Element II 🟡 → **Aug 10** (week of Aug 10, just past the Aug 3 week) · **⚠️ four 🟡 all land on Aug 12** —
778 Swim in Rising Water · 271 Encode/Decode Strings · 875 Koko Eating Bananas · 211 Add and Search Words
*(all four rated Aug 2; +10 each. That is a full day's cap in one slot — **split at build time**, and note
875 and 271 are both on the boundary/endpoint watch while 211 and 778 are not, so they are the natural pair
to separate)* · **1584 Min Cost Connect Points 🟡
→ Aug 11** *(⚠️ **the Advanced Graphs phase closes Aug 16**, and Prim's still has no 🟢 — this rep is inside
the window by 5 days, so it is the last chance to convert Prim's before the phase-exit read. Protect the
date; if the day is over cap, move something else)* · 543 Diameter of Binary Tree
🟢 s1 → **Aug 30** · 417 Pacific Atlantic 🟢 s1 → **Aug 30** *(two on Aug 30 — split at build time)* ·
**261 Graph Valid Tree (Union-Find) 🟢 s1 → Aug 31** *(⚠️ its **DFS twin** is on the Aug 3 slip list, due
Jul 23 — don't stack the two variants on the same day when that one finally lands)*.

⚠️ **Aug 4 and Aug 5 now each carry a teaching-measurement 🔴** — 332 on Aug 4, 1334 on Aug 5, both
hard-slotted overrides. Aug 5 also holds 787 and the displaced Redis re-sprint. **Check the per-day count
before adding anything to either day**, and treat the two 🔴s as protected: they are the measurement halves
of two teaching sessions, and moving them is what invalidates the measurement.

⚠️ **FOUR rows now stack on Aug 8** (560 · 912 · 269 · **235**) — every 🟡 from the Jul 29 session landing
on the same +10. **Spread them across Aug 6–10 at build time.** Note 235 and 912 are the two whose gap is
*mechanical* (return-plumbing; `append` vs `extend`) rather than conceptual, so they're the cheapest two
to move.

Also carrying in: the **6 slipped 🟢** above *(12 slipped at build time; 6 came back on the two
re-check passes)* · **Rate Limiter Mastery re-rep** · 1631 + 1514 **only if surplus turns positive** ·
**Redis re-sprint (RATED), overdue since Jul 31 → hard-slotted Wed Aug 5** · the **LB close-out's
remaining 3 sections** (rate-limit table · failure modes · metaphor) → lane ②.

**Run the surplus computation again at build time.** It should improve as this week's 🟡s convert to
🟢 s1 (+10 → +30 moves a row from 0.10 to 0.033 demand/day); 15 conversions would recover roughly
7 slots/week — which is the entire deficit.
