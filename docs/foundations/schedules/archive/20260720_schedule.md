# Week of July 20–26, 2026

**Goal:** **Clear the two graph 🔴s** (1584 Prim, 127 Word Ladder) that survived two exposures, and
keep **Advanced Graphs** moving (2 new: 332 Reconstruct Itinerary, 778 Swim in Rising Water — Alien
Dictionary → Jul 27). This is a **review-saturated week** — ~14 🟡 and a wall of 🟢 streak-1s all come
due — so the active blocks front-load the 🔴 retries and new intake stays light. **System design goes
3×/week** (three-lane rule): Redis re-sprint (Tue), Caching Bootstrap (Wed), and **Rate Limiter
Mastery** closes its arc Sunday. **🟢 backlog policy set: re-baseline + sample** — one Friday coded
spot-check of a 5-problem cross-pattern sample; the rest of the 23-item stale pile is accepted as stale
and re-baselined to a fresh +30 when next touched (see Backlog Status).

---

## Daily Schedule

| Day | Morning Warmup (15 min) | Evening Warmup (15 min) | Active Block (45 min) |
|-----|------------------------|------------------------|-----------------------|
| **Mon Jul 20, 2026** | ~~206\. Reverse Linked List (**Iterative**) 🟢~~ ✅ — *retention check held; Streak 1→2* + ~~1448\. Count Good Nodes 🟡~~ ✅ → 🟢 Streak 1 | ~~21\. Merge Two Sorted Lists (Recursion) 🟢~~ ✅ → Streak 2 + ~~130\. Surrounded Regions (BFS) 🟢~~ ✅ → Streak 2 *(learner override; agent flagged a seed-coord typo)* | ~~**1584\. Min Cost to Connect All Points 🔴 (retry — Prim's MST)**~~ ✅ → **🟡** (eager array Prim, self-recalled; nudged on loop shape) **· bonus:** ~~200\. Number of Islands (DFS) 🟢~~ ✅ → Streak 2 + ~~226\. Invert Binary Tree 🟢~~ ✅ → Streak 2 + ~~543\. Diameter of Binary Tree 🟢~~ ✅ → **🟡** (pulled forward from Jul 26; 543 slipped → now due Jul 30) |
| **Tue Jul 21, 2026** | ~~503\. Next Greater Element II 🟡~~ ✅ → 🟡 (correct approach; 4 small bugs flagged) + ~~211\. Add and Search Words 🟡~~ ✅ → 🔴 (wildcard DFS taught — (node,index) recursion; re-rep Jul 23) | ~~**SD ① — Redis re-sprint (RATED)**~~ ✅ → **🟡** (~6/11 clean; 4 fused-key finally clean; gaps: 9 scale-out, 8 ZSet "how", 11 footgun) → due Jul 31 | ~~**127\. Word Ladder 🔴 (retry — BFS shortest transform)**~~ ✅ → **🟡** (full structure rebuilt cold; Jul 18 bucket-adjacency teaching stuck; off-by-one on level init flagged) |
| **Wed Jul 22, 2026** | ~~**SD ② — Caching Bootstrap**~~ ✅ *(swaps both warmups, ~30 min)* — pushed from Jul 19; eviction/TTL already paid for by the Redis work — **DONE** (derive-the-design: 3 pillars + both failure modes + Bloom-filter mechanics & optimal-k derivation; full note in [`components/caching.md`](../../../../docs/foundations/system_design/components/caching.md)). *7-lens tack-on NOT reached → moved to Sun Jul 26 (feeds Rate Limiter Mastery).* · **+ ~~242\. Valid Anagram 🟢~~** ✅ → 🟢 Streak 2 (+60, due Sep 20; clean code, space Big-O miss noted — rating consequence pinned) (pulled fwd from Fri — due Jul 22, one warm review) | ↑ *(both warmup slots → Caching)* | ~~**332\. Reconstruct Itinerary — new**~~ ✅ → **🔴** (Eulerian path / Hierholzer taught: drain-heap + post-order append + reverse; derived edges-not-nodes/DFS/drain-all cold, core insight supplied) → rated re-rep Jul 28 |
| **Thu Jul 23, 2026** | ~~124\. Binary Tree Max Path Sum 🟡~~ ✅ → 🟢 Streak 1 (self-caught neg-branch clamp after coach flagged failing case `[2,-1,-2]`; learner held 🟢) + ~~271\. Encode and Decode Strings 🟡~~ ✅ → 🟡 (encode clean; decode loop-control bug — mixed `for j` with two-pointer jump, flagged via `5#Hello5#World` trace, fixed to i-driven while) | ~~875\. Koko Eating Bananas 🟡~~ ✅ → 🟡 (clean BS-on-answer structure; `l=0` lower bound → div-by-zero on `[1],h=1`, flagged; also nudged n·log m vs n log n) + ~~**211\. Add and Search Words 🔴 (re-rep — wildcard DFS)**~~ ✅ → **🟢 Provisional** (Streak 0, +10 lock-down; rebuilt (node,index) wildcard recursion cold, no hints — Jul 21 teaching stuck; search Big-O nudged to 26^d·n) | ~~**778\. Swim in Rising Water — new**~~ ✅ → **🟡** (derived min-heap/Dijkstra core cold via Socratic Qs; coach supplied frontier-sizing correction + caught empty-heap IndexError & missing destination check; space Big-O O(n) miss → O(n²)) **· bonus:** ~~3\. Longest Substring Without Repeating Characters~~ ✅ → 🟢 (re-baseline, pulled fwd from Fri; clean sliding window from blank page, no hints, fresh +30 → Aug 22) |
| **Fri Jul 24, 2026** | ~~206\. Reverse Linked List (**Recursion**) 🟡~~ ✅ → 🟢 S1 (clean recursive rewire, no hints; space O(1)→O(n) stack noted) + ~~567\. Permutation in String 🟡~~ ✅ → 🟢 S1 (finally clean; fixed-window freq-array, no hints; space O(n)→O(1) noted) | ~~229\. Majority Element II 🟡~~ ✅ → 🟡 (Boyer-Moore n/3 correct; leaked loop var `n` used as threshold `n//3` instead of `len(nums)`, flagged via `[1,1,2]`; space O(n)→O(1) miss = 229's freebie, carded) *(242 pulled fwd to Wed)* | **🟢 re-baseline — coded spot-check (4 stale, cross-pattern):** ~~238 (prefix)~~ ✅🟢 S1, 347 (heap) → **pushed to Sat Jul 25** (late Fri session), ~~15 (3-sum/two-ptr)~~ ✅🟢 S1, ~~210 (topo sort)~~ ✅🟢 S1 (Kahn's, reversed-graph trick; time O(V·E)→O(V+E) miss = 210's freebie, carded). Rate each; the rest of the stale pile rides a fresh +30. *(3 sliding-window pulled fwd to Thu Jul 23 to lighten Fri's 8-problem load.)* |
| **Sat Jul 25, 2026** | ~~743\. Network Delay Time 🟡 (Dijkstra)~~ ✅ → 🟡 (visited-on-pop bug + lazy-heap stale-entry guard, both nudged; time O(V log E)→O(E log V) miss = 743's freebie, carded) + ~~355\. Design Twitter 🟡~~ ✅ → 🟢 S1 (clean k-way-merge design from blank page; 2 complexity misses — `follow` O(1)→O(F) list scan, `getNewsFeed` O(n²logn)→O(F·T) capped-heap — both = 355's freebie, carded, no cap) + ~~347\. Top K Frequent 🟢~~ ✅ → 🟢 S1 (re-baseline; clean min-heap-of-k from blank page, time O(n log k) beats O(n log n), no miss; fresh +30 → Aug 24) | ~~143\. Reorder List 🟡~~ ✅ → 🟡 (Floyd+reverse+weave cold & correct; hint on merge pointer-advance, was a typo; complexity clean) + ~~912\. Sort an Array 🟡 (Merge Sort)~~ 📗 **TEACHING (unrated)** — learner surfaced a never-encoded D&C-coding gap ("know the approach, no clue how to code it"); taught divide skeleton + leap-of-faith + "D&C is a strategy not a framework, one fn divides down / merges up". Merge written cold & correct; full solution correct (fuzz-verified). **Rated re-rep → Jul 29** (§2a forgetting gap). Tracker row untouched (not rated today). | ~~**74\. Search a 2D Matrix 🟡**~~ ✅ → 🟢 S1 (clean max-boundary + exact binary search from blank page, no hints; boundary pattern nailed; complexity clean → +30, Aug 24) *(bumped from Thu to make room for 211 🔴)* |
| **Sun Jul 26, 2026** | ~~787\. Cheapest Flights 🟢 (**provisional lock-down**)~~ ✅ → **🟡** (lock-down did NOT hold; Streak stays 0, +10 → Aug 5. Snapshot/Bellman-Ford recognition fully cold, but carried a **vestigial queue** the snapshot made unnecessary; 2 real bugs flagged — counter `break` before the round's work, and stale-`distance` vs `workingDistance` comparison letting a worse edge overwrite a better one. Complexity clean, freebie unspent) + ~~53\. Maximum Subarray (Kadane) 🟢~~ ✅ → **🟢 Streak 2** (+60 → Sep 24; Kadane called before coding, clean from blank page, no hints; `max` taken *before* the reset so all-negative returns the largest element not 0; complexity itemized clean, freebie unspent) | ~~189\. Rotate Array 🟢~~ ✅ → **🟢 Streak 2** (+60 → Sep 24; three-reversals called before coding, clean from blank page, no hints; `k %= n` unprompted — the bug that breaks most attempts; complexity itemized clean, freebie unspent) + ~~1046\. Last Stone Weight 🟢~~ ✅ → **🟢 Streak 2** (+60 → Sep 24; max-heap-via-negation called before coding, clean from blank page, no hints; caught the empty-heap `[2,2]` case while coding that the plan didn't anticipate. Complexity right; sharpened "heap reconstructs" → *sift up/down along one root-to-leaf path*, plus heapify O(n) vs n-pushes O(n log n)) *(swapped in for 75 Bucket Sort — variant retired, see Warmup Notes; pulled fwd from Jul 27, was due Jul 23)* | ~~**SD ③ — Rate Limiter MASTERY**~~ ✅ → **🟡 — arc NOT closed.** 4 of 6 Transition targets cold (policy, why-Redis, TTL, `429`); **atomicity half** (right for `INCR`, wrong for token bucket — per-command ≠ across-commands); tradeoff not volunteered. **Two never-encoded pieces taught mid-mock: Redis `EVAL`/Lua, and LB routing strategies** — plus the degraded-mode fallback. **Scale step skipped entirely.** Full writeup in [`components/rate_limiter.md`](../../../../docs/foundations/system_design/components/rate_limiter.md). **Re-rep ~Aug 2–4** (§2a forgetting gap — measuring taught material inside a few days measures the conversation). *Lens fact-check deferred: doing it before the mock would have primed the exact questions Mastery measures.* |

---

## Warmup Notes

- **🔴 retries (1584, 127) get the ACTIVE block, not a warmup.** Both are Hard and survived two
  exposures — a 15-min warmup can't hold a full blank-page rebuild. 1584 Mon, 127 Tue. Per the
  procedure-first lesson: for 1584, open with the literal Prim loop on a 3-node example, not the proof.
- ⚠️ **Everything is coded from a blank page** (rule since Jul 14) — warmups included. No no-code
  blueprints as a default; it's an explicit opt-in that hard-caps at 🟡.
- **The two 4-🟡 stacks were spread.** 124/271/875/74 (all due Jul 23) split Thu; 743/355/143/912 (all
  due Jul 25) sit Sat. Never four boundary-arithmetic reps in one slot.
- **206 appears twice, deliberately:** Iterative (Mon, 🟢 streak-1, the motor-drill retention check)
  and Recursion (Fri, 🟡). The iterative form was drilled 5× on Jul 16 to make it the reflex fallback;
  Mon measures whether it stuck. Recursion variant still churns 🟡 — Fri is its own review.
- **75 Sort Colors — Bucket Sort variant retired Jul 26 (learner's call).** Dutch Flag is the
  recommended solve (one pass, O(1) space) and is already tracked 🟢 Streak 1, due Aug 16. Carrying
  Bucket Sort as a second variant doubled the rep cost for the weaker method — counting sort is two
  passes and doesn't generalize. Tracker row removed; the `sortColorsBucketSort` method stays in the
  file as history. **75 is not un-tracked** — the Dutch Flag row is the live one.
- **Recurring weakness still on watch:** window/pointer **boundary arithmetic** (424, 75, 567, 901).
  State the invariant aloud before running — "inclusive `[l,r]` → length `r-l+1`".

---

## Backlog Status ⚠️

**🟢 backlog policy — DECIDED Jul 19: re-baseline + sample.** The 23-item stale 🟢 pile (2–5 months
overdue; interval math no longer meaningful) is handled as: **(1)** a single **Friday coded spot-check**
of a sample chosen to span patterns — **238, 347, 15, 210** (Fri) + **3** (pulled fwd to Thu Jul 23) — rated normally; **(2)** the
remaining ~18 are **accepted as stale** and re-baselined to a fresh +30 the next time each is touched
(no mass tracker edit tonight — they simply stop being treated as "overdue"). Rationale: the week is
already at capacity on genuine due 🟡/🔴 + this week's 🟢 streak-1s; churning 23 stale spot-checks would
starve the real reviews.

**This week's genuine due load is protected:** all 🔴 (1584, 127, 787 lock-down, Redis) and all 14 🟡
are scheduled. Of the 🟢 streak-1s due this week, **2 slip to the Jul 27 week** (261 DFS, 1046) — 🟢 carries the most
slack and cleaning them only advances streak-1→2. *(200 DFS + 226 pulled forward to Mon Jul 20 as bonus
→ both 🟢 Streak 2. 543 also pulled forward Mon → slipped to 🟡, now due Jul 30.)*

**Phase-gated (not overdue):** 1216 Valid Palindrome III ×2 → Backtracking (Sep 14) / 1D DP (Oct 12).

---

## Spaced Rep Due This Week

| Problem | Comfort | Due | Slot |
|---------|---------|-----|------|
| 1584\. Min Cost Connect All Points (Prim) | 🔴 | Jul 20 | Mon active |
| ~~127\. Word Ladder (BFS)~~ | 🔴→🟡 | Jul 20 | ✅ Tue active → 🟡, due Jul 31 |
| 1448\. Count Good Nodes | 🟡 | Jul 20 | Mon |
| 206\. Reverse LL (Iterative) | 🟢 | Jul 20 | Mon |
| 21\. Merge Two Sorted Lists (Recursion) | 🟢 | Jul 20 | Mon |
| 130\. Surrounded Regions (BFS) | 🟢 | Jul 20 | Mon |
| ~~Redis (SD tech)~~ | 🔴→🟡 | Jul 21 | ✅ Tue SD → 🟡, due Jul 31 |
| 503\. Next Greater Element II | 🟡 | Jul 21 | Tue |
| 211\. Add and Search Words | 🟡→🔴 | Jul 23 | Thu (re-rep; Blank Jul 21, +2) |
| 229\. Majority Element II | 🟡 | Jul 22 | Fri |
| 567\. Permutation in String | 🟡 | Jul 22 | Fri |
| 242\. Valid Anagram | 🟢 | Jul 22 | Wed (pulled fwd from Fri — due Jul 22) |
| 124\. Binary Tree Max Path Sum | 🟡 | Jul 23 | Thu |
| 271\. Encode and Decode Strings | 🟡 | Jul 23 | Thu |
| 875\. Koko Eating Bananas | 🟡 | Jul 23 | Thu |
| 74\. Search a 2D Matrix | 🟡 | Jul 23 | → Sat (bumped for 211 🔴) |
| 261\. Graph Valid Tree (DFS) | 🟢 | Jul 23 | → Jul 27 (slipped) |
| ~~1046\. Last Stone Weight~~ | 🟢 | Jul 23 | ✅ un-slipped → **Sun Jul 26** (took 75's freed slot) |
| 206\. Reverse LL (Recursion) | 🟡 | Jul 24 | Fri |
| ~~53\. Maximum Subarray (Kadane)~~ | 🟢 | Jul 24 | ✅ Sun → **Streak 2**, +60 → due Sep 24 |
| ~~189\. Rotate Array~~ | 🟢 | Jul 24 | ✅ Sun → **Streak 2**, +60 → due Sep 24 |
| ~~75\. Sort Colors (Bucket Sort)~~ | 🟢 | Jul 24 | **RETIRED as a variant Jul 26** — row removed from tracker |
| ~~1046\. Last Stone Weight~~ | 🟢 | Jul 23 | ✅ Sun → **Streak 2**, +60 → due Sep 24 |
| 743\. Network Delay Time (Dijkstra) | 🟡 | Jul 25 | Sat |
| 355\. Design Twitter | 🟡 | Jul 25 | Sat |
| 143\. Reorder List | 🟡 | Jul 25 | Sat |
| 912\. Sort an Array (Merge Sort) | 🟡 | Jul 25 | Sat |
| ~~226\. Invert Binary Tree~~ | 🟢 | Jul 25 | ✅ done Mon Jul 20 (bonus) → Streak 2, Sep 18 |
| ~~787\. Cheapest Flights (Bellman-Ford)~~ | 🟢 prov.→**🟡** | Jul 26 | ✅ Sun — lock-down failed, +10 → **due Aug 5** (week of Aug 3) |
| ~~543\. Diameter of Binary Tree~~ | 🟢 | Jul 26 | ✅ done Mon Jul 20 (bonus) → 🟡, now due Jul 30 |
| ~~200\. Number of Islands (DFS)~~ | 🟢 | Jul 26 | ✅ done Mon Jul 20 (bonus) → Streak 2, Sep 18 |

---

## New Problems This Week

| Problem | Difficulty | Pattern | Day |
|---------|-----------|---------|-----|
| 332\. Reconstruct Itinerary | Hard | Eulerian path (Hierholzer) | Wed active |
| 778\. Swim in Rising Water | Hard | Dijkstra / binary-search-on-answer | Thu active |

**2 new** (Advanced Graphs, Hardest tier → 3/week pace, but 2 active blocks this week go to the 🔴
retries). **Alien Dictionary (269) → Jul 27 week** — the third of the remaining three. Phase runs
Jul 13–Aug 2; new intake finishes ~Jul 27 week. Watch the **Stack phase (Aug 3)** doesn't collide with
graphs still settling.

---

## System Design (3×/week — three-lane rule)

| Lane | Slot | This week |
|------|------|-----------|
| **① Tech fluency** | *light* midweek — swaps **one 15-min warmup** | **Redis re-sprint (RATED)** — due Jul 21 (+2 after Sun's 🔴). PostgreSQL new-note intake **yields** to the due 🔴 → **deferred to Jul 27**. |
| **② Blocks & probes** | *fuller* midweek — swaps **both warmups** (~30 min) | **Caching — Bootstrap** (Wed; pushed from Jul 19 now that Rate Limiter's blocker cleared). |
| **③ Designs** | **Sunday**, 45–60 min | ~~**Rate Limiter — MASTERY**~~ ✅ → **🟡, arc NOT closed.** Bootstrap ✅ Jul 5, Transition ✅ Jul 12, Mastery Jul 26 **🟡 → re-rep ~Aug 2–4**. **URL shortener** still opens the Jul 27 week (don't stack two design arcs — the Rate Limiter re-rep rides the Aug 3 week). |

**Redis re-sprint focus (Jul 19 was 🔴, ~4/11 cold):** atomicity is now solid; the misses were the
**request path** (card 7) and cards 8–11. Re-sprint targets those cold, then re-rate.

**⏩ MOVED to Sun Jul 26 (rides the Rate Limiter Mastery prep) — not reached Wed Jul 22.** Validate the **"7 question-lenses"** checklist
in [`framework.md`](https://github.com/michael-yrao/sd-progress/blob/main/framework.md) against Hello Interview's rate-limiter video/article —
any question their deep-dive raises that doesn't map to a lens is a real gap → add it. Feeds Sunday Jul 26
Rate Limiter Mastery (drill answers, not discover questions). *(The lens list is a synthesis, not a cited
framework — this pass is the fact-check.)*

---

## End-of-Week Targets

- [x] **Both graph 🔴s cleared** to at least 🟡 (1584 Prim ✅ Mon → 🟡; 127 Word Ladder ✅ Tue → 🟡)
- [ ] All 14 🟡 Shaky reviews done
- [~] **Rate Limiter reaches 🏁 Mastery** — **attempted Jul 26 → 🟡, arc NOT closed.** Real progress (4 of 6 Transition targets cold) but two never-encoded mechanisms surfaced. Re-rep ~Aug 2–4.
- [ ] Redis re-sprint cleared to at least 🟡; Caching Bootstrap done
- [ ] 2 new graph problems done (332, 778)
- [ ] 🟢 re-baseline: 5-sample spot-check done Fri; stale pile no longer treated as overdue

---

## Week of Jul 27 Preview

**Slipped 🟢 streak-1 (from this week):** 261 DFS — slot early, cleans to streak-2 (+60).
*(1046 no longer slipped — pulled forward to Sun Jul 26 into the slot freed by retiring the 75
Bucket Sort variant.)* **Note when slotting 261 DFS:** its Union-Find twin is due **Jul 28**, so
don't stack them — put DFS late in the week or let Union-Find go first. *(200 DFS + 226 done early Mon Jul 20 as bonus → both 🟢 Streak 2, next review Sep 18.
543 also done Mon → slipped to 🟡, now due Jul 30 — slot it that week.)*

**Spaced rep due Jul 27–Aug 2 (partial — audit when building):** 424 🟡, 540 🟡, 18 🟡 (Jul 27) · 169 🟢,
53 Prefix 🟢, 104 🟢, 704 🟢 (Jul 27) · 19 🟡, 261 UF 🟡 (Jul 28) · **332 🔴 Hierholzer re-rep (slot Jul 28 — rated; taught Jul 22)** *(tracker shows due Jul 24 = the +2 engine default, which the hook always recomputes and won't let us hand-edit; we intentionally do the rep ~4 days late for the §2a forgetting gap — Fri Jul 24's block is the 🟢 re-baseline. "Due Jul 24, done Jul 28" is a normal late rep, not a miss.)* · 417 🟡, 235 🟡 (Jul 29) · **912 Merge Sort — RATED re-rep (Jul 29; taught unrated Jul 25, §2a forgetting gap — measures whether the D&C structure stuck)** · **503 🟡, 127 🟡 (Jul 31)** — plus the 🔴/🟡
retries this week generates (1584, 127, 332, 778, Redis, Caching land on their new intervals). **Run a
full tracker audit at build time.**

**Method variants queued (two):** **778 Swim in Rising Water** gets two second-lens reps —
(a) **binary-search-on-answer + BFS/DFS reachability** (interleaves with the BS-on-answer work, Koko/Capacity),
and (b) **Union-Find / Kruskal** (sort cells ascending by elevation, union each into added neighbors; answer =
elevation at which (0,0) and (n-1,n-1) first connect — a distinct, high-ROI interview technique).
**Both gated:** slot them only once the base 778 (Dijkstra/min-heap) has logged ≥1 clean rep — master the base
approach first, then space the two variants out (don't stack both in one week — that's 3 methods for one Hard
problem). Learner promoted Kruskal from the Expansion Queue on Jul 23 (deemed important enough to schedule, not park).

**Active blocks:** Advanced Graphs **no longer finishes Aug 2 — phase extended to Aug 16, 11 problems**
(Jul 26: +1334 Floyd-Warshall, +721 Accounts Merge as new-technique; +1631, +1514 as consolidation reps).
**269 Alien Dictionary** still next, then the retry cascade for 1584/127/332/778/787.

**Phase-exit check is now per ALGORITHM, not "N of M 🏆 Retired"** (see study_guide → "Phase exit
standard"): each algorithm needs **recognition** (cold cue, label stripped) **+ execution** (≥1 problem
at 🟢). ⚠️ **Baseline as of Jul 26: zero of Advanced Graphs' own problems is at 🟢** — 332 🔴 and
743/778/787/1584/127 all 🟡. By the new standard the phase is nowhere near exit, which the old
per-problem count was hiding. Aug 16 is optimistic; expect to carry 2–3 algorithms into Stack, named
and slotted.

**System design:** stays 3×/week. ① **Redis re-rep due Jul 31** (🟡; drill cards 9 scale-out, 8 ZSet
"how", 11 footgun — **plus the new card 3b: `EVAL`/Lua and per-command-vs-across-commands atomicity**,
added Jul 26 from the Rate Limiter mock. PostgreSQL note **yields again** — a due 🟡 with fresh material
outranks new intake). ② **`components/load_balancer.md`** — the cold-hit block from Jul 26's mock
(round robin · least connections · IP-hash / consistent hashing · sticky sessions, and which of them
break a per-instance rate-limit fallback). This is exactly the pull-queue model working. ③ Sunday
**DESIGN: URL shortener** (small one — the *framework* is what's drilled).

**⚠️ Also owed, from the Jul 26 Rate Limiter mock:**
- **Rate Limiter Mastery re-rep, ~Aug 2–4** — §2a forgetting gap on `EVAL`/Lua + LB routing. Slot it
  in the Aug 3 week, *not* Jul 27.
- **The 7-lens fact-check vs Hello Interview's rate-limiter deep-dive** — still not done. Deliberately
  deferred Jul 26 (running it before the mock would have primed the questions being measured). Do it
  **after** the re-rep, or against a design that isn't imminent.
- **Scale-step drill.** The mock skipped Requirements → **Scale** entirely. Whatever the next design
  is, gate step 3 on the arithmetic being spoken aloud first. **Head start (bonus Jul 20):** [`framework.md`](https://github.com/michael-yrao/sd-progress/blob/main/framework.md) delivery-framework
reference built, and URL-shortener **steps 1–2 already done** (requirements: ~1k write / ~100k read QPS,
read-heavy + immutable → cache-first, replicas behind; core entity `URL`{shortCode, longUrl, createdAt,
ownerId, expiresAt}). **Sunday resumes at step 3 (API).** Coaching flag logged: learner defaults to LLD —
hold at HLD altitude through steps 1–4.

**🟢 re-baseline follow-through:** the ~18 stale 🟢 not sampled Friday get their fresh +30 as they're
next touched; don't re-list them as "overdue" in future audits.

**⏭ Beyond Jul 27 — carry into the week of Aug 3:** **787 Cheapest Flights 🟡 due Aug 5** (from Sunday's
failed lock-down). Slot it when that week is built — and per its stuck-log entry, the rep to watch for is
whether the **queue-free** shape comes back cold; the recognition was never the problem.
