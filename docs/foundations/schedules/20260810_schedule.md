# Week of August 10 – 16, 2026

**Goal:** Convert the Advanced Graphs algorithms that still have **zero 🟢** — Hierholzer, Floyd-Warshall,
Dijkstra, Bellman-Ford, Prim's. ⚠️ **Outcome as of Aug 14: 1 of 5 converted** (Prim's, via 1584 Tue). Dijkstra spent both chances and stayed 🟡; **332/Hierholzer became a teach rather than a rated rep** and moves to Tue Aug 18; 787/1334 remain on Sat. Every one of them has its rep this week (332 Fri · 1334 Sat · 743 Fri ·
787 Sat · 1584 Tue), plus the second Dijkstra (778 Wed). **All five chances land inside these seven days.**

> **📌 Policy, unchanged and worth re-reading before any date-driven urgency creeps in.** **Aug 16 is a
> checkpoint, not a deadline.** An unclosed phase **carries forward**, and that is a normal outcome.
> Report which algorithms have no 🟢 and let that drive the schedule — never frame the date as a countdown.
> A slipped phase does not raise the ceiling, does not license intake against a negative surplus, and does
> not make a protected rep movable. See [[feedback_phase_dates_are_advisory]].

---

## ⚖️ Capacity check — ⚠️ **the deficit closed**

```
110 rows · 0 🔴 · 19 🟡 · 85 🟢 · 6 🎓
demand  4.05 reps/day = 5.98 units/day  (41.8 units/week)
ceiling 9 units/day = 63 units/week      →  HEADROOM +21.2 units/week
overdue 13 rows = 11.0 units
```

**Last week opened at −9.6 reps/week. This week opens positive.** That is not drift — it is the direct,
measurable result of the Aug 3–9 conversion sweep:

| What happened Aug 3–9 | Effect on demand |
|---|---|
| **Both 🔴 cleared** (332, 1334 → 🟡) | 🔴 bills 0.50/day, 🟡 bills 0.10 — **−0.8/day** |
| **~21 🟢 s1 → s2** (interval 30d → 60d) | each halves its own billing — **−0.35/day** |
| **4 fast-tracks to 🎓** (110, 88, 100, 21) | 0.033 → 0.006/day each — **−0.11/day** |

> ⚠️ **Say this out loud when the backlog comes up:** the stale-🟢 pile was **arithmetic, not neglect**.
> Diligence could not drain it while demand exceeded capacity; only *maturation* could, and maturation is
> exactly what the sweep did. The same arithmetic now says the pile is drainable — which is why 9 of the
> 13 overdue rows are scheduled below rather than listed as slippage.

**⚠️ Headroom is not a mandate to fill.** Demand sets the **floor**; the ceiling stays a quality judgment.
A Medium 🟡 bills ~73 units/year against 🟢 s2's ~6.1, so a rep rushed into a 🟡 costs **12× forever** —
chasing throughput with a fuller board *increases* future demand. This week was planned at **8.0–8.5/day**
— **revised Aug 13 to 9.0/day** at the learner's request, filled entirely with **overdue 🟢 burn-down**
rather than new intake (see the totals below for why that distinction is the whole rule).

### ⚠️ An SD lane slot now costs 3.0 units, not 2.0 (raised Aug 9 by the learner)

> *"I think SD typically tire me out more, we can push them up closer to 9."*

**This adds no work — it corrects the price of work already scheduled.** At 2.0 an SD day measured ~7
while feeling like ~8+, so the budget was under-reporting exactly the days that ran hardest, and the
correction was being made in prose. That is the same failure the problem *count* had, one level down.

**The mechanical effect is the point:** an SD slot now consumes a third of the ceiling, so the scheduler
fits **fewer DSA problems alongside it**. SD days get lighter in DSA terms without anyone having to
remember to make them lighter.

**Calibration evidence:** Sun Aug 9 priced 8.5/9 and ended with the learner too tired to finish step 4 of
the design. At 3.0 that same day prices **9.5 — over ceiling**, which is the honest reading of what
happened. Changed in `cse.config.yml` → `effort_budget.sd_lane_units`.

### Per-day load

| | Mon 10 | Tue 11 | Wed 12 | Thu 13 | Fri 14 | Sat 15 | Sun 16 |
|---|---|---|---|---|---|---|---|
| **Units** | **7.0 actual** ⚠️ | **9.0** | **8.5** | **8.5** | **9.0** | **8.0** | **9.0** |
| DSA (review) | 3.0 done / 2.0 moved | 5.0 | 7.0 | 5.5 | 7.0 | 5.0 | 6.0 |
| **NEW / probe** | **probe #1** 1.0 ✅ | **150** 3.0 + **probe #2** 1.0 | **155** 1.5 | — | **739** 3.0 | **22** 3.0 † | **probe #3** 1.0 ‡ |
| SD | ① 3.0 ✅ | — | — | **rework** 3.0 | — | — † | — ‡ |
| **🔥 backfill** *(Aug 13)* | — | — | — | **680** 0.5 | — | **695** 1.0 | **323-UF** 1.0 + **2300** 1.0 |
| **Units — REVISED, script-priced** | 7.0 actual | 9.0 | 8.5 | **9.0** | **10.0** ⚠️ | **9.0** | **9.0** |

🔥 **Backfill to the ceiling, set by the learner Aug 13:** *"i want to go as close to 9 if possible, go
over if needed."* Freeing the SD lanes left Thu/Sat/Sun at 8.5 / 8.0 / 7.0, so **four overdue 🟢 come off
the slip list** — oldest-first, all pure burn-down, no new permanent demand. Slip list **10 → 6**.

⚠️ **Friday was mispriced in the original build and is 10.0, not 9.0.** `effort_budget.py --day 138 743
332 739` reads 2.0 + 2.0 + 3.0 + 3.0; **739 prices 3.0 as an untracked new problem, not the 2.0 the build
wrote** (the consolidation argument — Monotonic Stack already 🟢 — is a *prediction* about how it will go,
and the budget prices what is on the board). Friday carries **two protected reps** (743 is Dijkstra's last
🟢 chance this week, 332 is Hierholzer's only one) plus new intake, so it stands at 10.0 rather than
trimming a protected rep. **Nothing is added to Friday.**

† **Sat Aug 15 changed by the Aug 13 SD rework.** The Redis rated sprint is cancelled — technology rows
came off the review engine, so there is no row left to log it to. That frees 3.0 units, and by the
standing rule a day with no SD slot carries an unseen problem, so **22 Generate Parentheses** (phase
intake, was held for the Aug 17 week) takes the slot at the same price. Week intake goes 3 → 4 against a
moderate cap of 5. **Decline it and Saturday just runs at 5.0** — the two protected reps are what matter
there.

‡ **Sun Aug 16 — ⏸️ no SD at all this week, by the learner's call:** *"let's skip this week's mock
interview. I will be doing core concepts + key technology on HelloInterview and when I am ready, I will
be coming over to do the mock interviews."* Mock #1 is **not deferred to a date** — SD is in **study
mode** and the board carries no SD slot until they open one. The freed 3.0 goes back as headroom, not as
more problems: Sunday drops to **7.0**, and the unseen-problem rule is satisfied by **probe #3 (Easy,
1.0)**, which creates no permanent demand. Decline the probe and Sunday runs at 6.0.

> **📊 Mon Aug 10 closed at 7.0 of a planned 10.0 — and the shortfall is the interesting part.**
> **Delivered:** 977 probe 🟢 · 503 🟡→🟢 · Redis TEACH (4 cards) · BFS-counter TEACH · 66 🟢 s2 ·
> 703 🟢 s2. **Moved:** 98 · 80 → Sun Aug 16, probe #2 (202) → Tue Aug 11.
>
> **The 10.0 was never run, and the yield order worked exactly as pre-committed.** The learner's 10:30
> override put the 🟢 tail *behind* SD rather than trimming it up front, and when the day ran out at ~23:15
> it was the 🟢 tail that gave way — not the SD lane, not the protected teach. **Two of the four 🟢 still
> got done** (66, 703, both overdue), so the burn-down landed too.
>
> ⚠️ **Read this against build-agenda item #4.** The Aug 9 complaint was that DSA-first left SD as the
> thing that got cut. Today SD ran in full, both teaches landed, and the low-diagnostic reps absorbed the
> overrun. **That is the valve working**, and it is the first day this week it has been tested.

**Total 61.5 units** against a 63 ceiling *(revised Aug 13 — was 59.0)*. Two changes cancel most of each
other out: **−6.0** from cancelling the Sat SD sprint and Sun's mock, **+8.5** from the Aug 13 backfill
(22 Generate Parentheses, probe #3, and four overdue 🟢), plus **+1.0** correcting Friday's mispriced 739.

⚠️ **The learner asked for this explicitly** — *"i want to go as close to 9 if possible, go over if
needed"* — and it does **not** contradict the "headroom is not a mandate to fill" rule above. That rule
guards against rushing **new** reps into 🟡s that bill ~12× forever. The backfill is **overdue 🟢
burn-down**: demand that already exists on the board, at 0.5–1.0 units each, where the risk of a rushed
🟢 slipping to 🟡 is the lowest available anywhere. Filling with new intake instead would be the thing
the rule forbids.

⚠️ **Monday is now AT ceiling at 9.0, and probe #2 was started Monday night.** If 202 lands before bed
Monday prices **10.0 — over ceiling**. The board has not been touched yet at that point, so the honest fix
is to open Monday by **trimming a 🟢 warmup** (66 Plus One is the candidate — it is the third warmup and
already overdue, so it slips rather than disappears), *not* to run 10.0 and call it a good day. Per the
standing rule the 🟢 warmups yield; the SD ① Redis teach and the BFS-counter teach do not.

> ✅ **RECORDED OVERRIDE — Mon Aug 10, 10:30, by the learner.** *"I don't mind being over 9, but let's
> [put] the low priority ones behind SD."* **Nothing was trimmed; Monday runs at 10.0.** The trade was made
> the *other* way instead: rather than cutting a 🟢 to protect the ceiling, the **order** was changed so the
> 🟢 tail is what yields if the day runs out. Measured board: **8.0** (5 tracked problems + SD lane) **+ 1.0**
> probe #1 **+ 1.0** probe #2 = **10.0**.
>
> **This directly answers build-agenda item #4** (below): the Aug 9 complaint was that DSA-first left SD as
> the thing that got cut. Putting the 🟢 backlog *behind* SD is the structural version of that fix — the
> valve is now the low-diagnostic work, not the lane. **Run order:** 503 🟡 → ⚡ BFS-counter teach →
> SD ① Redis teach → probe #2 (202) → 🟢 tail (98 · 703 · 80 · 66).
>
> ⚠️ **Watch what over-ceiling buys.** 10.0 is fine as a decision; it is not fine as a habit — a rep rushed
> into a 🟡 bills ~12× forever, so the overrun pays for itself only if the 🟢 tail comes back clean. **If
> anything in that tail lands 🟡, note it here at the Aug 17 build** before repeating a 10.0 day.

**Note the shape:** the three non-SD days (Tue/Wed/Fri) run at **8.5–9.0** and each carries an unseen
problem; the SD days carry none. That is the standing rule below, not a coincidence of this week's
arithmetic.

⚠️ **Monday is now the exception, and it is a real one, not a rounding error.** Mon is an SD day and by the
standing rule should carry **no** unseen problem — it now carries one (probe #1), because the learner asked
for a probe late Sunday night and it was run then. **The rule is not broken:** its purpose is to guarantee
unseen work on days that would otherwise be all-review, not to *forbid* it on SD days. But the units are
real, which is why Monday is at ceiling above. **Do not read this as precedent** — if probes start landing
on SD days routinely, the 3.0 SD lane and the probe are competing for the same tired hour, and that is the
exact failure the Aug 9 reprice was meant to fix.

Verify any change with `python3 scripts/effort_budget.py --day <numbers> [--sd]` — do not hand-compute.
⚠️ The script counts a problem *number* once, so **323's two rows (DFS + Union-Find) price as one**; Tue's
true total is 8.0, not the 7.0 the script prints.

---

## Daily Schedule

**Status only — no commentary in this table.** Per-rep analysis lives in the mastery ledgers: [`stuck_log.md`](../dsa/mastery/stuck_log.md) · [`complexity_gotchas.md`](../dsa/mastery/complexity_gotchas.md) · [`recognition_gotchas.md`](../dsa/mastery/recognition_gotchas.md). Schedule-level decisions go in the sections below, never in a cell.

Done = struck through. Tags: ⚠️ protected · 🔥 backfill · 🆕 new intake · 🎯 probe · ⚙️ teach (unrated) · → moved.

| Problem | Start | End | Next | Note |
|---|---|---|---|---|
| ▸ **Mon Aug 10** · 7.0 units |  |  |  |  |
| ~~[503 Next Greater Element II](../../../dsa/leetcode/stack/503_next_greater_element_ii.py)~~ · [LC](https://leetcode.com/problems/next-greater-element-ii/) | 🟡 | 🟢 s1 | Sep 9 |  |
| ~~[66 Plus One](../../../dsa/leetcode/arrays_and_hash/66_plus_one.py)~~ · [LC](https://leetcode.com/problems/plus-one/) | 🟢 s1 | 🟢 s2 | Oct 9 | was overdue Jul 31 |
| ~~[703 Kth Largest in Stream](../../../dsa/leetcode/heap/703_kth_largest_element_in_stream.py)~~ · [LC](https://leetcode.com/problems/kth-largest-element-in-a-stream/) | 🟢 s1 | 🟢 s2 | Oct 9 | was overdue Aug 1 |
| ~~[977 Squares of a Sorted Array](../../../dsa/probes/977_squares_of_a_sorted_array.py)~~ · [LC](https://leetcode.com/problems/squares-of-a-sorted-array/) | 🆕 | 🟢 | — | 🎯 probe · no tracker row |
| [98 Validate BST](../../../dsa/leetcode/trees/98_validate_binary_search_tree.py) · [LC](https://leetcode.com/problems/validate-binary-search-tree/) | 🟢 s1 | — | — | moved to Sun 16 |
| [80 Remove Duplicates II](../../../dsa/leetcode/two_pointers/80_remove_dup_from_sorted_array_ii.py) · [LC](https://leetcode.com/problems/remove-duplicates-from-sorted-array-ii/) | 🟢 s1 | — | — | moved to Sun 16 |
| ~~**SD ① Redis teach**~~ | — | unrated | — | ⚙️ teach |
| ~~**BFS-counter teach**~~ | — | unrated | — | ⚙️ teach · measured Thu |
|  |  |  |  |  |
| ▸ **Tue Aug 11** · 9.0 units |  |  |  |  |
| ~~[1584 Min Cost to Connect Points](../../../dsa/leetcode/graphs/1584_min_cost_to_connect_all_points.py)~~ · [LC](https://leetcode.com/problems/min-cost-to-connect-all-points/) | 🟡 | 🟢 s1 | Sep 10 | ⚠️ protected · Prim's converted |
| ~~[875 Koko Eating Bananas](../../../dsa/leetcode/binary_search/875_koko_eating_bananas.py)~~ · [LC](https://leetcode.com/problems/koko-eating-bananas/) | 🟡 | 🟢 s1 | Sep 10 |  |
| ~~[323 Connected Components (DFS)](../../../dsa/leetcode/graphs/323_number_of_connected_components_in_an_undirected_graph.py)~~ · [NC](https://neetcode.io/problems/count-connected-components) | 🟢 s1 | 🟢 s2 | Oct 10 | complexity cap overridden by learner |
| ~~[202 Happy Number](../../../dsa/probes/202_happy_number.py)~~ · [LC](https://leetcode.com/problems/happy-number/) | 🆕 | 🟡 | Aug 21 | 🎯 probe · first probe to earn a row |
| ~~[150 Evaluate RPN](../../../dsa/leetcode/stack/150_evaluate_reverse_polish_notation.py)~~ · [LC](https://leetcode.com/problems/evaluate-reverse-polish-notation/) | 🆕 | 🟡 | Aug 21 | phase intake |
|  |  |  |  |  |
| ▸ **Wed Aug 12** · 8.5 units |  |  |  |  |
| ~~[211 Add and Search Words](../../../dsa/leetcode/trie/211_design_add_and_search_words_data_structure.py)~~ · [LC](https://leetcode.com/problems/design-add-and-search-words-data-structure/) | 🟡 | 🟡 | Aug 22 |  |
| ~~[778 Swim in Rising Water](../../../dsa/leetcode/graphs/778_swim_in_rising_water.py)~~ · [LC](https://leetcode.com/problems/swim-in-rising-water/) | 🟡 | 🟡 | Aug 22 | ⚠️ protected |
| ~~[271 Encode and Decode Strings](../../../dsa/leetcode/arrays_and_hash/271_encode_and_decode_string.py)~~ · [NC](https://neetcode.io/problems/string-encode-and-decode) | 🟡 | 🟡 | Aug 22 |  |
| ~~[155 Min Stack](../../../dsa/leetcode/stack/155_min_stack.py)~~ · [LC](https://leetcode.com/problems/min-stack/) | 🆕 | 🔴 | Aug 14 | phase intake |
|  |  |  |  |  |
| ▸ **Thu Aug 13** · 9.0 units |  |  |  |  |
| ~~[121 Best Time to Buy/Sell](../../../dsa/leetcode/greedy/121_best_time_to_buy_and_sell_stock.py)~~ · [LC](https://leetcode.com/problems/best-time-to-buy-and-sell-stock/) | 🟢 s1 | 🟢 s2 | Oct 12 |  |
| ~~[901 Online Stock Span](../../../dsa/leetcode/stack/901_online_stock_span.py)~~ · [LC](https://leetcode.com/problems/online-stock-span/) | 🟡 | 🟡 | Aug 23 |  |
| ~~[680 Valid Palindrome II](../../../dsa/leetcode/two_pointers/680_valid_palindrome_2.py)~~ · [LC](https://leetcode.com/problems/valid-palindrome-ii/) | 🟢 s1 | 🟢 s2 | Oct 12 | 🔥 backfill · 32-day slip cleared |
| ~~[138 Copy List w/ Random Pointer](../../../dsa/leetcode/linked_list/138_copy_list_with_random_pointer.py)~~ · [LC](https://leetcode.com/problems/copy-list-with-random-pointer/) | 🟡 | 🟢 s1 | Sep 12 | pulled forward from Fri |
| ~~[127 Word Ladder](../../../dsa/leetcode/graphs/127_word_ladder.py)~~ · [LC](https://leetcode.com/problems/word-ladder/) | 🟡 | 🟡 | Aug 23 | measured Mon's teach — held |
| ~~[146 LRU Cache](../../../dsa/leetcode/linked_list/146_lru_cache.py)~~ · [LC](https://leetcode.com/problems/lru-cache/) | 🟢 s1 | 🟢 s2 | Oct 12 |  |
| ~~**SD track rework**~~ | — | unrated | — | ⚙️ |
|  |  |  |  |  |
| ▸ **Fri Aug 14** · 10.0 units |  |  |  |  |
| ~~[503 Next Greater Element II](../../../dsa/leetcode/stack/503_next_greater_element_ii.py)~~ · [LC](https://leetcode.com/problems/next-greater-element-ii/) | 🟢 s1 | 🟢 s2 | Oct 13 | unscheduled pull, 26 days early |
| ~~[743 Network Delay Time](../../../dsa/leetcode/graphs/743_network_delay_time.py)~~ · [LC](https://leetcode.com/problems/network-delay-time/) | 🟡 | 🟡 | Aug 24 | ⚠️ protected |
| ~~[332 Reconstruct Itinerary](../../../dsa/leetcode/graphs/332_reconstruct_itinerary.py)~~ · [LC](https://leetcode.com/problems/reconstruct-itinerary/) | 🟡 | ⚙️ unrated | Aug 18 | converted to a teach |
| ~~[739 Daily Temperatures](../../../dsa/leetcode/stack/739_daily_temperatures.py)~~ · [LC](https://leetcode.com/problems/daily-temperatures/) | 🆕 | 🟡 | Aug 24 | phase intake |
| ~~[155 Min Stack](../../../dsa/leetcode/stack/155_min_stack.py)~~ · [LC](https://leetcode.com/problems/min-stack/) | 🔴 | 🟢 prov | Aug 24 | +2 measured Wed's teach |
|  |  |  |  |  |
| ▸ **Sat Aug 15** · 9.0 units |  |  |  |  |
| ~~[572 Subtree of Another Tree](../../../dsa/leetcode/trees/572_subtree_of_another_tree.py)~~ · [LC](https://leetcode.com/problems/subtree-of-another-tree/) | 🟢 s1 | 🟡 | Aug 25 |  |
| ~~[787 Cheapest Flights Within K Stops](../../../dsa/leetcode/graphs/787_cheapest_flights_within_k_stops.py)~~ · [LC](https://leetcode.com/problems/cheapest-flights-within-k-stops/) | 🟡 | 🟢 s1 | Sep 14 | ⚠️ protected · Bellman-Ford converted |
| ~~[1334 Find the City](../../../dsa/leetcode/graphs/1334_find_the_city_with_the_smallest_number_of_neighbors_at_a_threshold_distance.py)~~ · [LC](https://leetcode.com/problems/find-the-city-with-the-smallest-number-of-neighbors-at-a-threshold-distance/) | 🟡 | 🟡 | Aug 25 | ⚠️ protected |
| ~~[853 Car Fleet](../../../dsa/leetcode/stack/853_car_fleet.py)~~ · [LC](https://leetcode.com/problems/car-fleet/) | 🆕 | ⚙️ unrated | Aug 17 | teach · took 22's slot |
| ~~[695 Max Area of Island](../../../dsa/leetcode/graphs/695_max_area_of_island.py)~~ · [LC](https://leetcode.com/problems/max-area-of-island/) | 🟢 s1 | 🟢 s2 | Oct 14 | 🔥 backfill · 29-day slip cleared |
| **22 Generate Parentheses** · [LC](https://leetcode.com/problems/generate-parentheses/) | 🆕 | — | Sep 14 phase | moved to Backtracking |
|  |  |  |  |  |
| ▸ **Sun Aug 16** · 8.0 units |  |  |  |  |
| ~~[496 Next Greater Element I](../../../dsa/leetcode/stack/496_next_greater_element_i.py)~~ · [LC](https://leetcode.com/problems/next-greater-element-i/) | 🟡 | 🟢 s1 | Sep 15 | ⭐ carried 739's *store the index* forward, pre-code |
| ~~[208 Implement Trie](../../../dsa/leetcode/trie/208_implement_trie_prefix_tree.py)~~ · [LC](https://leetcode.com/problems/implement-trie-prefix-tree/) | 🟢 s1 | 🟢 s2 | Oct 15 | ⭐ rejected the stock O(N·L); freebie refunded on review |
| ~~[261 Graph Valid Tree](../../../dsa/leetcode/graphs/261_graph_valid_tree.py)~~ · [NC](https://neetcode.io/problems/valid-tree) | 🟡 | 🟢 s1 | Sep 15 | ⭐ cleared the Aug 11 complexity debt, fired cold |
| [98 Validate BST](../../../dsa/leetcode/trees/98_validate_binary_search_tree.py) · [LC](https://leetcode.com/problems/validate-binary-search-tree/) | 🟢 s1 |  |  | ⚠️ re-scaffold before attempting |
| [80 Remove Duplicates II](../../../dsa/leetcode/two_pointers/80_remove_dup_from_sorted_array_ii.py) · [LC](https://leetcode.com/problems/remove-duplicates-from-sorted-array-ii/) | 🟢 s1 |  |  | ⚠️ re-scaffold before attempting |
| [75 Sort Colors](../../../dsa/leetcode/arrays_and_hash/75_sort_colors.py) · [LC](https://leetcode.com/problems/sort-colors/) |  | 🟢 s1 |  |  | ⚠️ **was MISSING from this board** — engine dates it to today |
| ~~**🎯 Probe #3 — 69 Sqrt(x)**~~ · [file](../../../dsa/probes/69_sqrtx.py) · [LC](https://leetcode.com/problems/sqrtx/) | 🆕 | 🟢 | — | recognition hit **cold** · no array to pattern-match · **no row** (disposable) |

## 🎯 NEW THIS WEEK — the recognition probe (Tue Aug 11)

**One unseen LeetCode problem, label stripped, solved cold.** Adopted by the learner Aug 9. Full design in
`.claude/memory/project_recognition_probes.md`; operating rules in [`dsa/probes/README.md`](../../../dsa/probes/README.md).

**Why it exists:** `dsa_progress.md` measures *execution* well and *recognition* barely at all, because
**a retry names its own technique** — the tracker row says "(Dijkstra)" and the file sits in
`advanced_graphs/`. Recognition evidence is supposed to come from new problems and cold cues, and this
board is ~100% retries. The axis the learner most wants to train is the one nothing currently tests.

| | |
|---|---|
| **Pull from** | a technique already **🟢/🎓** in `technique_coverage.md`, **Medium or below** |
| **Eligible pool today** | Binary Search 🎓 · Tree DFS 🎓 · Grid BFS 🎓 · Linked List Merge 🎓 · Sliding Window · Union-Find · Heap · Monotonic Stack · Topological Sort · Hash Set Membership |
| **NOT eligible** | Dijkstra, Bellman-Ford, Floyd-Warshall, Hierholzer, Prim's, BFS-on-implicit-graph — all 🟡 with no 🟢. Those need **conversion reps**, and a probe there just manufactures a 🟡 row at ~73 units/year |
| **Scaffold to** | `dsa/probes/` — **never** `dsa/leetcode/<pattern>/`, whose folder name *is* the answer |
| **Logging** | recognition call → always. 🟢 → **no tracker row** (disposable rep). 🟡/🔴 → earns a row |

### Probe #1 specifics

| | |
|---|---|
| **Day** | **Tue Aug 11** — a non-SD day, per the standing rule. It was originally on Sunday; Sunday has SD ③ and carries none |
| **Difficulty** | **Easy** — deliberately, for the first run. Prices at **1.0**, and the thing being measured is the **pre-code recognition call**, not the coding grind. A cheap trial tells you whether the format works before spending Medium-sized units on it |
| **Steady-state price** | **2.0** — an unseen Medium behaves like a 🟡, not a 🟢. Do not price future probes at 1.0 unless they are Easy |
| **Bought by** | dropping **323 Connected Components (Union-Find)** to the slip list. 323-DFS still runs that day, so the technique is still touched |

⚠️ **The problem is deliberately NOT named here.** Writing it into the schedule lets it be looked up in
advance, which destroys the only thing the probe measures. **Choose it at scaffold time**, from a 🎓
technique, and scaffold it to `dsa/probes/` — never `dsa/leetcode/<pattern>/`, whose folder name is the
answer.

**⚠️ The surplus exception, recorded rather than assumed.** The fill table says *"surplus ≤ 0 → no
consolidation reps."* That gate exists to stop **permanent** demand growth, and a disposable probe adds
one slot once and **zero forever after**. It is also moot this week — the deficit closed and there are
21 units of headroom. **Restate this reasoning at each build**; if the probe's row-creation rate runs
high (lots of 🟡), the exception has stopped being cheap and must be revisited.

---

## 📥 New-problem intake: 3 this week — ⚠️ **and an active phase had been running empty**

**Learner, Aug 9:** *"let's try to have new problems on tues, weds and fri where there are no SD."*

Acting on it surfaced something worse than the freeze itself. **The `Sliding Window (finish) + Stack`
phase opened Aug 3 and has ZERO problems in the tracker** — all eight (239, 155, 150, 22, 739, 853, 84,
76) untouched, a week into a three-week phase that closes **Aug 23**. Nothing in the repo flags an
*active phase with no reps*, so it stayed invisible while the board looked full of legitimate work.

**Intake this week — one per non-SD day, cheapest and most-connected first:**

| Day | Problem | Units | Why this one |
|---|---|---|---|
| **Tue** | **150 Evaluate Reverse Polish Notation** | 3.0 | Stack-eval; nearest neighbour to 20 (🟢) |
| **Wed** | **155 Min Stack** | 1.5 | Easy, and the cheapest entry into the phase — a *design* problem, so it exercises a different muscle than the traversal reps around it |
| **Fri** | **739 Daily Temperatures** | 2.0 | **Monotonic Stack is already 🟢** (496, 503, 901), so this is a consolidation rep, not a blank-tax rep — priced 🟡 not 🔴 |

That is **4 of 8** phase problems scheduled across this week and next, which fits the moderate-category
intake cap. **Left for the Aug 17 week:** 22 Generate Parentheses · 853 Car Fleet · 239 Sliding Window
Maximum (Hard) · 84 Largest Rectangle (Hard). *(76 Min Window Substring stays parked — it's in
`discovery_skip`.)*

### 📌 STANDING RULE (set by the learner Aug 9): an unseen problem on every non-SD day

> *"I want to always have new or probe problems on days that have no system design. problems that I have
> seen more than twice will always be a bit more lenient on me."*

**Every day without an SD lane slot carries at least one unseen problem** — new phase intake *or* a
recognition probe, interchangeable. At `three_weekly` that is **4 unseen problems a week**. SD days carry
none; the lane at 3.0 units is already doing the hard work.

**Why, in the learner's terms:** a problem seen 3+ times is a *lenient* test — the rep measures retention
of that problem's solution rather than the technique, and the tracker row even names the method. It still
buys decay insurance, but its **diagnostic** value has collapsed. Unseen problems are the only thing that
tests recognition and transfer.

**Build order:** mark SD days → place an unseen problem on every remaining day → *then* due reviews →
*then* 🟢 backlog. ⚠️ **Intake caps still bind** (moderate 5 / hard 4 / DP 3 per week); **the probe is the
release valve**, since it creates no tracker row on 🟢 and therefore never fights the cap. Full rule:
`.claude/memory/feedback_unseen_on_non_sd_days.md`.

### ⚠️ The trade, stated plainly

**New problems and the 🟢 backlog burn-down compete for the same units.** Buying 6.5 units of intake means
**5 overdue 🟢 go back to the slip list** — 680, 14, 695, 684, 75. The slip list grows 4 → 9.

I think that is the right trade and it is worth saying why: a 🟢 that sat three months came back **clean**
(all 8 of the Apr–May block, Aug 5–7), so the backlog is **not decaying on the timeline the tracker
implies**. Phase progress *is* time-boxed — Aug 23 is real work that doesn't get done later for free.
**Deferrable vs. not-deferrable is the axis, not old vs. new.**

### ⏳ Still held: 1631 · 1514 — trigger `green:Dijkstra`

Both are **Dijkstra consolidation reps** (cost-as-max; multiply-and-maximize). **Dijkstra has three
problems and zero 🟢.** Consolidation deepens a technique; it cannot substitute for proving one, so
scheduling these in the same week as the reps meant to produce the first 🟢 inverts the order.
**Chances to fire it: 778 Wed, 743 Fri.**

⚠️ **This replaces a date-shaped deferral with a state-shaped one, and that is the lesson.** The previous
hold read *"surplus is −9.6"* — which **expires silently the moment the number moves**, exactly like a bare
date. Nothing watches it, and the item keeps not being scheduled while its stated reason is no longer true.
**Evaluate at the Aug 17 build:** fired ⟹ slot both, or re-defer *with a written reason*.

---


## ⚠️ Protected reps — do not move, do not rush

**Five algorithms have zero 🟢, and every one gets its only chance this week.**

| Rep | Day | Algorithm | Why protected |
|---|---|---|---|
| ✅ ~~**1584**~~ | Tue | **Prim's** | ~~its *only* problem; 0×🟢~~ → **🟢 Clean Aug 11. Prim's now has its 🟢** — 1 of 5 down, 4 to go (778 Wed · 743/332 Fri · 787/1334 Sat) |
| ⚠️ ~~**778**~~ | Wed | **Dijkstra** | ~~Dijkstra has 3 problems and 0×🟢~~ → **🟡 Shaky Aug 12. Dijkstra STILL has zero 🟢.** Chance 1 of 2 spent; **743 Fri is the last one this week.** The hold on 1631/1514 (trigger `green:Dijkstra`) has **not** fired |
| **743** | Fri | **Dijkstra** | the second chance; recognized cold last time, lost on aggregation |
| ⚙️ ~~**332**~~ | ~~Fri~~ → **Tue Aug 18** | **Hierholzer** | ~~its only problem; 0×🟢~~ → **converted to a TEACH (unrated) at 02:00 Aug 14.** Recognition hit; execution absent; taught rather than re-blanked. **Hierholzer still has zero 🟢** and this was its only chance this week — the conversion carries into the Aug 17 week. ⚠️ **The rated rep is protected AND gap-protected**: do not pull it into Sat/Sun |
| **787** | Sat | **Bellman-Ford** | its only problem; 0×🟢 |
| **1334** | Sat | **Floyd-Warshall** | its only problem; 0×🟢 |
| **127** | Thu | — | **measures Monday's teach.** A 3-day gap on purpose |

⚠️ **Fri and Sat each carry two protected reps.** That is unavoidable — the dates come from the interval
engine, not from preference. If a day overruns, **the 🟢 warmups yield, never the protected rep.**

⚠️ **None is eligible for a 🟢 lock-down shortcut.** A 🟢 directly after a 🔴 is **provisional (Streak 0,
+10)**. These are all 🟡→ reps, so a 🟢 is a normal Streak-1 → +30.

---

## Warmup Notes

- **Everything is coded from a blank page**, warmups included. A no-code blueprint is an explicit opt-in
  that hard-caps at 🟡.
- **⚡ Mon Aug 10 · 15 min · TEACH: counter semantics in layered BFS.** Decided Aug 3 after 127's *second
  consecutive identical* off-by-one. Two decisions only: **(1) increment per layer drained, never per node
  popped; (2) start at 0 or 1 depending on whether the answer counts nodes or the steps between them.**
  Measured by 127 on **Thu Aug 13** — the gap is deliberate. Write it into `recognition_gotchas.md`
  **at the teach**, not before. The lesson is *not* BFS: 994 was flawless the same day.
- **271 (Wed)** — ✅ **RESOLVED Aug 12: the edge-set teach RAN, after 271.** Two decisions derived on
  throwaway graphs, 269 never opened; written into `recognition_gotchas.md`. **Measured by 269 on Aug 17
  — a 5-day gap.** ~~269's edge-set bookkeeping teach was flagged Aug 8 and is NOT slotted.~~ Two
  consecutive reps lost to edge-set construction fired the 540/19 trigger. 269 itself is not due until
  Aug 17. **Decide at this build whether the teach rides along with 271 Wednesday or waits for 269.**
- **323 (Tue)** — both the DFS and Union-Find rows land this week and they are the *same file*. UF is the
  one to protect: it's the coldest and it's the only one attached to a live gap (721's modeling side).
- **496 / 503 (Sun / Mon)** — both Monotonic Stack, both 🟡 from Aug 6. Deliberately split across the week.

---

## Backlog Status

**6 overdue 🟢 slip** — ⚠️ **revised down from 10 on Aug 13**, when the freed SD lanes were backfilled to
the ceiling. Down from **25 two weeks ago**.

*Scheduled off the slip list Aug 13 (🔥 backfill, oldest-first):* ✅ ~~**680** Valid Palindrome II (Jul 12)~~ →
**Thu — DONE 🟢, 32-day slip cleared** · **695** Max Area of Island (Jul 17) → Sat · **2300** Successful Pairs (Jul 19) → Sun ·
**323** Connected Components — Union-Find (Jul 29) → Sun.

⚠️ **Returned to the slip list Aug 16** — Sunday priced 9.0 against the 8.0 ceiling once the two misses below were corrected, so both 🔥 backfills yielded rather than a due-today review. **They are the two oldest items on the list and take the first slots at the Aug 17 build, oldest-first:** **2300** Successful Pairs (Jul 19) · **323** Connected Components — Union-Find (Jul 29).

*Still slipping, oldest first:* 14 Longest Common Prefix (Jul 15) · 684 Redundant Connection (Jul 31) ·
19 Remove Nth Node — Iterative (Aug 8) · 36 Valid Sudoku (Aug 8) · 33 Search in Rotated Sorted Array
(Aug 3) · 75 Sort Colors (Aug 9).

**Total 5.5 units** across 6 rows. They re-enter at the Aug 17 build in overdue order — and with SD
carrying no slots, that build has ~9 units/week of room they were competing for.

⚠️ **This is a deliberate trade, not slippage.** A 🟢 untouched for three months came back **clean** — all
8 of the Apr–May block, Aug 5–7 — so the backlog is **not decaying on the timeline the tracker implies**.
Phase progress *is* time-boxed: Aug 23 does not extend, and an unstarted phase problem is not work that
gets done later for free. **The axis is deferrable vs. not-deferrable, not old vs. new.**

⚠️ **Nothing is 3+ months overdue any more.** The Apr–May block was cleared Aug 5–7, all 8 came back 🟢,
and the staleness hypothesis was **not** confirmed — evidence for the fast-track, carried into the sweep
agenda below.

---

## System Design — ⚙️ **the track was rebuilt Aug 13, 2026**

> **SD is now mock interviews.** The learner studies system design independently on
> [HelloInterview](https://www.hellointerview.com/learn/system-design/in-a-hurry/introduction); the coach
> runs cold mocks on HelloInterview's 35-question board and scores them on the 7-point rubric. **Lanes
> ①/②/③ are gone**, along with note-building reps, Recall-Card blind sprints, the
> Bootstrap→Transition→Mastery arc, the designs-pull-blocks queue and the prereq-tech gate.
>
> **Why:** the repo was trying to be a textbook *and* a scoreboard, and after ~6 weeks of lanes the
> textbook half stood at 2 of 9 technology notes, 0 of 8 patterns, 2 of 9 core concepts. Writing a
> system-design textbook is open-ended work with no exit condition and it was competing for the slots
> that produce the thing being graded. HelloInterview is a finished textbook.
>
> Mechanics → [`system_design/study_guide.md`](../../../docs/foundations/system_design/study_guide.md) · sequence, phases and
> the rubric → [`senior_ramp.md`](../../../https://github.com/michael-yrao/sd-progress/blob/main/senior_ramp.md) · board →
> [`design_progress.md`](../../../docs/foundations/system_design/mastery/design_progress.md).

**New cadence: `mock_plus_deep_dive`.** Sunday = one cold mock (3.0 units). One midweek = a ~25-min
deep-dive round on an already-mocked design, drawn from its debrief's open probes (2.0 units) —
**conditional, and it does not exist until something is banked.** This week nothing is banked, so there
is no midweek SD slot from Aug 17 either; it starts once Bitly has a debrief.

### This week's slots, as they actually landed

| Slot | Item |
|---|---|
| **Mon Aug 10** | ✅ **DONE — Redis TEACH, UNRATED (old model).** All four cards cleared. Derive-the-design throughout; write-ups inline in the card, plus a new `❓ Open — not yet asked` bank of 10 questions for the mock. ⚠️ **Watch on Aug 15:** SPOF was named only when asked directly — beat 1 has still never been *volunteered* first; and timeout/circuit-breaker is **new** material, so Aug 15 measures 5 days of retention on it, not a re-test. ~~The four clauses that missed three sprints running:~~ SPOF naming before mitigations · *the score is the mechanism* (ZSet: points vs timestamp, `ZREMRANGEBYSCORE`) · scale **out** ≠ scale **up**, and the cap is on **cores, not RAM** · the `volatile-*` immortal-key footgun |
| **Thu Aug 13** | ⚙️ **The rework itself** (unrated). ~~Networking TCP re-teach~~ — rolled back to untaught Aug 9 at the learner's request, and now their own study on [HelloInterview's Networking Essentials](https://www.hellointerview.com/learn/system-design/core-concepts/networking-essentials). The [local card](../../../https://github.com/michael-yrao/sd-progress/blob/main/concepts/networking_basics.md) stays as reference |
| **Sat Aug 15** | ~~Redis MEASURE — RATED~~ **cancelled.** Technology rows came off the review engine, so the sprint has no row to log to and would be measuring an instrument the repo no longer keeps. The Aug 10 teach is not wasted — it is Redis knowledge, and it will be probed inside a mock that reaches for a cache. 3.0 units freed |
| **Sun Aug 16** | ⏸️ **nothing — SD is in study mode.** ~~Mock #1 — Bitly~~ |

### ⏸️ Study mode — what this state is, and what clears it

> *"let's skip this week's mock interview. I will be doing core concepts + key technology on
> HelloInterview and when I am ready, I will be coming over to do the mock interviews. So I will do
> another restructure session to lockdown the core concepts + key technologies in a way where we don't
> go too deep and make sure they are where they need to be for a system design interview."*

- **No SD slot goes on the board** — not a mock, not a placeholder — until the learner opens one.
- **The next SD session is a restructure session**, scoping Core Concepts + Key Technologies to
  interview depth and no deeper. **They call it.** Do not schedule it and do not ask at each build
  whether they're ready; that converts a learner-owned gate into a coach-owned one.
- **Nothing else waits on this.** DSA runs at full capacity and every non-SD day carries an unseen
  problem — which, at zero SD days, is now every day.

**When it clears, Bitly is still mock #1**: HelloInterview's Easy #1, the same system as the in-flight
URL shortener, and running it cold closes an arc already at an unratable third sitting. **The learner's
own derivation below carries over and caps nothing** — only reading the worked breakdown would.

**The Rate Limiter row was emptied to 🔴 with no history** at the learner's request, in the same pass.
Its three July reps were staged sessions against a different rubric on a different source; carrying the
🟡 forward would have priced a re-mock against a rating no mock produced. The arc content stays in
[`components/rate_limiter.md`](../../../https://github.com/michael-yrao/sd-progress/blob/main/components/rate_limiter.md).

### URL shortener — what was already derived (Aug 9), and why it doesn't spoil Sunday

**This is the learner's own work, not a worked answer**, so it caps nothing:

1. **Scale reconciled** — 100 write / 10k read QPS. The method is the keeper: three quantities were
   asserted (read rate, write rate, ratio) but **only two are free**; the ratio was the actual requirement
   so it was held, and one rate was **derived from a premise** (100M DAU × ~10% creating a link/day)
   instead of picked.
2. **Short-code generation** — 6 characters (`62⁶` = 56.8B supply vs 18B demand, ~3×). Uniqueness from
   **one global counter** with **block allocation**; **bijective scramble** before encoding so codes
   aren't enumerable. Snowflake was derived and then rejected on length (~11 chars) — *coordination-freedom
   costs bits, and the budget is 6 characters.*

⏳ **Step 4 was started and stopped.** The learner hit the floor on HLD diagramming (*"I don't know how to
draw this at all"*, then *"I do not recall anymore"*) and the coach began supplying boxes.
**Correctly called as a teaching session and stopped rather than pushed.**

⚠️ **Two things for Sunday:**
- **Run it as a full mock from step 1, not as a resumption of step 4.** Re-deriving requirements and the
  short-code scheme costs ten minutes and is exactly the framework rep Phase A exists for. The earlier
  work will come back fast or it won't — either answer is information.
- **HLD diagramming is a named gap now, not an assumption.** Open with the notation itself — a box is
  something that can fail independently, an arrow is a request from caller to callee, draw one path at a
  time left to right — *before* asking for any design output. Tooling: Excalidraw for the whiteboard
  motion, Mermaid in the note for the durable copy.

---

## 🧾 Build agenda — items raised Aug 9, all needing a decision

**1. ⏳ Recursion space/time for trees and graphs — PINNED by the learner.** *"I want to revisit… when I
have time."* **Trigger: first warmup slot on a day pricing ≥1.0 unit under the ceiling** (~15 min, a
concept refresher, not a rep). **This is the most repeated family in the complexity ledger** — 206, 104,
778, 105 — and the **tree-height `O(h)` item from 235/100 is the same root cause.** ⚠️ **Teach them
together, as one session, not two.** Scope: stack depth vs *retained* structures held alive across a
recursive call · why `n + n/2 + n/4` needs a **guaranteed** balanced split and a tree's split is
input-dependent · `O(h)` vs `O(log n)` · graph `O(V+E)` and where the `V` gets dropped.

**2. ⚠️⚠️⚠️ UPGRADED Aug 11 — the freebie hole now has a SECOND category behind it, and it forced a
live override.** Graph-traversal time has missed **4 times across 3 problems** (210 Jul 24 `O(V·E)` ·
323-BFS Aug 5 `O(E)` · 133 Aug 9 `O(n)` · 323-DFS Aug 11 `O(V·E)`) and capped **once** — only because it
happened to recur on the same problem. The Jul 24 miss was the *identical* `O(V·E)` error and cost nothing.
**On Aug 11 the learner overrode the resulting 🟡** on the correct grounds that the category, not the
problem, is the unit and it gets three cold retests in twelve days. **A category freebie would have capped
Aug 5 and there would have been no override to make.** Two independent categories now demonstrate the hole;
option (b) below is no longer a hypothetical. Full write-up + the override record in `complexity_gotchas.md`.

**2a. ⚠️⚠️ The per-problem complexity freebie cannot catch a category that hops problems.** Fixed-alphabet
has now missed **five times across five different problems** (242, 567, 424, 269×3, 621) and the 🟡 cap
fired **once** — only on 269, the sole problem where it recurred *on itself*. **The most repeated miss in
the ledger is structurally invisible to the mechanism meant to enforce it**, because a gap recurring
across problems is a *missing transfer*, not decay. **Do not change rating semantics unilaterally.**
Options to put up as a menu: (a) leave as-is, rely on the cue table; (b) add a **category** freebie so the
3rd occurrence of a family caps regardless of problem; (c) fire the category cue **proactively** whenever
a problem's constraints name a bounded alphabet. Written up in `complexity_gotchas.md`.

**3. Two vocabulary findings, both spotted by the learner** — and both are the recognition instinct working:
- **721 is not a pure Union-Find problem.** LeetCode tags it UF *and* DFS *and* BFS; the engine is
  interchangeable and the real content is **equivalence modeling**. Grouping it with 130/261/684 — where
  UF *is* the answer — overstates UF coverage.
- **"Graph Clone (DFS + Hash Map)" is over-specified.** The learner solved 133 with **BFS** (first time;
  prior three were DFS) and the load-bearing idea — *the map is clone identity, edge-recording is a
  separate concern* — is traversal-agnostic.
**Both are `techniques.yml` edits. Decide the rename/split at this build.**

**4. The DSA-first ordering inverted the overrun valve on day one.** DSA-first was adopted Aug 9 precisely
so SD would get an unhurried tail — and SD is what got cut. The yield offer was made at kickoff (8.5/9)
and declined, which is the learner's call and correctly recorded. **Data, not a verdict:** on a near-ceiling
day the yield has to be a real pre-commitment or the valve doesn't exist. This week is planned at 7.0–8.0
rather than 8.5–9.0 for exactly this reason.

**5. 🏆 Traversal fast-track sweep — still not run.** Four accepted (110, 88, 100, 21) against one declined
(323-BFS). Graph/grid BFS+DFS carry 12 rows, tree BFS+DFS carry 10; of those 22 only 127 is non-🟢.
**Bring it as a menu, not a backlog** — a decline is a valid outcome. ⚠️ **Concentration check:** 110 and
100 both lean on 543 and 105 as their harder representatives, and both of those are 🟢. If they graduate
too, tree-DFS loses its live test.

**6. Two technique notes still unwritten and both are gate-relevant.** `bellman_ford.md` does not exist,
and the `dijkstra.md` staggered write plan set Jul 31 never ran. Of the five no-🟢 algorithms only
Floyd-Warshall and Prim's have notes. **A phase gate judged per algorithm should not close with two of
them undocumented.**

**7. Redis coverage gap #2 is NOT slotted.** The note covers Redis-through-the-rate-limiter only —
missing cache-aside/write-through · stampede · invalidation · Cluster hash slots · Redlock · Pub/Sub &
Streams · data types beyond ZSet. **Needs ≥2 slots.** Recorded so it isn't lost.

**8. ⚠️ Learner challenged the VALUE of 150, not its difficulty** (Aug 11): *"I don't understand the point of
this problem, I don't see anything of value here."* **They are substantially right about the algorithm** —
push, pop two, push back; there is no insight to find, and they had it within a minute. The defence offered
was (a) it is the base rung of the expression-evaluator family, with **224/227/772 Basic Calculator I–III**
being the same stack plus precedence plus parens, and (b) the rep it actually delivered was **spec-reading**,
since all three bugs were language-default-vs-statement mismatches rather than stack errors.
**Decide at the build, don't let it drift:** if 150's justification is that it is a prerequisite, then
**one of 224/227 belongs on the board** — otherwise the prerequisite leads nowhere and the learner's
objection stands unanswered. Check the rest of the Stack phase (22 · 853 · 239 · 84) for the same
thin-algorithm shape while you are there. ⚠️ **The learner's reaction is the data here, not the rebuttal.**

**9. ✅ RESOLVED Aug 13 — superseded by the rework, and the resolution is bigger than the three questions.**
The learner's call: *"SD is way too open ended for us to tackle the way we've been tackling it… I will
study on the side via HelloInterview and your job is to hold mock interviews based on the questions."*
**All three decisions below are moot** — there is no lane ② to have a conflict in (a), the technology rows
are off the review engine entirely rather than relabelled (b), and blob storage is now covered by the
learner's own study plus the Dropbox mock rather than by a row (c). The map itself survives, rewritten:
it now answers *"which of the 8 patterns has the mock sequence never rehearsed"*, which is the one
coverage question the tracker cannot. **Kept below verbatim as the record of what was being decided.**

~~**9. 🆕 SD syllabus cross-walk written Aug 11 — [`coverage_map.md`](../../../https://github.com/michael-yrao/sd-progress/blob/main/coverage_map.md) — and it
needs THREE decisions at this build.**~~ The learner asked how to fold HelloInterview's *System Design in a
Hurry* into the repo; the answer is that **three of its four pillars are already here** (Delivery **is**
`framework.md`; all 10 Key Technologies have tracker rows; Caching + Networking cover 2 of 9 Core Concepts)
and **Patterns is 0 of 8**. Decide:
- **(a) The lane conflict — unavoidable.** Lane ② already falls back to `concepts/`; patterns would be a
  *third* bidder for one midweek slot, which the lane rule forbids. Options in the map: patterns **displace**
  concepts until drained (~4 wks, recommended) · a **4th SD slot** (+3.0 units/wk) · alternate (weakest).
- **(b) Re-label tech rows `Role — Product`?** HI keys by role (*Queue*, *Distributed Lock*), this repo keys
  by product (*Kafka*, *ZooKeeper*). Role-keying is the better interview instrument and makes an undrilled
  **role** visible — a `Kafka` row cannot show that *queue* has never been drilled. One cheap edit.
- **(c) Blob Storage has no row at all**, and it blocks Dropbox · YouTube · Instagram, all already on the
  board. Add it.
- **(d) 🆕 Numbers to Know — raised by the learner Aug 12** (*"can we apply these numbers in our
  system design methodology?"*). This is coverage_map's order item **#2**: one of the three things
  **nothing pulls** (with API Design and DB Indexing), so it must be front-loaded like the quantitative
  lane or it never lands. Two decisions, and the second is the real one:
  - **Slot.** It is a **FOURTH bidder** for lane ②'s one midweek slot. Resolve it inside (a) above —
    not as a separate carve-out, which is exactly how the lane rule gets broken one exception at a time.
  - **Shape — do not import it as a flat cheat sheet.** [`framework.md`](https://github.com/michael-yrao/sd-progress/blob/main/framework.md)
    §1 already commits to *"a number that doesn't change a decision is theater"*; a latency table keyed by
    *fact* is memorizable and unreachable mid-sentence. Key each number by **the fork it resolves**, matching
    the `You'll want this when…` trigger line every `concepts/` card opens with. Worked bindings:
    24 TB RAM on one box kills *"it's big, so we shard"* (**S**; write throughput forces sharding, size
    rarely does) · the latency ladder (**sub-1ms** same-AZ · **1–2ms** cross-AZ · **50–150ms** cross-region)
    says a p99 <100ms budget survives ~**one** cross-region hop, which is what prices cross-region strong
    consistency (**L** + **C**) · 25 Gbps in-DC ≈ 3 GB/s ≈ ~3M × 1 KB msg/s caps single-node fan-out ·
    60 TB SSD / 336 TB HDD picks row-store vs blob store.
  - ⭐ **Why it is worth pulling forward:** [`littles_law.md`](../../../https://github.com/michael-yrao/sd-progress/blob/main/concepts/littles_law.md) is
    the **operator** (`L = λW`) and
    [`percentiles_and_tail_latency.md`](../../../https://github.com/michael-yrao/sd-progress/blob/main/concepts/percentiles_and_tail_latency.md) picks the
    percentile — the repo already has the machinery and **no constants to feed it**. This card is the
    missing input, not another fact.
  - ✅ **UNBLOCKED Aug 12 — the learner pasted the premium tables.** Figures captured verbatim in
    [`concepts/numbers_to_know.md`](../../../https://github.com/michael-yrao/sd-progress/blob/main/concepts/numbers_to_know.md), which is a **source-data
    stub with NO tracker row** — a row means drillable and the scheduler would place it. Add the row in the
    same edit that writes the card (the coverage_map maintenance rule).
  - ⭐ **A finding came out of the paste, and it changes what the card should be.** Every scale trigger in
    HelloInterview's table fires at **70–80% of its stated ceiling** (cache memory > 80% · CPU > 70% ·
    memory > 80% · queue at 800k of 1M · connections *near* 100k). That is
    [`utilization_and_queueing.md`](../../../https://github.com/michael-yrao/sd-progress/blob/main/concepts/utilization_and_queueing.md)'s knee restated
    as ops practice — so the sheet collapses to **one rule already in the repo plus a list of ceilings**, and
    the card should be built on that spine rather than as sixteen flat figures. It also reprices the free
    tier: 24 TB of RAM is a ~19 TB budget, and quoting the derated number *with its reason* is the signal.
  - ⭐ **Three bindings land on cards that already exist and are unusable without a constant:** 100k
    concurrent connections is `L` for [`littles_law.md`](../../../https://github.com/michael-yrao/sd-progress/blob/main/concepts/littles_law.md) · the
    CPU-above-70% trigger is the queueing knee · the 80% cache-hit floor is only reachable *because* access is
    [Zipfian](../../../https://github.com/michael-yrao/sd-progress/blob/main/concepts/zipfian_distribution.md). This card is the missing **input** to
    machinery already built — the strongest argument for where it lands in (a).

⚠️ **Do not merge `concepts/` into HI's Core Concepts** — 7 of its 9 files (Little's Law, Zipf, percentiles,
sketches…) map to nothing in HI. That is the quantitative lane, which exists *because* the pull model cannot
catch concepts. Different axis, keep both.

---

## End-of-Week Targets

- [ ] **≥2 of the five no-🟢 Advanced Graphs algorithms reach 🟢.** *(Carried from last week, where it was
      0-of-5 with all four chances spent. All five reps land this week.)*
      **1 of 2 done — ✅ Prim's (1584, Aug 11).** Remaining chances: Dijkstra (778 Wed, 743 Fri) ·
      Hierholzer (332 Fri) · Bellman-Ford (787 Sat) · Floyd-Warshall (1334 Sat).
- [ ] **127 comes back clean on the counter**, measuring Monday's teach. A miss on the *counter's origin*
      for a third time means the teach didn't take and 127 becomes a phase-gated park, not another re-rep.
- [x] ⚙️ **SD track rebuilt around mock interviews** (Aug 13) — board reseeded to HelloInterview's 35
      questions, lanes retired, AI track removed, `mocks/` + debrief template added.
- [x] ⏸️ ~~Mock #1 (Bitly) run cold Sun Aug 16~~ — **skipped by the learner's call; SD is in study mode.**
      Not a slip and not deferred to a date: mocks start when they open one.
- [x] ~~Redis rated sprint~~ **cancelled** — the row it would log to no longer exists.
- [x] ~~Networking TCP re-teach~~ **moved to the learner's own study** on HelloInterview.
- [x] ✅ **Recognition probe #1 run** (Mon Aug 10, 977, 🟢 Clean), logged to `recognition_gotchas.md`,
      **no tracker row created.** Recognition call was a **pre-code, unprompted hit** — the first
      full-strength entry in the call log, since nothing named the technique in advance.
- [x] ✅ **Probe #2 (202, from the interview pool) run and logged** — Aug 11, **🟡 Shaky → Aug 21.**
      ⚠️ **The trigger written here has fired: it came back 🟡, so it created a row.** Per this bullet's own
      pre-commitment the exception gets **revisited at the Aug 17 build, not silently renewed.** The numbers
      to bring: 2 probes run, **1 row created**, ~73 units/yr of new permanent demand against a 1.0-unit
      one-off cost. **One 🟡 in two is a data point, not a trend** — the decision is whether to keep the
      surplus-gate bypass, not whether to keep probes. Full write-up in `recognition_gotchas.md`.
- [ ] **Overdue count ≤ 4.** It was 13 at the build; **13 are now scheduled** after the Aug 13 backfill
      (9 in the original plan + 680 · 695 · 2300 · 323-UF). Hitting ≤ 4 now depends on Thu/Sat/Sun
      actually landing at 9.0, not on more scheduling.

---

## 📌 PINNED — picked up here Aug 17, 2026

Parked mid-conversation late on Aug 16. Three open threads, in the order they block each other.

### 1. Coverage bar to 3 for every technique — BLOCKED on a tool gap

**Decided:** `min_problems: 3` for all techniques, and problems get pulled from the **company-wise
lists via `scripts/pull_interview.py`** — the repo's existing mechanism, frequency-ranked and gated on
patterns already learned. *(The coach first proposed NeetCode 250 / Sean Prashad; the learner corrected
that, since the pull machinery already existed.)*

**The number:** 39 of 52 techniques sit under 3. A flat bar implies **63 new problems** — roughly
150–190 units, about 19–24 days at the 8.0 ceiling, against standing demand of ~42/week. Large enough
that the fill has to be paced, not batched.

⚠️ **The blocker: `pull_interview.py` gates on 11 broad NC150 categories** (Arrays & Hashing, Graphs,
Stack, …) while `techniques.yml` names **52** fine-grained techniques. It can surface "a high-frequency
Google problem in Graphs"; it cannot surface "one that fills *Multi-source BFS*". For 39 thin techniques
that is the difference between a targeted fill and 63 hand-mappings.

**Agreed next step, not yet built:** add a `--technique` flag filtering on the CSV's **`Topics`** column
(finer than the categories — `Monotonic Stack`, `Hash Table`, `Matrix`), then produce the per-technique
candidate list for the learner to approve **before** anything enters the backlog. Do not skip the
approval step; it was asked for explicitly.

Two findings from the trial run to carry over:
- **Window matters.** Google returned **1752** eligible on `All`, Meta **56**. `--window 6mo` is likely
  the honest filter.
- **Some candidates are already on the roadmap** — Merge Intervals (Aug 24 Intervals phase), Subsets
  (Sep 14 Backtracking), Min Window Substring (current phase). The pull partly *validates* the roadmap,
  so the genuinely-new count lands well under 63.

⚠️ **Unresolved and worth one more push before building:** a flat 3 also applies to one-shape idioms
(*Length-Prefix Encoding*, *In-Place Array Rotation*, *Dutch National Flag*) where a 2nd and 3rd problem
is repetition, not transfer. The coach raised an algorithm/idiom split twice; the learner restated the
flat 3. **Treat flat 3 as decided** — raise it only if the candidate lists make the waste concrete.

### 2. SD reference cards — awaiting a read-through

`concepts/`, `components/`, `technologies/`, `templates/`, `coverage_map.md`, `senior_ramp.md` all went
back to **private sd-progress** on Aug 16 because any of them may reproduce HelloInterview text verbatim
and **history cannot answer that — only reading them can**. Public `system_design/` holds just
`study_guide.md` and `mastery/design_progress.md`.

**To promote any card back:** the learner skims it for copied passages and says which; they move one at a
time. No rush — the only cost of leaving them private is cross-repo links that 404 for anonymous
visitors, and the learner is authenticated.

### 3. cse-coach PR still unsent

`problem_link_reminder.py` is staged as **candidate 8** in [[project_upstream_candidates]], classified a
defect (no soak needed). Nothing has been sent.

---

## Week of Aug 17 Preview

**⏸️ SD: build the week with ZERO SD slots.** The learner is studying HelloInterview's Core Concepts +
Key Technologies and will open the next SD session themselves (a restructure session, then mocks).
**Do not put a placeholder slot on the board and do not ask whether they're ready** — they said they'd
come over. Two consequences the build must handle:

- **+9.0 units/week freed** (three lanes at 3.0). **Build the week at 9.0/day** — the learner's standing
  preference (Aug 13: *"go as close to 9 if possible, go over if needed"*). ⚠️ **Spend it on the 🟢
  backlog and due reviews, not on extra new intake.** The distinction is the whole rule: a rushed *new*
  problem lands 🟡 and bills ~12× forever, while an overdue 🟢 is demand that already exists. Intake stays
  at the difficulty-tier caps.
- **Every day without an SD slot carries an unseen problem — that is now all 7.** Against intake caps of
  moderate 5 / hard 4 / DP 3, **the caps bind before the rule does.** Fill the remainder with recognition
  probes, which create no permanent demand. Resolve it explicitly at the build; do not silently break
  either rule. ⚠️ Probe row-creation rate is already 1-in-2 (202 came back 🟡) — if probes become the
  default filler, re-check that they're still cheap.

**When SD restarts, the question is named at the session, never in a schedule file** — writing it down
lets its breakdown be read in advance, which caps the mock at 🟡. Same discipline as the recognition
probe's problem. *(Bitly is named above only because it was already in flight.)*

**📌 STANDING PREFERENCE, set by the learner Aug 13: new intake is NOT the default yield.** *"I don't want to slip the new problem of tomorrow always."* Raised when the proposed fix for Friday's overflow was, for the second time this week, to move the new problem. ⚠️ **This is the same structural complaint as the Aug 9 one about SD** — *"DSA-first left SD as the thing that got cut"* — one level down: **a lane that always yields is not protected by being scheduled.** New intake is the only rep that tests recognition and transfer (a 3rd-sighting retry measures that problem's solution, not the technique), so slipping it is the *most* expensive trim available disguised as the cheapest. **At the Aug 17 build: when a day is over ceiling, name what yields BEFORE the day runs, and it must not be new intake two weeks running.**

**📌 Reviews landing IN the Aug 17 week — place these at the build (schedule-integrity rule).**

| Due | Problem | Note |
|---|---|---|
| **Sat Aug 22** | 211 Add and Search Words 🟡 · 778 Swim in Rising Water 🟡 · 271 Encode/Decode Strings 🟡 | from Wed Aug 12. ⚠️ **778 is Dijkstra's next chance** — still zero 🟢 if 743 (Fri) misses |
| **Sun Aug 23** | 901 Online Stock Span 🟡 · 127 Word Ladder 🟡 | from today. 127 is Hard, prices 3.0 |
| **Fri Aug 21** | 150 Evaluate RPN 🟡 · 202 Happy Number 🟡 (probe #2) | from Tue Aug 11. 150's retry is the `rated:150` trigger for 224 Basic Calculator |
| **Fri Aug 14** | 155 Min Stack 🔴 | ⚠️ **tomorrow, and the only 🔴 on the board.** +2 measures whether Wednesday's teach stuck; it is **not** on Friday's table yet — place it |
| **⚠️ Tue Aug 18** | **332 Reconstruct Itinerary 🟡 (Hierholzer) — the RATED rep after the Aug 14 teach** | ⚠️⚠️ **PROTECTED, AND THE GAP IS THE POINT.** Taught 02:00 Aug 14 (unrated). Per §2a a teach must not be measured on fresh recall — **do NOT pull this into Sat 15 or Sun 16**, which is exactly the temptation since 332 is due and its row is now overdue. 3–4 days is the minimum; **127 is the worked example** (3-day gap, Aug 10 teach → Aug 13 measure, and it held). Hard, prices 3.0. **Hierholzer still has zero 🟢** — this is now its next chance, and the Aug 10–16 goal of converting all five zero-🟢 algorithms carries forward with Hierholzer, Dijkstra, Bellman-Ford and Floyd-Warshall outstanding |

⚠️ **Aug 22–23 alone is 5 🟡 rows (≈12 units) landing on a weekend**, against a week already holding the Stack phase's four remaining problems and a phase close on **Aug 23**. Spread them backward across the week at the build rather than discovering it on Saturday.

*Further out, noted so they are not lost:* **Mon Aug 24** — 743 Network Delay Time 🟡 · 739 Daily Temperatures 🟡, both from Fri Aug 14 (≈ 5.0 units; **743 is Dijkstra’s next 🟢 chance after this week went 0-for-2**). · **Oct 12** — 121 · 680 · 146, all 🟢 streak 2, from Thu Aug 13. · **Tue Oct 13** — 503 Next Greater Element II 🟢 streak 2.

**📌 853 Car Fleet — TAUGHT UNRATED Aug 15, rated rep MON AUG 17** *(learner: "we should re-visit 853 in
2 days")*. Took the slot vacated by 22 Generate Parentheses. The learner asked for pseudocode and a full
walkthrough after the derivation stalled, so it was converted to a teach — **nothing logged, no comfort**,
same handling as 332 on Aug 14.

**A 2-day gap is shorter than the 3–4 days the teach rule asks for, and that is defensible here** — it is the
🔴 Blank interval, and the Blank interval exists for exactly this case (*"the +2 measures whether the teach
stuck, not retention"* — the 155 note, Aug 12). Two different rules point two different ways: a **teach**
wants 3–4 days so the rep measures retention; a **Blank** wants +2 so it measures whether the teaching
landed. The learner picked the second. Worth knowing which question Monday's rep is answering: **did the
teach stick**, not *do they own the technique*.

⚠️ **The time complexity is NOT cold on Monday.** It was given away during the teach (*"O(n log n), dominated
by the sort"*). The learner supplied **O(n log n) / O(n)** afterwards, which is correct, but only the **space**
half is a real measurement. Fire the gate on both anyway; score only space.

✅ **RESOLVED Aug 15 — the row KEEPS its 🔴 Blank** (learner: *"either blank or delete is fine, no
preferences"*, so the call was made on mechanics). Row set to **Medium · 🔴 · streak 0 · attempted
2026-08-15 · next review 2026-08-17**.

**Why 🔴 rather than deleting the row**, since the 332 precedent pointed the other way:

1. **It is what the repo's own definition says.** 🔴 Blank = *"couldn't recall the approach; had to look it
   up."* The learner asked for the pseudocode and a full walkthrough after the derivation stalled. That is
   looking it up. The *teach* framing was the coach's, not a different event.
2. ⭐ **It makes the review engine do the right thing on Monday with no special handling.** If Monday comes
   back 🟢, the **provisional-Clean rule** fires — a 🟢 directly after a 🔴 logs at **streak 0 → +10
   lock-down**, not streak 1 → +30. That is *exactly* the treatment a Clean straight after a teach deserves,
   and the provisional rule's own stated rationale is the same argument: *"one Clean right after a Blank may
   be recall of fresh teaching, not durable retention."* Delete the row and Monday's 🟢 would bank a full
   +30 on a problem whose solution was handed over 48 hours earlier.
3. The +2 it computes **is** the Mon Aug 17 date the learner asked for, so nothing had to be forced.

**This is now the convention for a taught-unrated NEW problem: keep the 🔴.** It differs from 332 only
because 332 already had a row and a rating to preserve — *"row untouched"* means something different when
there is no row. ⚠️ Note the asymmetry deliberately: **an existing row is never overwritten by a teach; a new
problem gets its 🔴.**

**📌 POLICY — a hard algorithm needs more than one problem, set by the learner Aug 15.** *"I want more
practice with floyd warshall… this applies for all the harder algorithms, we should have more than just
one problem tackling it."*

**Implemented in `techniques.yml`:** Floyd-Warshall and Prim's MST were the last two `advanced_graphs`
techniques pinned at `min_problems: 1`. Both pins are removed, so they inherit the file's default of **3**
(Hierholzer went to 3 earlier the same day). **All five advanced-graph techniques now report against the
same bar, and all five are thin:**

| Technique | Coverage | Green |
|---|---|:---:|
| Bellman-Ford | 1/3 (787) | ✅ Aug 15 |
| Dijkstra | 2/3 (743, 778) | ❌ |
| Floyd-Warshall | 1/3 (1334) | ❌ |
| Hierholzer | 1/3 (332) | ❌ |
| Prim's MST | 1/3 (1584) | ✅ |

**Floyd-Warshall candidates, in the order they should be taken:**

| Problem | Slot | Why this one |
|---|---|---|
| **1462 Course Schedule IV** (Medium, NEW) | **Aug 17 week** | The best second F-W problem on the site. Identical triple loop, but **boolean transitive closure** rather than additive distance — which teaches that Floyd-Warshall is a **pattern over a relation**, not a shortest-path algorithm that happens to use a matrix. That generalisation is the thing one problem cannot teach |
| **2642 Design Graph With Shortest Path Calculator** (Hard, NEW) | **Aug 24 week** | F-W under **edge insertion** — re-running the whole triple loop per query is the naive answer and the problem punishes it. Pairs naturally with 753 in the same week |
| **743 via Floyd-Warshall** (variant) | hold — `graduates:743` | 743 already carries a queued Bellman-Ford contrast rep behind that same gate, and the row is still 🟡. A third method on an unstable problem measures nothing |

⚠️ **CAPACITY — these do not all fit, and the Aug 17 week is where it breaks.** With the ceiling moving to
**8.0** (56 units/week) against **41.9** of standing demand, headroom is **~14 units/week**. Already
queued for that week: **2097** 4.5 · **1462** 3.0 · **Kruskal variant** ~1.0 · the Stack phase's remaining
**853 · 239 · 84** at ~3.0 each = **~9.0**. That totals **~17.5 against ~14.1** — over by roughly a
day's worth, *before* the week's own reviews are placed.

**Do not resolve this by raising the ceiling back to 9.0** — that is the exact move the effort-budget rule
forbids, and the learner has just asked for the opposite. Resolve it by **naming what slips at the build,
in advance**, per the Aug 13 standing preference (new intake is not the default yield, and must not be
the yield two weeks running). The honest candidates are the Stack-phase problems, which are a *count*
against a phase close, not a technique with zero green.

**📌 Also for the build — the same audit outside `advanced_graphs` was NOT run.** Raising every
`min_problems: 1` at once would flood the coverage report with fresh `thin` findings and it would stop
being read, which is the failure mode the `queued:` mechanism exists to prevent. Candidates worth a
deliberate look, one at a time: **Tree Construction (D&C)** · **Graph Cycle Detection (DFS)** ·
**Multi-source BFS** · **BFS on Implicit Graph** · **Graph Clone**. Decide per technique whether it is
genuinely a *hard algorithm* or a one-shape idiom; only the former earns the bar.

**📌 DECISION — drop the ceiling 9.0 → 8.0 at this build, set by the learner Aug 15.** *"it is getting
harder to fit in SD filling DSA with 9 points each day, so I might want to lax back to 8."*

**The arithmetic says yes, comfortably.** Demand is **5.99 units/day (41.9/week)**. An 8.0 ceiling gives
**56 units/week**, so headroom stays **+14.1/week** — the deficit does not reopen. What it costs is
**backfill speed**: 12 overdue rows / 13.5 units burn down about a unit a day slower.

⚠️ **This is the ceiling rule running in the direction it was written for.** The standing warning is
never to *raise* the ceiling to chase a backlog, because a rep rushed into a 🟡 bills ~12× forever.
The same logic makes *lowering* it cheap: the ceiling is a quality judgment, demand sets the floor, and
demand is nowhere near 8.

**The real finding underneath the request — the budget prices the wrong universe.** The learner's SD work
is now self-directed HelloInterview study in a separate repo, so it is **off-board and unpriced**, while
still consuming the same evening. A day the script calls 9.0 is really 9.0 **plus** whatever SD took.
That is not a scheduling error; it is the SD rework's side effect, and it means the DSA ceiling has been
silently doing double duty since Aug 13. **Lowering to 8.0 is the honest correction**, not a concession.

**Actions at the build:** set `effort_budget.ceiling: 8.0` in `cse.config.yml` (line 46) — *not before*,
or today's already-priced 9.0 board retroactively reads as over-ceiling for no reason. Then re-run
`effort_budget.py` and rebuild the per-day load row against 8.0; the Aug 17 preview above still says
*"build the week at 9.0/day"* in the SD section and **that line must be updated in the same edit.**

**📌 NEW — edge-based graph expansion, requested by the learner Aug 15.** *"I can confidently get the
shape and write them out for the others more often than not but for Hierholzer, it is 50:50."* The
vocabulary agreed once it was asked properly: Hierholzer was declared `min_problems: 1`, so a technique
with a single problem and a self-reported coin-flip **was never flagged**. Raised to 3 (LeetCode has only
three Eulerian problems in total, and all three are wanted); it now reads `no-green · thin (1/3)`.

⚠️ **The diagnosis is NOT recognition.** The Aug 14 ledger entry is the strongest picking-feature call in
the repo (*"visited holds edges not nodes"*, pre-code, unprompted). The failure was **bookkeeping** —
`for` + `return` instead of `while` + delete. Those are the same gap in two places: `while graph[node]`
means *"while this node isn't stuck"* (edge-thinking), `for neighbour in graph[node]` means *"visit each
neighbour once"* (node-thinking). **Do not schedule recognition work for this** — it will measure something
that already passes.

| Item | Slot | Note |
|---|---|---|
| **2097 Valid Arrangement of Pairs** (Hard, NEW) | **Thu Aug 20** | The purest Hierholzer available — no lexicographic ordering, and the start node must be **derived** from `out−in = 1`, which 332 hands over for free (`JFK`). Attacks the bookkeeping gap directly. ⚠️ **After Tue Aug 18, never before** — a second Eulerian problem in front of the rated rep contaminates Hierholzer's only 🟢 chance. Counts against the **Hard intake cap of 4** |
| **753 Cracking the Safe** (Hard, NEW) | **Aug 24 week** *(no file yet — noted here per the schedule-integrity rule)* | de Bruijn sequence via Eulerian circuit. Real, but the difficulty is **constructing** the graph; the Hierholzer part is almost incidental. Third rep, not second — hold it behind 2097 |
| **Kruskal's MST** (variant rep on 1584, already 🟢) | **Wed Aug 19** | Edge-first MST: sort edges, union-find, skip cycles. Not Hierholzer, but genuine edge-centric thinking and cheap — the problem is already 🟢. Declared `queued` in `techniques.yml` |

⚠️ **PRICING TRAP — `effort_budget.py` prices any untracked problem as a new 🔴 *Medium* = 3.0.** Both new
problems here are **Hard**, so the true cost is **4.5 each**, not 3.0. That is the same class of
mispricing that put Fri Aug 14 at 10.0 rather than the 9.0 the build wrote. **Price them by hand at the
build.**

**📌 Pattern notes — written AFTER the rep, never before.** `patterns/README.md` lists Hierholzer,
Kruskal's, Dijkstra and Bellman-Ford as ⚠️ *not written*, and the learner asked for them Aug 15. **Every
one of them maps to a live or queued rep**, which is very likely *why* they are unwritten — so writing them
early hands over the answer to a protected rep. Sequence:

| Note | Write it after | Why the order matters |
|---|---|---|
| **Bellman-Ford** | **787, today (Sat Aug 15)** | 787 is on today's board, protected, and is Bellman-Ford's only 🟢 chance this week. Writing this note before it is a direct spoiler |
| **Hierholzer** | **332, Tue Aug 18** | Rated rep, protected *and* gap-protected. The learner's stated goal (*"I don't understand why it exists"*) is exactly what the note should capture — as a debrief artifact, not a primer |
| **Kruskal's** | the Kruskal variant rep, **Wed Aug 19** | Same reason |
| **Dijkstra** | **778, Sat Aug 22** | Dijkstra went 0-for-2 this week and still has zero 🟢 |

Floyd-Warshall, Prim's and topological sort already have notes. **The learner writes these** — the note is
the rep; a coach-authored note is reference material and does not convert 50:50 into a stated procedure.

**📌 NEW — complexity drills are their own item, raised by the learner Aug 13.** After 127: *"this is a very hard problem to knock down for Big O, we should definitely practice it more."* Worth taking literally, because the evidence agrees — **five of this week's six rated reps were capped or nearly capped on complexity, never on code** (211 recursion space · 271 unit-of-work ×3 · 901 design-object space · 127 string cost ×2 + convention drift; only 121/680 came through clean). The code axis is not what is holding 🟢 s back right now.

- ⚠️ **The cheap version is NOT another problem.** These reps are already being *solved* correctly; adding a problem buys another solve, not another complexity answer. Build the drill as a **cold Big-O pass over problems already 🟢/🎓 in the tracker** — no coding, ~1.0 unit for several, and it exercises the one axis directly. It also creates **zero permanent demand**, which is the same argument that bought the recognition probe.
- **Pull the drill set from `complexity_gotchas.md`'s category table, not at random** — the recurring families are already enumerated there, and *a string is not O(1)* (new Aug 13) has exactly one occurrence and no second exposure scheduled.
- ⚠️ **Decide at the build whether this competes with the recognition probe** for the non-SD unseen slot, or sits beside it. Both are cheap and disposable, which is precisely why they can quietly crowd out real reps if neither is priced.

**📌 NEW — the Grind 75 fill tier opens (added Aug 13).** 13 problems that Grind 75 carries and NC150
does not, now the **first filler slot** ahead of consolidation reps (list, reasons and gates:
[`dsa_progress.md`](../dsa/mastery/dsa_progress.md) → *Grind 75 Fill*). **≤2/week, never displacing
active-phase intake.** For the Aug 17 build specifically:

- **438 Find All Anagrams** is the first pull — it targets the **fixed-alphabet complexity miss** (5
  occurrences across 5 problems, capped once), and Sliding Window is already 🟢 so it is cheap.
- **224 Basic Calculator** is gated `rated:150` — **150's retry is Aug 21**, so the trigger can fire
  that same week. It also settles the Aug 11 objection that 150 had no value.
- ⚠️ **This competes with the Stack phase's four remaining problems** (22 now on Sat · 853 · 239 · 84)
  and the phase closes **Aug 23**. Phase intake wins; the fill tier takes what's left.
- **815 Bus Routes / 1197 Minimum Knight Moves** are held behind `solved:127` — **127 runs Thu Aug 13**,
  so check whether that trigger fired before building.

**Already dated:**
- **133 Clone Graph 🟡 → Aug 19** *(🟢 s1 → 🟡 on Aug 9; node-identity vs edge-recording conflated)*
- **560 Subarray Sum = K 🟡 → Aug 18** · **235 LCA of BST 🟡 → Aug 18** — ⚠️ **both land the same day, split them at the build**
- **19 Remove Nth Node (Postorder) 🟡 → Aug 17** · **269 Alien Dictionary 🟡 → Aug 17** · **912 → Sep 7**
- **150 Evaluate RPN 🟡 → Aug 21** *(new, Aug 11)* — ⚠️ **lands the same day as 202 below; split them at the
  build.** On the retry, the thing to test is **not** the stack (that was never the problem) — it's whether
  the truncation-vs-floor distinction survived. Fire `["-7","2","/"]` cold.
- **202 Happy Number 🟡 → Aug 21** *(probe #2, Aug 11)* — ⚠️ **new row, and it lands on a Friday.** Its retry
  is where the **Floyd fast/slow** variant becomes interesting: the seen-set version is O(1) space *because
  the state space collapses to 243*, so fast/slow buys nothing here — which makes it a clean place to ask
  *why* you'd ever reach for it. Variant is declared `queued: rated:202` in `techniques.yml`, so it will not
  re-surface as a new coverage gap.
- **721 Accounts Merge 🟢 s1 → Sep 8** *(🟡 → 🟢 Aug 9)* · **503 Next Greater Element II 🟢 s1 → Sep 9** *(🟡 → 🟢 Aug 10)*
- **1584 Min Cost Connect Points 🟢 s1 → Sep 10** · **875 Koko Eating Bananas 🟢 s1 → Sep 10** *(both 🟡 → 🟢 Aug 11)*
  — ⚠️ **Sep 7 (912) · Sep 8 (721) · Sep 9 (503) · Sep 10 (1584 + 875) — four consecutive days and FIVE
  problems**, with Sep 10 carrying two. That week has no file yet; **split them at the build that creates
  it**, not on the day. 1584 is the dearest (Medium, and the only one carrying a protected-algorithm
  history), so if anything moves off Sep 10, move 875.
- **323 Connected Components (DFS) 🟢 s2 → Oct 10** *(s1 → s2 Aug 11, cap overridden — see the Tue row)*
  ⚠️ **This row is off the board for two months on the strength of the override.** The complexity category
  it missed is covered by **261 (Aug 16) · 133 (Aug 19) · 210 (Aug 23)** instead — **fire the gate cold on
  all three.** If the category misses again on any of them, that is the signal to revisit, not Oct 10.
- **105 · 141 · 621 · 1011 🟢 s2 → Oct 8** *(all four converted Aug 9 — ⚠️ four on one day, split at that build)*
- **66 · 703 🟢 s2 → Oct 9** *(both converted Aug 10)* — ⚠️ **so Oct 8 carries 4 and Oct 9 carries 2: six s2 conversions across two adjacent days.** Both are Easy (0.5 each) so Oct 9 is cheap, but **the pile is the point** — the Aug 9–10 conversion sweep is depositing its whole yield into one October week. Spread it at the build that creates that week.

**⏳ Waiting Room — evaluate every trigger at the Aug 17 build:**
- **42 Two-Pointer**, trigger `graduates:42`. 42-Array went 🟢 s2 on Aug 7 → **Oct 6 rep fires it.** Not before.
- **1216 Valid Palindrome III**, triggers `phase:Backtracking` (Sep 14) and `phase:1D-DP` (Oct 12).
- **DFS-postorder topological sort**, trigger `graduates:210`.
- **D&C on 53**, trigger `rated:912 + surplus>=1`. ⚠️ **912 was rated 🟢 s1 on Aug 8 and surplus is now
  positive — this trigger has FIRED.** Slot it or re-defer with a written reason; do not leave it sitting.

✅ **269's edge-set teach RAN Wed Aug 12** (see Warmup Notes) and measures on **Aug 17** — the 5-day gap
was preserved. **Aug 17 build: check bugs #1 and #4 specifically** (node set seeded from `adjMap`; no `break`
after the first differing position). A clean 269 that never exercises those two is not evidence the teach took.


---

## Appendix — pre-Aug 16 daily rows (verbatim)

Moved out of the table **Aug 16, 2026** at the learner's request: *"the schedule table has once again become a mess and I can't read it at all… No comments allowed inside the table."* Individual cells had reached **7,193 characters**. Kept verbatim rather than deleted, because some of it predates this session; nothing here is load-bearing for a future build — the analysis it duplicates lives in the mastery ledgers.

<details>
<summary>Expand</summary>

### (superseded) Daily Schedule — original rows

| Day | Warmups | Active block | Other |
|-----|---|---|---|
| **Mon Aug 10** | ✅ ~~[503 Next Greater Element II](../../../dsa/leetcode/stack/503_next_greater_element_ii.py)~~ · [LC](https://leetcode.com/problems/next-greater-element-ii/) — **🟡 → 🟢 Clean, streak 1 → Sep 9**<br>~~[80 Remove Duplicates II 🟢]~~ → **moved to Sun Aug 16** *(due today, not overdue; yielded at 23:15)*<br>✅ ~~[66 Plus One](../../../dsa/leetcode/arrays_and_hash/66_plus_one.py)~~ · [LC](https://leetcode.com/problems/plus-one/) — **🟢 s1 → s2 → Oct 9** *(was overdue Jul 31; 10-day burn-down)* | ~~[98 Validate BST 🟢]~~ → **moved to Sun Aug 16** *(due today, not overdue; yielded at 23:15)*<br>✅ ~~[703 Kth Largest in Stream](../../../dsa/leetcode/heap/703_kth_largest_element_in_stream.py)~~ · [LC](https://leetcode.com/problems/kth-largest-element-in-a-stream/) — **🟢 s1 → s2 → Oct 9** *(was overdue Aug 1; 9-day burn-down)*. Complexity freebie spent: `__init__` is **O(n log k)**, not `n log n` — the trim caps the heap at k | **SD ① — Redis TEACH (unrated)**<br>✅ **⚡ 15-min TEACH: counter semantics in layered BFS — DONE (unrated).** Both decisions derived on a throwaway graph; 127 deliberately never opened. Written into `recognition_gotchas.md`. **Key finding: a hand-trace on a LINE makes the broken version produce the right answer** — which is why three reps never caught it. Measured Thu Aug 13<br>✅ 🎯 **PROBE #1 — [977 Squares of a Sorted Array](../../../dsa/probes/977_squares_of_a_sorted_array.py) · 🟢 Clean** *(pulled forward from Tue; run 00:50)* — recognition **hit, pre-code and unprompted**. Disposable rep: **no tracker row**<br>~~🎯 **PROBE #2 — 202 Happy Number**~~ → **back to Tue Aug 11** *(not reached; deliberately NOT attempted at 23:15 — a probe is judged on a cold recognition call, and a 🟡 would create a permanent tracker row at ~73 units/yr on a rating that reflects the hour)*<br>▶️ **Run order (revised 15:30):** ~~503~~ ✅ → **SD ①** → 202 → ⚡ BFS-counter teach *(deferred within-day at the learner's request; **started and stopped after one question**)* → 🟢 tail (98 · 703 · 80 · 66). **The 🟢 tail is what yields**, not SD and not the teach<br>⚠️ **The BFS teach cannot slip past today.** 127 measures it **Thu Aug 13** and the 3-day gap is the design — a teach on Tue/Wed makes Thursday a recall check, not a measurement. If today runs out, the correct move is to **move 127 later**, never to compress the gap |
| **Tue Aug 11** | ✅ ~~[1584 Min Cost Connect Points](../../../dsa/leetcode/graphs/1584_min_cost_to_connect_all_points.py)~~ · [LC](https://leetcode.com/problems/min-cost-to-connect-all-points/) — **🟡 → 🟢 Clean, streak 1 → Sep 10.** ⚠️ **protected rep, and it delivered: Prim's is off the zero-🟢 list — the first of the five.** Recognition hit pre-code (the comment *was* the call, naming both the technique and the `distance[]` invariant). Complexity clean, **no freebie spent** — O(V²) time / O(V) space, itemized. Coached point, not a miss: on a complete graph `O(V²)` and `O(E)` are the same number, but the loops are V-rounds-of-V-scan and never touch an edge list, so O(V²) is the durable statement. Spun out into a new **sparse-vs-dense** section in `recognition_gotchas.md` — density is what picks array-Prim's over heap-Prim's, and the array choice here was right ✅ ~~[875 Koko Eating Bananas](../../../dsa/leetcode/binary_search/875_koko_eating_bananas.py)~~ · [LC](https://leetcode.com/problems/koko-eating-bananas/) — **🟡 → 🟢 Clean, streak 1 → Sep 10.** Zero hints. Bounds *derived* from the constraints in the pre-code comment, not recalled. ⭐ **Complexity was the story: O(n log k) with k = max(piles), volunteered unprompted** — that is precisely the 1011 miss from Aug 9 (*"binary is log n"*) firing correctly on the sibling problem two days later. Logged as a **transfer** in `complexity_gotchas.md`<br>✅ ~~[323 Connected Components (DFS)](../../../dsa/leetcode/graphs/323_number_of_connected_components_in_an_undirected_graph.py)~~ · [NC](https://neetcode.io/problems/count-connected-components) — **🟢 Clean, streak 1 → 2 → Oct 10.** Code was 🟢-grade unaided. **Complexity missed: said O(V·E), actual O(V+E)** — a *repeat* on 323 (BFS row, Aug 5), so the rule capped it at 🟡. ✅ **CAP OVERRIDDEN BY THE LEARNER, recorded in `complexity_gotchas.md`:** *"I just want more practice on recognizing it on the other problems, redoing this in 10 days won't really help much there."* The category gets three cold shots in 12 days (**261 Aug 16 · 133 Aug 19 · 210 Aug 23**) — ⚠️ **so fire the complexity gate COLD on 261; it is now carrying this rep's weight**<br>~~🎯 **RECOGNITION PROBE #1**~~ → **done early, Mon Aug 10** (977, 🟢). ✅ 🎯 **PROBE #2 — [202 Happy Number](../../../dsa/probes/202_happy_number.py) · 🟡 Shaky → Aug 21.** ⚠️ **The first probe to earn a tracker row** — the "probes are free" exception is now 2 run / 1 row. Recognition **did not fire cold**: the pre-code comment stated a sub-goal (*"define an end point"*), not a technique; the real call (cycle detection → seen-set) came only after the coach challenged the termination condition. Real fix supplied and not self-caught: `seen` was a **class attribute**, so state leaked between calls (`isHappy(19)` then a fresh `isHappy(82)` → False, and 82 is happy). Complexity double-freebie spent — said O(1) time, actual **O(log n)**; the 810→243 collapse argument was coach-supplied<br>✅ ~~🆕 **[150 Evaluate Reverse Polish Notation](../../../dsa/leetcode/stack/150_evaluate_reverse_polish_notation.py)**~~ · [LC](https://leetcode.com/problems/evaluate-reverse-polish-notation/) — *NEW, phase intake.* **🟡 Shaky → Aug 21.** Three correctness bugs, none self-caught (operand order · `//` floors where the spec truncates · `["5"]` falling off the end); the division one needed two rounds. **All three were spec-vs-language-default, not stack bugs** — the algorithm was in hand immediately. Recognition **not measurable** (folder + docstring both said `stack`). Complexity clean, no freebie. ⚠️ **Learner pushback worth carrying to the Aug 17 build:** *"I don't see anything of value here"* — the algorithmic content really is thin; the case for it is that it's the base rung under 224/227/772 Basic Calculator. **Their reaction is the data, not my rebuttal** | — |
| **Wed Aug 12** | ✅ ~~[211 Add and Search Words](../../../dsa/leetcode/trie/211_design_add_and_search_words_data_structure.py)~~ · [LC](https://leetcode.com/problems/design-add-and-search-words-data-structure/) — **🟡 Shaky → Aug 22.** Code was flawless from a blank page, zero hints, all four standard failure modes handled. **Capped by complexity:** search *space* given as `O(26^d)` then `O(26)` — fan-out, not stack depth; the `children.values()` loop is sequential so the stack is one path, `O(c)`. 211's freebie was spent Aug 2. ⭐ **Time was right and unprompted** (`O(c·26^d) → O(c)`), which is exactly the Aug 2 miss on this problem firing correctly. ⚠️ **4th recursion-space miss** (235 · 332 · 261 · 211) — carry to the Aug 17 build | ✅ ~~[778 Swim in Rising Water](../../../dsa/leetcode/graphs/778_swim_in_rising_water.py)~~ · [LC](https://leetcode.com/problems/swim-in-rising-water/) ⚠️ **protected** — **🟡 Shaky → Aug 22.** ⚠️ **Dijkstra still has ZERO 🟢 after its first of two chances.** Recognition hit pre-code and named the real tension (water level vs heap min). Bug was **event-placement, not algorithm**: the destination check sat in the neighbour-discovery loop, so it fired before the cell was enterable; two rounds of coaching, the load-bearing insight was coach-supplied. Complexity **passed, not missed** (heap terms right unprompted; asked to be taught the `time += 1` term — bounded by the *value range*, not the grid). **743 Fri is now the only remaining Dijkstra 🟢 chance this week**<br>✅ ~~[271 Encode and Decode Strings](../../../dsa/leetcode/arrays_and_hash/271_encode_and_decode_string.py)~~ · [NC](https://neetcode.io/problems/string-encode-and-decode) — **🟡 Shaky → Aug 22.** Code correct first pass, zero hints, recognition pre-code; verified on 3000 randomized round-trips. **Capped by complexity: unit-of-work ×3** — counted strings where the work is per character, on `encode` time, `encode` space and `decode` time. ✅ Got `decode` space right *and defended it under challenge* (`result` is the output there, a midpoint in `encode`). ⚠️ **Unit-of-work is now 6 occurrences** (721 · 271 Aug 2 ×2 · 271 today ×3) — carry to the Aug 17 build<br>✅ ~~🆕 **[155 Min Stack](../../../dsa/leetcode/stack/155_min_stack.py)**~~ · [LC](https://leetcode.com/problems/min-stack/) — *NEW, phase intake.* **🔴 Blank → Aug 14.** Design (pair each value with min-so-far) was **coach-supplied** after deque and single-variable attempts both failed; learner diagnosed *why* each failed, unprompted, but did not reach the fix. Then three `stack[0]`-vs-`stack[-1]` bugs, none self-caught. ⚠️ **The +2 measures whether the teach stuck, not retention.** Recognition **not measurable** — `stack/` path + `Pattern: stack` docstring, 2nd occurrence on a new problem after 150 | — |
| **Thu Aug 13** ⚙️ | ✅ ~~[121 Best Time to Buy/Sell](../../../dsa/leetcode/greedy/121_best_time_to_buy_and_sell_stock.py)~~ · [LC](https://leetcode.com/problems/best-time-to-buy-and-sell-stock/) — **🟢 Clean, streak 1 → 2 → Oct 12.** Zero hints. Recognition **hit, pre-code and unprompted** — the comment named two-pointer *and* the `l` -reset invariant. Complexity clean, no freebie: O(n) one pass (`r` monotonic, `l` only jumps forward), O(1) three scalars<br>✅ ~~[901 Online Stock Span](../../../dsa/leetcode/stack/901_online_stock_span.py)~~ · [LC](https://leetcode.com/problems/online-stock-span/) — **🟡 Shaky → Aug 23.** Recognition **hit pre-code** (*"stack of tuples… decreasing stack where each value holds its own span"*) but the learner was stuck on the **mechanism**: the pop was read as a single check, not a loop. Unstuck by a hand-trace of `75` over `[(100,1),(80,1),(70,2),(60,1)]` — coach-supplied, which is what carries the 🟡. Code then correct first pass, `>=` right (equal prices must be absorbed). ⭐ **Complexity was the interesting part and neither half was a miss:** time "O(n) worst case per call" is **correct** — refined to **amortized O(1)** (each price pushed once, popped once), and "no extra space" was defended as **per-call auxiliary O(1)**, which is also correct. The gap is the **design-object convention** — a class's space means the object's footprint, **O(n)**. Logged as a phrasing gap, **no freebie spent**<br>✅ 🔥 ~~[680 Valid Palindrome II](../../../dsa/leetcode/two_pointers/680_valid_palindrome_2.py)~~ · [LC](https://leetcode.com/problems/valid-palindrome-ii/) — **🟢 Clean, streak 1 → 2 → Oct 12** *(backfill; was the oldest row on the slip list, overdue since Jul 12 — **32 days, now cleared**)*. Zero hints, correct first pass. Recognition **hit pre-code**, naming the *both-sides* branch, which is the one feature separating this from 125. ⭐ **Complexity clean and the hard half was volunteered:** the nested `while` is **not** O(n²) because the helper returns immediately, so it fires **at most once** — total ≤ 2n. Space O(1). **No freebie spent**<br>✅ 🔄 ~~[138 Copy List w/ Random Pointer](../../../dsa/leetcode/linked_list/138_copy_list_with_random_pointer.py)~~ · [LC](https://leetcode.com/problems/copy-list-with-random-pointer/) — **PULLED FORWARD from Fri Aug 14** at the learner's request (*"bring 138 in today first and we go from there"*) — **🟡 → 🟢 Clean, streak 1 → Sep 12.** Blank page, algorithm right first pass, complexity correct and itemized unprompted (two independent passes → **add, don't multiply**; the map is the only structure). Recognition hit pre-code. ⚠️ **A missing `return` in the file was a paste artifact, not an algorithmic miss** — verified against the LC run before rating, so it capped nothing. ⭐ **This unblocks the parked expansion-queue item**: the one-pass **O(1)-space** interweave (`A→A'→B→B'`, wire `.random`, unweave, no map), parked since Jul 5 — a real Aug 17 candidate now the map version is 🟢 | ✅ ~~[127 Word Ladder](../../../dsa/leetcode/graphs/127_word_ladder.py)~~ · [LC](https://leetcode.com/problems/word-ladder/) ⚠️ **measured Monday's teach** — **🟡 Shaky → Aug 23.** ⭐⭐ **THE TEACH HELD.** `depth = 1`, increment after the level loop, `return depth + 1` at **discovery** — the exact break that recurred on all three prior reps (Jul 18 · Jul 21 · Aug 3), correct cold after a deliberate 3-day gap, zero hints. **First teach in the repo measured on a gap and returned intact.** Code was 🟢-grade throughout; recognition hit pre-code, naming the graph as **constructed, not given**. ⚠️ **Capped on complexity, three corrections:** BFS phase undercounted (`O(V+V·L)` → `O(V·L²)` — the loop re-slices exactly as the build does, so the phases **tie**), space undercounted (`O(V·L)` → `O(V·L²)` — `V·L` keys **of length L**), and `L` held symbolic for time then collapsed via `L ≤ 10` for space. **Freebie spent** — new category *a string is not O(1)*. ⭐ The build phase *was* priced right unprompted, and the learner **volunteered the fixed-alphabet collapse** (≤26 per bucket kills the `E` term — 6th of that family)<br>✅ ~~[146 LRU Cache](../../../dsa/leetcode/linked_list/146_lru_cache.py)~~ · [LC](https://leetcode.com/problems/lru-cache/) *(moved off Sat)* — **🟢 Clean, streak 1 → 2 → Oct 12.** Blank page, zero hints, correct first pass — including the eviction ordering (`tail.prev.key` read for the map *before* unlinking) and the single-node `get`. Recognition hit pre-code, naming DLL + **two dummy nodes** + map with reasons. Time O(1) per op, and the eviction `while` doesn't break it — `put` grows the map by at most one, so it runs at most once. ⭐ **Space is the story: *"O(n) across multiple calls"* is 901's design-object convention transferred cold, unprompted, ~3 hours after it was taught** — refined to **O(capacity)**, since the cap is an input and the point is that the cache does not grow with usage. **No freebie spent** | ⚙️ **SD slot spent on the REWORK — the SD track was rebuilt around mock interviews (unrated).** ~~Networking TCP re-teach~~ → now the learner's own HelloInterview study ([Networking Essentials](https://www.hellointerview.com/learn/system-design/core-concepts/networking-essentials)). See [`study_guide.md`](../../../docs/foundations/system_design/study_guide.md) |
| **Fri Aug 14** | ⚠️ **UNSCHEDULED PULL, late night: 503 Next Greater Element II 🟢 s1 → s2 → Oct 13** — *not due until Sep 9; pulled 26 days early while 332 🟡 and 155 🔴 were still outstanding.* Execution flawless (blank page, 3000 randomized cases, zero hints, complexity clean both axes). **Recognition NOT measurable** — the 739 debrief had quoted its Aug 10 call verbatim. 📌 **Learner set a standing rule off the back of this: "if they are not in today’s todo list, don’t populate the links"** — links are for the day’s board, not for context mentions, because a link is an invitation. See `feedback_kickoff_table_links.md` + the Aug 14 `self_eval_log.md` entry (flag-and-proceed)<br>~~138 Copy List w/ Random Pointer 🟡~~ → **moved to Thu Aug 13** *(pulled forward to lighten Friday; the only unprotected 🟡 on the day)*<br>✅ ~~[743 Network Delay Time](../../../dsa/leetcode/graphs/743_network_delay_time.py)~~ · [LC](https://leetcode.com/problems/network-delay-time/) ⚠️ **protected** — **🟡 Shaky → Aug 24.** ⚠️ **Dijkstra still has ZERO 🟢 — both chances this week are now spent (778 Wed, 743 today).** Recognition hit pre-code but named the `visited` set without its timing; `visited` was marked on **push**, and the heap tuple was `(node, weight)` so it ordered by node id. Both coach-supplied, one via counterexample — a **regression** on the settle-on-pop rule that Aug 4 had correct. Fix was then clean and self-written. **Complexity capped it further:** `O(E + V log V)` is the Fibonacci-heap bound, not `heapq`’s — pushes are per edge → `O(E log V)`; final assembly was a freebie. Space `O(V + E)` right unaided ⚙️ ~~[332 Reconstruct Itinerary 🟡](../../../dsa/leetcode/graphs/332_reconstruct_itinerary.py)~~ · [LC](https://leetcode.com/problems/reconstruct-itinerary/) ⚠️ **was protected — CONVERTED TO A TEACH, UNRATED (§2a never-encoded).** ⭐ **Recognition was a hit and the strongest picking-feature call in the ledger** — *"visited holds edges not nodes"* (the exact Eulerian-vs-Hamiltonian discriminator) **and** *"get to end node first then take care of call stack"* (the post-order append), both pre-code. ❌ **And it still could not be written**: two attempts, the second returning `[]` (a `return` inside the `for` made the append unreachable). Learner: *"I don't think I understand Hierholzer."* **The gap was bookkeeping, not the algorithm** — `for` + `return` instead of `while` + *delete*; a `visited` set cannot represent duplicate tickets, which is why it kept fighting them. **Taught procedure-first**: consume-the-edge, stranding is structurally forced to happen at the end node, post-order splices leftover cycles in. Hand-traced on 3 tickets; learner reached *"adjMap with a minHeap"* themselves — `heappop` picks the smallest **and** consumes the ticket in one call. The *why* (balanced-degree argument) was started and **pinned at the learner's request** — 02:00, *"too late in the day"*. **Nothing logged; 332 keeps its 🟡 and its row is untouched.** ⚠️ **This is the repo's cleanest recognition/execution split** — both axes measured on one rep and they disagreed, which is direct evidence for the two-axis phase-exit gate. ⭐ **Complexity gate fired anyway (unrated, no freebie spent): both numbers right — O(E log E) / O(E) — but the why-clause was wrong in both directions.** Named the heap as a space term (it isn't; the heaps *are* the adjacency lists) and never named the **recursion stack**, which is the O(E) contributor. ⚠️ **The Aug 4 miss on this same problem was the space *number* (O(V)); tonight the number was right and the word `nodes` was still in the reasoning — a half-transfer.** Written up in `complexity_gotchas.md` as the worked argument for why the gate demands contributors rather than a number. **Rated rep → Tue Aug 18**, gap deliberate (see the Aug 17 preview); fire the complexity gate COLD there<br>✅ ~~🆕 **[739 Daily Temperatures](../../../dsa/leetcode/stack/739_daily_temperatures.py)**~~ · [LC](https://leetcode.com/problems/daily-temperatures/) — *NEW, consolidation.* **🟡 Shaky → Aug 24.** Technique named pre-code and unaided; loop, the `>=` for duplicate temperatures and the guard were all correct **first run** (verified on 5 cases). **Capped by one coach-supplied insight:** the stack held *temperatures*, so the answer — a **distance between days** — was unreachable; *store indices* and the literal `answer[i] = stack[-1] - i` were both mine. ⚠️ **This is a regression: 503 on Aug 10 named "store the index" explicitly**, same technique, four days earlier. ⭐ **Complexity clean and unaided, no freebie** — O(n) with the *amortization* why-clause volunteered (each index pushed once, popped once, so the nested `while` is not O(n²)), O(n) space with the never-popping worst case named. Recognition **not phase-exit evidence** (3rd `stack/` scaffold-path spoil after 150, 155). ⚠️ **Coach error logged** — the call was confirmed as a hit on the technique leg alone; see `self_eval_log.md`<br>✅ ~~[155 Min Stack](../../../dsa/leetcode/stack/155_min_stack.py)~~ · [LC](https://leetcode.com/problems/min-stack/) *(its +2 landed here; it was flagged as missing from this table and is now placed at the rep)* — **🔴 → 🟢 Clean, ⚠️ PROVISIONAL (streak 0) → Aug 24.** Blank page, correct first pass, zero hints; all three Aug 12 `stack[0]`-vs-`stack[-1]` bugs gone. Verified on 2000 randomized op-sequences against a reference. ⭐ **Complexity: the per-call / whole-object split was volunteered** — O(1) per op, O(n) for the object — which is 901 + 146’s design-object convention (Aug 13) transferred cold two days later, **3rd occurrence, no freebie spent**. ⚠️ **+10 not +30, by the Blank→Clean rule:** the pair-with-min design was coach-supplied Aug 12, so this rep measures whether that teach held; the lock-down check is what converts it to a normal Streak 1. Recognition **not phase-exit evidence** (retry, `stack/` scaffold path) | — |

⚠️ **FRIDAY IS 11.0 / 9 AND THE TRIM IS UNRESOLVED — decide at Friday's kickoff, not mid-day.** Board is 743 (2.0) + 332 (3.0) + 739 (3.0) + **155 (3.0, its +2 lands here)**. Moving 138 to Thursday took it from 13.0 to 11.0; the remaining 2.0 has no cheap source, and that is the honest statement of the problem:
- **743 and 332 are protected** — 743 is Dijkstra's last 🟢 chance this week (still zero after 778 came back 🟡 Wed), 332 is Hierholzer's only one.
- **739 is new intake** and the learner set a standing rule Aug 13 that new intake is *not* the default yield (see the Aug 17 preview). Slipping it a second time this week is exactly what that rule forbids.
- **155 is a 🔴 measurement rep** — its +2 asks whether Wednesday's teach stuck. ⚠️ **It was offered a Thursday slot and the learner declined it, correctly:** *"if doing 155 today will always land it a blank, why should I do it today?"* A measurement rep run on session-end fatigue measures fatigue. Same principle as 127's deliberate 3-day gap, which paid off the same day.
- ✅ **The learner is willing to run over:** *"I don't mind doing more tomorrow."* **Running 11.0 knowingly is a legitimate outcome here** — what is not legitimate is discovering it at 22:00. If it must yield, name the order **before** the day starts.

| **Sat Aug 15** | ✅ ~~[572 Subtree of Another Tree](../../../dsa/leetcode/trees/572_subtree_of_another_tree.py)~~ · [LC](https://leetcode.com/problems/subtree-of-another-tree/) *(was overdue Jul 12 — 34-day slip cleared)* — **🟢 → 🟡 Shaky → Aug 25.** Two bugs, one root cause: **both jobs implemented with one function**. First committed to the identity check on any value match (**self-caught from a counterexample** — *"I'm returning the second I find the first match"*); then the match arm called the *search* function on the children, so `[3,4,5,1,null,2]` vs `[3,1,2]` matched values across different branches. **The two-function decomposition was coach-supplied, and for this problem that structure is the algorithm** — which is what carries the 🟡. Recognition **partial**: the pre-code comment named both jobs (*"a tree in root"* / *"exactly same as subRoot"*) but never that they need two functions — the gap in the comment is verbatim the gap in the code. ⭐ **Complexity was the strong half:** time O(m·n) after a correction (space logic does not carry to time — space is a max over one path, time a sum over all), and **space O(h) derived by the learner** — they worked out that once `isSubtree` has descended `d`, `isSameTree` can only go `h−d` further, so the depths trade off and the bound is root's height, not `m+n`. Miss corrected en route: `log m` for a worst-case height. **📌 Expansion-queue item raised by the learner:** the O(m+n) serialize + KMP version — parked behind learning KMP on 28 first | ✅ ~~[787 Cheapest Flights](../../../dsa/leetcode/graphs/787_cheapest_flights_within_k_stops.py)~~ · [LC](https://leetcode.com/problems/cheapest-flights-within-k-stops/) ⚠️ **protected — and it delivered: 🟡 → 🟢 Clean, streak 1 → Sep 14. Bellman-Ford is off the zero-🟢 list**, the second of the five after Prim's. Blank page, zero hints, correct first run (6 fixed cases + 3000 randomized vs brute force). **Both classic traps handled deliberately and with the reasoning written down pre-code**: `k` stops = `k+1` edges, and the snapshot copy (reads `prices`, writes `workingPrices`) — the comment gives the actual reason rather than a remembered rule. Recognition **hit pre-code on the picking feature, not the label**: *"the only shortest path algorithm that allows us to limit number of steps"* — rounds *are* hop count, which is exactly what Dijkstra cannot express. Time O(k·E) itemized unprompted. ⚠️ **Space missed once — O(E) for two arrays that are `[inf] * n`, so O(V)**; corrected from a cue pointing at the line, **freebie spent, no cap** (first miss on this problem). ⭐ **Mirror image of 332's Aug 4 miss** (stack priced in V when it was E) — same V/E axis, opposite direction, so the durable rule logged is *name the index set of each structure you allocated*<br>✅ ~~[1334 Find the City](../../../dsa/leetcode/graphs/1334_find_the_city_with_the_smallest_number_of_neighbors_at_a_threshold_distance.py)~~ · [LC](https://leetcode.com/problems/find-the-city-with-the-smallest-number-of-neighbors-at-a-threshold-distance/) ⚠️ **protected** — **🟡 Shaky → Aug 25. ⚠️ Floyd-Warshall still has ZERO 🟢.** Correct in the end (both worked examples + 3000 randomized vs Dijkstra-from-every-node), complexity clean and unprompted both halves (O(n³) time, O(n²) space). **The block was the representation, not the algorithm**: `distances` was declared 1-D when Floyd-Warshall runs on a V×V matrix — the learner's own pre-code comment said *"source → middle + middle → dst"*, an expression needing two indices. Their three reported symptoms were all correct diagnoses of that one cause. Unblocked by being pointed back at their own comment. **Theirs unaided:** `middle` as the outermost loop (the classic trap), `distances[i][i] = 0`, both directions seeded, the relaxation body, and `<=` for the greatest-index tie-break. **Coach-supplied:** the 2-D shape, the construction syntax (twice), and that the count is a post-pass over one row — plus the recognition discriminator (*all-pairs / every node is a source*; the learner's *"not looking for a specific node to get to"* does not separate this from Dijkstra). ⭐ Learner then took the second half unaided: `n ≤ 100` makes V³ affordable — **all-pairs picks the family, the constraint picks the member**. ⭐ Not a bug, worth knowing: the count includes `j == i`, inflating every city by exactly one — uniform, so the argmin is unaffected. **Right by cancellation, not by design** | ~~SD ① — Redis MEASURE (RATED)~~ **CANCELLED by the Aug 13 rework** — technology rows are off the review engine, so there is no row to log it to. Redis is now HelloInterview study material; the [local card](../../../https://github.com/michael-yrao/sd-progress/blob/main/technologies/redis.md) stays as reference<br>~~🆕 **22 Generate Parentheses**~~ → **SWAPPED OUT for [853 Car Fleet](../../../dsa/leetcode/stack/853_car_fleet.py) · [LC](https://leetcode.com/problems/car-fleet/), same 3.0.** ⚠️ **The learner caught this before attempting it:** *"isn't 22 a backtracking problem… should this not be reserved for the backtracking section. Is there a stack method that I should be focused on right now?"* — both halves correct. **There is no stack technique in 22**; the iterative form is the same recursion with an explicit stack, a presentation variant. It was on this phase only because **the phase table inherits NC150's shelving** and nothing had re-checked it against the actual technique. **Moved to the Backtracking phase (Sep 14–Oct 11), which now runs 10 problems; the Stack phase drops to 7.** Learner's reason, kept verbatim because it is the right one: *"I'd rather not forget everything in a month if I do backtracking early."* ⭐ **Recognition worth noting: the scaffold path handed over the WRONG answer and was overridden** — the previous three new problems were all logged *not measurable* because `<pattern>/` gave the technique away. 853 also removes 3.0 from the over-committed Aug 17 week<br>✅ 🔥 ~~[695 Max Area of Island](../../../dsa/leetcode/graphs/695_max_area_of_island.py)~~ · [LC](https://leetcode.com/problems/max-area-of-island/) — **🟢 Clean, streak 1 → 2 → Oct 14** *(backfill; was overdue since Jul 17 — **29-day slip cleared**)*. Blank page, zero hints, correct first pass (5 fixed cases + 4000 randomized). Recognition hit pre-code, and the load-bearing clause is *"get that land's **area**"* — the one feature separating this from 200. Complexity clean and unprompted on both halves with why-clauses attached: O(rows·cols) time *because visited-marking means no repeated work*, O(rows·cols) space *worst case an all-land grid*. **No freebie spent.** ⭐ **Marked `visited` on PUSH, which is right for unweighted BFS — and is the OPPOSITE of the settle-on-pop correction taken on 743 the previous night.** Not over-generalising a fresh correction onto a problem where it does not apply is the thing to note here. ⭐ **Ran BFS on a row titled "(DFS)"** — deliberate variant choice, now dual-credited to Grid BFS in `techniques.yml` (same convention as 200)<br>**Sat prices 9.0.** ⚠️ Two protected reps here (787 Bellman-Ford, 1334 Floyd-Warshall) — **if the day runs out, 695 yields first, then 22.** Never the protected reps |
| **Sun Aug 16** | [496 Next Greater Element I 🟡](../../../dsa/leetcode/stack/496_next_greater_element_i.py) · [LC](https://leetcode.com/problems/next-greater-element-i/)<br>[208 Implement Trie 🟢](../../../dsa/leetcode/trie/208_implement_trie_prefix_tree.py) · [LC](https://leetcode.com/problems/implement-trie-prefix-tree/) | [261 Graph Valid Tree (DFS) 🟡](../../../dsa/leetcode/graphs/261_graph_valid_tree.py) · [LC](https://leetcode.com/problems/graph-valid-tree/)<br>[98 Validate BST 🟢](../../../dsa/leetcode/trees/98_validate_binary_search_tree.py) · [LC](https://leetcode.com/problems/validate-binary-search-tree/) *(from Mon)*<br>[80 Remove Duplicates II 🟢](../../../dsa/leetcode/two_pointers/80_remove_dup_from_sorted_array_ii.py) · [LC](https://leetcode.com/problems/remove-duplicates-from-sorted-array-ii/) *(from Mon)* | ⏸️ ~~MOCK #1 — Bitly~~ **no SD this week.** SD is in **study mode** until the learner opens a mock — they are working Core Concepts + Key Technologies on HelloInterview first, and the next SD session is a **restructure session** scoping those two pillars to interview depth, called by them. Nothing is deferred to a date<br>🎯 **PROBE #3** (Easy, unseen, label stripped) — the standing unseen-problem rule for a non-SD day; 1.0, no permanent demand<br>🔥 [323 Connected Components — **Union-Find** 🟢](../../../dsa/leetcode/graphs/323_number_of_connected_components_in_an_undirected_graph.py) · [NC](https://neetcode.io/problems/count-connected-components) — *backfill, overdue since Jul 29.* ⚠️ **Same file as Tuesday's DFS row, different method row** — this is the one flagged to protect: coldest of the two, and the only one attached to a live gap (721's equivalence-modeling side)<br>🔥 [2300 Successful Pairs 🟢](../../../dsa/leetcode/binary_search/2300_successful_pairs_of_spells_and_potions.py) · [LC](https://leetcode.com/problems/successful-pairs-of-spells-and-potions/) — *backfill, overdue since Jul 19*<br>**Sunday prices 9.0** — at ceiling, and now the heaviest pure-DSA day of the week<br>⚠️⚠️ **RE-SCAFFOLD 98 AND 80 BEFORE ATTEMPTING** — `python scripts/new_problem.py --number 98 …` and `--number 80 …`. They were scaffolded **Mon Aug 10** and carry `_20260810` stubs with their priors stashed. Writing into a stale-dated stub means `restore_history.py` looks for `_20260816`, finds no dated attempt, and **keeps the stash out** — so the work lands but the history never merges back. Re-running the scaffold is safe and idempotent: an un-attempted stub is dropped and the **existing stash is left untouched, never clobbered** |

</details>
