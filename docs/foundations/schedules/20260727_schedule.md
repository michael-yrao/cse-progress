# Week of July 27 – August 2, 2026

**Goal:** **Convert, don't accumulate.** Advanced Graphs has *zero* of its own problems at 🟢 — by the
new per-algorithm exit standard the phase has not locked down a single algorithm. So this week is
built around turning 🟡s into 🟢s, with new intake held exactly at the cap. **332 Hierholzer is the
critical path**: it's the only 🔴 on the board, and a second Blank means the technique was never
encoded rather than forgotten (teach, don't re-rep). Advanced Graphs finishes new intake this week —
**269 Alien Dictionary** (last NC150 problem in the phase), **1334 Floyd-Warshall**, **721 Accounts
Merge**. SD gives its first slot to the **infrastructure review** at the learner's request.

---

## ⚖️ Capacity check (first build under the surplus rule)

```
107 rows · 1 🔴 · 19 🟡 · 67 🟢s1 · 19 🟢s2 · 1 🟢s0
weekly demand  35.3     capacity  28     SURPLUS  −7.3
```

**Over-subscribed, so: reviews + new intake at the cap, and nothing else.** Per the fill table, a
≤0 surplus means **no consolidation reps and no application pulls** this week.

**Consequence, stated plainly — this changes the Advanced Graphs plan.** 1631 and 1514 were promoted
into the phase Jul 26 as consolidation reps. **They cannot be scheduled while surplus is ≤0**, and the
projection doesn't open surplus until Oct–Dec. They are therefore **deferred out of the Aug 16 phase
window** and become the first thing scheduled when surplus turns positive. The phase exit is unaffected:
exit is judged **per algorithm** (≥1 🟢 + recognition), and Dijkstra already has 743 + 778 — the
consolidation reps deepen it beyond the bar rather than reaching it. **Advanced Graphs is judged on 9,
not 11.**

**8 🟢 reviews slip to the Aug 3 week** — 🟢 carries the most slack and cleaning one only advances
streak 1→2. Listed below so none are lost.

**Per-day load after the Wed/Sun top-up** (cap is 5 DSA problems/day):

| | Mon | Tue | Wed | Thu | Fri | Sat | Sun |
|---|---|---|---|---|---|---|---|
| DSA problems | 5 | 3 | **5** | 4 | 3 | 5 | **4** |
| Other | — | infra | SD ② | — | SD ① | — | SD ③ |

Mon, **Wed**, and Sat sit at the cap; nothing else does. *(Wed rose to 5 on Jul 27 when 269 logged
🔴 and landed there — the learner kept 733 rather than slip it, on the grounds that a basic BFS is a
~5-minute rep. Noted because a day at cap has no absorption left if Tue's 332 also comes back 🔴 and
needs a +2 slot on Thu.)* **The surplus figure measures the *week*, not the
*day*** — a −7.3 week can still contain slack days, and this build had two. Check the per-day row
before accepting a slip list.

---

## Daily Schedule

| Day | Morning Warmup (15 min) | Evening Warmup (15 min) | Active Block (45 min) |
|-----|------------------------|------------------------|-----------------------|
| **Mon Jul 27** | ~~424\. Longest Repeating Char 🟡~~ ✅ **🟢 Clean** *(streak 1 → Aug 26)* + ~~540\. Single Element in Sorted Array 🟡~~ ✅ **🟡 Shaky** *(→ Aug 6)* | ~~18\. Four Sum 🟡~~ ✅ **🟢 Clean** *(streak 1 → Aug 26)* + ~~104\. Max Depth of Binary Tree 🟢~~ ✅ **🟢 Clean** *(streak 2 → Sep 25)* | ~~269\. Alien Dictionary — NEW~~ ✅ **🔴 Blank** *(→ Jul 29)* — Kahn's recalled cold and correct; **all four failures were graph *modeling***, not the algorithm. See `stuck_log.md` |
| **Tue Jul 28** | ~~**SD — INFRASTRUCTURE REVIEW**~~ ✅ topic chosen by coach → **PostgreSQL** (3× deferred, zero coverage, and Sunday's URL shortener needs a datastore). **Teaching session, UNRATED** — [`technologies/postgresql.md`](../system_design/technologies/postgresql.md) written. ⚠️ **Arc half-open: writes/sharding not covered** | ~~19\. Remove Nth Node (Postorder) 🟡~~ ✅ **🟡 Shaky** *(→ Aug 7)* — counter counts from the **end**, compared against a front-indexed `n−1` + ~~169\. Majority Element 🟢~~ ✅ **🟢 Clean** *(streak 2 → Sep 26; over-learned fast-track offered and **declined** — learner chose the extra rep)* | ~~332\. Reconstruct Itinerary 🔴 — RATED re-rep~~ ✅ **🔴 Blank (2nd consecutive)** — became a **TEACHING session** mid-rep, per the pre-written rule. ⚠️ **+2 DELIBERATELY OVERRIDDEN → rated re-rep Tue Aug 4** (see below) |
| **Wed Jul 29** | ~~**SD ② — `components/load_balancer.md`**~~ ⏳ **STARTED, half-open — TEACHING session (UNRATED)**. Derived round robin → least connections from constraints; note's algorithm table filled for both (+ a learner-requested **Tradeoff** column). Taught: least-connections mechanics, and the `% N` rehash arithmetic (`%3`→`%4` keeps only **1/4** of clients). ⚠️ **Outstanding: IP hash · consistent hashing · sticky sessions · the rate-limit interaction table · failure modes · Recall Card** → see close-out below | ~~704\. Binary Search 🟢~~ ✅ **done early Jul 26 → 🎓 GRADUATED** *(over-learned fast-track — first ever graduation in the tracker; next look Jan 22 2027)* · ~~**269\. Alien Dictionary 🔴 — re-rep**~~ ✅ **🟡 Shaky** *(→ Aug 8)* — **🔴→🟡, real movement.** Recognition, full Kahn's, cycle check *and* the **prefix-invalid rule** (one of Monday's four modeling failures) all cold. Two bugs supplied: `words[i]` vs `words[i-1]`, and duplicate edges double-counting indegree. Complexity **repeat miss → capped at 🟡** + **backfilled:** ~~560\. Subarray Sum Equals K 🟢~~ ✅ **🟡 Shaky** *(→ Aug 8)* — map *contents*, not technique: pre-counted the whole array, then missed the `diffMap[0]=1` empty-prefix seed + ~~128\. Longest Consecutive Sequence 🟢~~ ✅ **🟢 Clean** *(streak 2 → Sep 27; fast-track **refused** — no harder problem in Hash Set Membership, so 128 IS the coverage)* + ~~733\. Flood Fill 🟢~~ ✅ **🟢 Clean → 🎓 GRADUATED** *(over-learned fast-track — 2nd graduation; BFS coverage gate held by 127 🟡 / 417 🟡 / 200 / 994. Next look Jan 25 2027)* | ~~**912\. Sort an Array (Merge Sort) 🟡 — RATED re-rep**~~ ✅ **🟡 Shaky** *(→ Aug 8)* — **the D&C teaching STUCK**: skeleton, split/recurse-both, uniform return contract all cold and correct. Sole defect `append` vs `extend` on the leftover slice → gap moved **conceptual → Python API**, so no re-teach |
| **Thu Jul 30** | 235\. LCA of BST 🟡 + 417\. Pacific Atlantic 🟡 | **721\. Accounts Merge — NEW** *(doubled warmup, ~30 min — Union-Find mechanics already 🟢 across 323/684/261, so only the modeling step is new)* | **1334\. Find the City — NEW** (Floyd-Warshall; closes the shortest-path family) |
| **Fri Jul 31** | **SD ① — Redis re-sprint (RATED)** — due today. Drill cards 9 scale-out, 8 ZSet "how", 11 footgun, **+ new card 3b (`EVAL`/Lua, per-command vs across-commands atomicity)** | 503\. Next Greater Element II 🟡 + 543\. Diameter of Binary Tree 🟡 | **127\. Word Ladder 🟡** (Hard; needs the full block) |
| **Sat Aug 1** | 271\. Encode and Decode Strings 🟡 + 875\. Koko Eating Bananas 🟡 | 261\. Graph Valid Tree (Union-Find) 🟡 + 211\. Add and Search Words 🟢 **prov.** | **1584\. Min Cost to Connect All Points 🟡** (Prim's MST) |
| **Sun Aug 2** | 778\. Swim in Rising Water 🟡 + 53\. Maximum Subarray (Prefix Sum) 🟢 | 110\. Balanced Binary Tree 🟢 + 973\. K Closest Points to Origin 🟢 *(pulled back off the slip list — both due today anyway)* | **SD ③ — DESIGN: URL shortener** (resumes at **step 3, API** — steps 1–2 were banked Jul 20). ⚠️ **Gate step 3 on the scale arithmetic being spoken aloud first** — the Jul 26 mock skipped it entirely |

---

## Warmup Notes

- **⚠️ 211 is a provisional lock-down** (🟢 Streak 0, due Aug 2) — the +10 check after its 🔴→🟢. Same
  shape as 787 last week, which **failed** its lock-down. Treat it as higher priority than a normal 🟢.
- **Everything is coded from a blank page** — warmups included. No-code blueprints are an explicit
  opt-in that hard-caps at 🟡.
- **332's two outcomes are not symmetric.** 🟡 or better → the phase's hardest algorithm is alive.
  A second 🔴 → **stop re-repping and teach it** (§2a: nothing to forget means the +2 loop just
  re-blanks). Do not schedule a third sprint on the reflex.
  - **RESOLVED Jul 28: the second 🔴 landed.** Jul 28 converted to a teaching session (derive-the-design
    for the post-order append; direct explanation for the exhaust-all-edges loop). What the learner
    brought cold: graph modeling, min-heap for lexical order, and the *edges-not-nodes* discriminator.
    What was supplied: the Eulerian/Hierholzer name, the post-order insight, the `while` loop, the
    append-`node`-not-`child` fix, and the empty-heap guard removal.
  - ⚠️ **The +2 is overridden to Aug 4, and the tracker CANNOT express that.**
    `update_review_dates.py` recomputes `Next Review Date` from comfort+streak+latest on every run
    (there is no override field), so the tracker will read **2026-07-30** and 332 will show as overdue
    Jul 30 → Aug 4. **That is intentional, not a missed problem.** The schedule is the source of truth
    for this slot. Rationale: a Thursday re-rep would measure recall of Tuesday's teaching conversation,
    not retention — an inflated rating there corrupts every interval computed from it (§2a).
  - **Third-🔴 rule, pre-written so it isn't decided in the moment:** if Aug 4 blanks again, stop
    re-repping Hierholzer entirely and change format — the failure is not the algorithm, it is that
    explanation-then-attempt has now failed twice.
- **Boundary arithmetic still on watch** — 424, 540, 875, 271 all land this week and all four are
  window/pointer boundary problems. State the invariant aloud before running: *inclusive `[l,r]` →
  length `r−l+1`*.
- **721 is new-technique, not a consolidation rep** — the modeling step (the node is an *email*, not
  an account) is genuinely new even though Union-Find mechanics are 🟢. It counts against the cap.

---

## Backlog Status ⚠️

**New intake: 3 — exactly at the Hardest-tier cap** (269, 1334, 721). No 4th, and no consolidation
reps or application pulls, per the −7.3 surplus.

**🟢 slipping to the Aug 3 week (6)** — none lost, all re-slotted at build time:

| Problem | Was due |
|---|---|
| 323 Connected Components (UF) | Jul 29 |
| 66 Plus One · 684 Redundant Connection (UF) | Jul 31 |
| 703 Kth Largest in Stream | Aug 1 |
| 130 Surrounded Regions (UF) | Aug 2 |
| 261 Graph Valid Tree (**DFS**) | Jul 23 — carried; its **Union-Find twin is Sat Aug 1**, so don't stack them |

**Six pulled back off the slip list**, in two passes:
- *First pass (load re-check):* Wed was carrying 1 DSA problem and Sun 2, both far under the cap.
  **704 + 733 → Wed; 110 + 973 → Sun evening** (the Sun pair were due Aug 2 anyway, so not pulled
  forward — just not slipping).
- *Second pass (704 finished early Jul 26):* that vacated Wed's slot, dropping it back to 2.
  **560 + 128 → Wed**, both due Jul 28/29, so again not pulled forward — they land on their own due
  dates. Wed is now at 4.

**Note the pattern:** an early finish *vacates* a future slot, and the vacancy is invisible unless the
per-day row is re-checked. Backfilling is the default when the day is under cap and something on the
slip list is due that day anyway.

**Stale 🟢 pile (~18):** still accepted as stale per the Jul 19 re-baseline policy — each gets a fresh
+30 the next time it's touched. **Not overdue, don't re-list.** And per the capacity math, this pile is
**arithmetic, not neglect**: at −7.3 it cannot drain until demand falls below capacity.

**Phase-gated (not overdue):** 1216 Valid Palindrome III ×2 → Backtracking (Sep 14) / 1D DP (Oct 12).

---

## System Design (3×/week + 1 one-off)

| Lane | Slot | This week |
|------|------|-----------|
| **— (one-off)** | Tue, one warmup | ✅ **PostgreSQL, teaching session (unrated)** — spine (query-by-non-key · transactions · one write machine), ACID with Read Committed as the default, and the read-scaling path: cache → primary+replicas, with the replication-lag tradeoff and the read-your-own-writes fix. **Two follow-ups owed:** (a) the **writes/sharding half** of the note, (b) the first **rated blind sprint**, both → Aug 3 week |
| **① Tech fluency** | Fri, one warmup | **Redis re-sprint (RATED)** — due Jul 31. **New card 3b** from Sunday's mock. *PostgreSQL yields again: a due 🟡 carrying fresh material outranks new intake.* |
| **② Blocks & probes** | Wed, both warmups (~30 min) | ⏳ **`components/load_balancer.md` — STARTED, half-open (teaching session, unrated).** Done: round robin + least connections, derived from constraints, with assumption / breaks / tradeoff each. Also landed the `% N` rehash number (**75% of clients remap** when a 3-server pool becomes 4) — the motivation for consistent hashing, stopped one derive-step short of the ring itself. **Outstanding: IP hash · consistent hashing · sticky sessions · the rate-limit interaction table · failure modes · Recall Card** |
| **③ Designs** | Sun, 45–60 min | **URL shortener**, resuming at step 3 (API) |

**⚠️ The infrastructure slot has a real cost.** It's a 4th SD-shaped slot in a week already at −7.3, so
it comes out of warmup capacity and deepens the 🟢 slip. Accepted deliberately — noting it so it isn't
discovered later.

**Owed from the Jul 28 PostgreSQL teaching session:**
- **Finish the note's writes/sharding half** → next lane ① or ② slot in the Aug 3 week. The arc is
  half-open (reads covered, writes not), and the finish-the-arc guardrail says close it before starting
  a new technology. Includes the **MVCC/vacuum thread**, deliberately deferred from Tuesday — it
  explains both "readers never block writers" and the bloat problem, and bloat is a write-path concern.
- **First RATED blind sprint on PostgreSQL** → Aug 3 week, *after* the writes half exists and with a
  real forgetting gap. Do **not** sprint it this week: it would measure recall of Tuesday's
  conversation, the same §2a error being corrected on 332.

**Owed from the Jul 29 load-balancer session (lane ②, half-open):**
- **Finish `components/load_balancer.md`** → Aug 3 week lane ②. Same finish-the-arc guardrail as the
  PostgreSQL note above: close it before pulling a new block. Outstanding: **IP hash · consistent
  hashing · sticky sessions · the rate-limit interaction table · failure modes · Recall Card.**
- **Resume at the exact derive-step where we stopped**, so the payoff stays the learner's to invent:
  they had identified that `% N` (not hashing) is what breaks, and the open question was *"what could
  you do to the servers, with the same hash function, so a key can find one without counting them?"*
  → the ring. **Do not open by explaining consistent hashing** — one question gets there.
- Already banked and not to be re-taught: round robin, least connections (assumption / breaks /
  tradeoff), the state-that-can't-be-wrong vs state-that-goes-stale distinction, and the 75% remap
  arithmetic.

**Owed from the Jul 26 Rate Limiter mock (not this week):**
- **Rate Limiter Mastery re-rep → Aug 3 week.** §2a forgetting gap on `EVAL`/Lua + LB routing.
- **7-lens fact-check vs Hello Interview's rate-limiter deep-dive** — still deferred; run it *after*
  the re-rep, or against a design that isn't imminent.

⚠️ **SD CONTENTION — the Aug 3 week now has FOUR owed SD items against three lane slots:**
PostgreSQL writes/sharding half · PostgreSQL first rated sprint · Rate Limiter Mastery re-rep ·
load-balancer close-out. **Two lane-② claims (LB close-out, PostgreSQL writes) bid for one slot** —
that's exactly the "lanes bidding for the same slot" failure the lane rule exists to prevent. Resolve
it *at build time*, don't discover it mid-week. Suggested split: **lane ② → LB close-out** (it's the
active cold-hit block and the pull model's own output), **lane ① → PostgreSQL writes half**, and the
**Rate Limiter re-rep + PostgreSQL sprint compete for Sunday** or slip a week with a written reason.

---

## End-of-Week Targets

- [ ] **332 Hierholzer clears to 🟡+** — or is correctly diagnosed as never-encoded and *taught*
- [ ] **Advanced Graphs new intake complete** (269, 1334, 721) — phase closes to conversion-only after this
- [ ] All 15 due 🟡 done
- [x] 912 rated re-rep done (measures the Jul 25 D&C teaching) — **🟡, but the D&C structure held; residual is Python API**
- [ ] Redis cleared to 🟡+ **including card 3b**
- [~] `components/load_balancer.md` written — **half done** (2 of 4 algorithms + tradeoff column); close-out slotted to the Aug 3 week's lane ②
- [ ] URL shortener reaches step 5, **with the scale arithmetic spoken aloud at step 2**
- [ ] Infrastructure review done

---

## Week of Aug 3 Preview

**Advanced Graphs is conversion-only from here** — no new intake. The phase closes **Aug 16** and is
judged **per algorithm** (recognition + execution, ≥1 🟢 each), on **9 problems**, not 11.

**Per-algorithm status is now GENERATED — read [`technique_coverage.md`](../dsa/mastery/technique_coverage.md), don't re-derive it by hand.** Its `no-green` list *is* the phase-exit blocker list (7 techniques as of Jul 28, four of them this phase), and `thin` names what to pull. It also caught two the hand-maintained line below missed: **912's D&C** and **271 length-prefix encoding**, both 🟡 with no 🟢. Kept for reference:

**Per-algorithm status to re-check when building:** Dijkstra (743, 778) · Bellman-Ford (787) ·
Prim (1584) · Hierholzer (332) · BFS-transform (127) · Topo (269) · Floyd-Warshall (1334) ·
Union-Find modeling (721). **As of Jul 26 not one has a 🟢.**

**Carrying in:** the 6 slipped 🟢 above *(12 were slipped at build time; 6 came back on the two re-check passes)* · **540 Single Element in Sorted Array 🟡 due Aug 6** — ⚠️ *5th
attempt still 🟡; if the pair-start-parity invariant has to be supplied again, stop re-repping and
teach it (§2a). See `stuck_log.md`.* · **787 Cheapest Flights 🟡 due Aug 5** (failed lock-down;
watch whether the *queue-free* shape comes back — recognition was never the problem) · **743 due
Aug 4** · **560 Subarray Sum Equals K 🟡 due Aug 8** — *broke a 🟢 streak; the empty-prefix seed is the thing to check cold* ·
**912 Sort an Array (Merge Sort) 🟡 due Aug 8** — *3rd consecutive 🟡, but do NOT read it as the §2a teach-again trigger: the D&C structure is now solid and the failure moved to Python API (`append` vs `extend`)* ·
**269 Alien Dictionary 🟡 due Aug 8** — *🔴→🟡; recognition + Kahn's + prefix rule now cold, so the remaining gap is **edge-set bookkeeping** (dedupe before incrementing indegree), not modeling* ·
⚠️ **THREE rows now stack on Aug 8** (560, 912, 269) — all three are today's 🟡s landing on the same +10. Spread them across Aug 6–10 at build time rather than dropping three onto one day · **⚠️ 332 Reconstruct Itinerary — RATED re-rep, Tue Aug 4 (hard-slotted)**, the measurement
half of Jul 28's teaching session; tracker will show it due Jul 30 and that date is to be ignored ·
**19 Remove Nth Node (Postorder) 🟡 due Aug 7** — ⚠️ *2nd consecutive 🟡 on this variant; both
failures were the counter's **origin** (it counts from the tail), not the recursion itself. A third
supplied off-by-one means the front/back index mixup was never encoded — teach it (§2a).* ·
**Rate Limiter Mastery re-rep** · 1631 + 1514 **only if surplus turns positive**.

**Run the surplus computation again at build time.** It should improve as this week's 🟡s convert to
🟢 s1 (+10 → +30 moves a row from 0.10 to 0.033 demand/day); 15 conversions would recover roughly
7 slots/week — which is the entire deficit.
