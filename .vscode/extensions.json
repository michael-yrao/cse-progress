{
  // No extensions are required. Prior-attempt spoilers used to be hidden by an
  // auto-collapsing fold (zokugun.explicit-folding); they are now physically moved out
  // of the file into <root>/.history/ on a retry and restored at session end
  // (scripts/new_problem.py + scripts/restore_history.py), so nothing needs installing.
  "recommendations": []
}
